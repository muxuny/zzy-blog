<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.ArticleMapper">
    <select id="selectTotalViewCount" resultType="long">
        SELECT COALESCE(SUM(a.view_count), 0)
        FROM article a
        WHERE a.deleted = 0
    </select>

    <select id="selectLowViewArticleCount" resultType="long">
        SELECT COUNT(1)
        FROM article a
        WHERE a.deleted = 0
          AND a.status = 'published'
          AND (a.visibility = 'public' OR a.visibility IS NULL)
          AND a.view_count &lt; #{threshold}
    </select>
</mapper>
