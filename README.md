# Blog Project

个人博客项目总仓库，包含后端、前端和项目文档。

## 目录

- `blog-backend/`：Spring Boot 后端
- `blog-frontend/`：Vue 前端
- `docs/`：设计文档和执行计划

## 本地启动

后端：

```powershell
cd blog-backend
mvn.cmd spring-boot:run
```

前端：

```powershell
cd blog-frontend
npm.cmd run dev -- --host 127.0.0.1
```
