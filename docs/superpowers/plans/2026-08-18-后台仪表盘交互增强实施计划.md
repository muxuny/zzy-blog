# 后台仪表盘交互增强 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不改变现有后台仪表盘布局、模块顺序、标题、内容结构和统计口径的前提下，补齐仪表盘可探索交互、快捷跳转和后台用户状态筛选。

**Architecture:** 仪表盘继续只消费 `GET /api/admin/dashboard/overview`，在 `Dashboard.vue` 内派生前端交互状态和路由目标。后台文章页和用户页读取 URL query 初始化现有筛选状态，用户列表接口增加可选 `status` 参数。数据库结构、dashboard overview DTO 和聚合服务保持不变。

**Tech Stack:** Vue 3 `<script setup>`、Vue Router、Element Plus、Pinia、Vite、node:test、Spring Boot 2.7、MyBatis-Plus 3.5.3.1、JUnit 5、Mockito、AssertJ。

---

## Scope Guard

- 不创建新分支或 worktree，继续使用 `dev_202607_FunctionExpansion`。
- 不调整 `blog-frontend/src/views/admin/Dashboard.vue` 的整体布局：保留 `signal-row`、`dashboard-core`、`lower-grid`、5 张信号卡、3 个核心面板、3 个底部面板。
- 不替换仪表盘现有标题、模块顺序、统计口径和 dashboard overview 数据结构。
- 不新增数据库脚本，不直接修改数据库。
- 不触碰 `blog-backend/src/main/java/com/blog/controller/admin/ArticleGroupController.java`。
- 所有可点击、可跳转、可聚焦元素必须具备主题化 hover 和 `:focus-visible` 反馈；`prefers-reduced-motion: reduce` 时取消位移但保留颜色、边框或阴影反馈。
- “收藏反馈”保持现有模块命名，不新增“收藏趋势”功能。

## File Structure

- Modify: `blog-backend/src/main/java/com/blog/controller/admin/AdminUserController.java`
  - 为后台用户分页增加可选 `status` 查询参数，校验 `pending`、`active`、`disabled`，构造 `LambdaQueryWrapper<User>`。
- Modify: `blog-backend/src/test/java/com/blog/controller/admin/AdminUserControllerTest.java`
  - 更新现有 controller 调用签名，新增状态筛选和非法状态测试。
- Modify: `blog-frontend/src/views/admin/Users.vue`
  - 增加状态筛选控件，读取 `route.query.status` 初始化筛选，加载列表时传入 `status`。
- Modify: `blog-frontend/src/views/admin/Articles.vue`
  - 读取 `route.query.status` 和 `route.query.visibility` 初始化现有文章筛选，支持仪表盘跳转后的精准落点。
- Modify: `blog-frontend/src/views/admin/Dashboard.vue`
  - 在现有布局内补齐信号卡、排行、队列跳转；为阅读柱图、收藏反馈、图例、按钮增加 hover/focus 反馈和范围切换。
- Modify: `blog-frontend/src/utils/listPaginationSource.test.mjs`
  - 增加后台用户分页状态筛选源码契约测试。
- Modify: `blog-frontend/src/utils/adminDashboardTechSource.test.mjs`
  - 增加仪表盘交互、阅读范围、收藏反馈 tooltip、主题化 hover/focus 的源码契约测试。
- Modify: `blog-frontend/src/utils/visualRefreshSource.test.mjs`
  - 保留现有布局契约，补充文章页 query 初始化契约。
- Modify after implementation: `docs/项目状态与目标.md`
  - 功能实现并验证通过后，将“仪表盘交互增强（A+B）”从已排期更新为已完成开发、待页面验收。

---

### Task 1: Backend User Status Filter

**Files:**
- Modify: `blog-backend/src/test/java/com/blog/controller/admin/AdminUserControllerTest.java`
- Modify: `blog-backend/src/main/java/com/blog/controller/admin/AdminUserController.java`

- [ ] **Step 1: Write failing backend tests**

Update existing calls in `AdminUserControllerTest` from:

```java
controller.list(2, 5);
controller.list(1, 101);
```

to:

```java
controller.list(2, 5, null);
controller.list(1, 101, null);
```

Add these imports:

```java
import com.baomidou.mybatisplus.core.MybatisConfiguration;
import com.baomidou.mybatisplus.core.metadata.TableInfoHelper;
import org.apache.ibatis.builder.MapperBuilderAssistant;
import org.mockito.ArgumentCaptor;
```

Add these tests after `listReturnsPagedUsersAndClearsPasswords`:

```java
@Test
@SuppressWarnings({"rawtypes", "unchecked"})
void listFiltersUsersByValidStatus() {
    TableInfoHelper.initTableInfo(
            new MapperBuilderAssistant(new MybatisConfiguration(), ""),
            User.class);
    Page<User> page = new Page<>(1, 10);
    page.setRecords(Collections.emptyList());
    page.setTotal(0);
    when(userService.page(any(Page.class), any(Wrapper.class))).thenReturn(page);

    PageResult<User> result = controller.list(1, 10, "pending");

    assertThat(result.getTotal()).isZero();
    ArgumentCaptor<Wrapper<User>> wrapperCaptor = ArgumentCaptor.forClass(Wrapper.class);
    verify(userService).page(any(Page.class), wrapperCaptor.capture());
    assertThat(wrapperCaptor.getValue().getSqlSegment())
            .contains("status =")
            .contains("ORDER BY")
            .contains("created_at DESC");
}

@Test
@SuppressWarnings({"rawtypes", "unchecked"})
void listTreatsBlankStatusAsAllUsers() {
    TableInfoHelper.initTableInfo(
            new MapperBuilderAssistant(new MybatisConfiguration(), ""),
            User.class);
    Page<User> page = new Page<>(1, 10);
    page.setRecords(Collections.emptyList());
    page.setTotal(0);
    when(userService.page(any(Page.class), any(Wrapper.class))).thenReturn(page);

    controller.list(1, 10, " ");

    ArgumentCaptor<Wrapper<User>> wrapperCaptor = ArgumentCaptor.forClass(Wrapper.class);
    verify(userService).page(any(Page.class), wrapperCaptor.capture());
    assertThat(wrapperCaptor.getValue().getSqlSegment())
            .doesNotContain("status =")
            .contains("ORDER BY")
            .contains("created_at DESC");
}

@Test
@SuppressWarnings({"rawtypes", "unchecked"})
void listRejectsInvalidStatusBeforeQuerying() {
    assertThatThrownBy(() -> controller.list(1, 10, "locked"))
            .isInstanceOf(BusinessException.class)
            .hasMessage("用户状态不合法");

    verify(userService, never()).page(any(Page.class), any(Wrapper.class));
}
```

- [ ] **Step 2: Run backend tests and verify red**

Run:

```powershell
cd D:\03PerProject\blog-backend
mvn -q -Dtest=AdminUserControllerTest test
```

Expected: FAIL at compilation because `AdminUserController.list` still accepts only `page` and `size`.

- [ ] **Step 3: Implement minimal backend filter**

Modify `AdminUserController.java`:

```java
import com.blog.common.BusinessException;
```

Replace `list` with:

```java
@GetMapping
public PageResult<User> list(@RequestParam(defaultValue = "1") long page,
                              @RequestParam(defaultValue = "10") long size,
                              @RequestParam(required = false) String status) {
    PageQueryValidator.validate(page, size);
    String normalizedStatus = normalizeStatus(status);
    LambdaQueryWrapper<User> queryWrapper = new LambdaQueryWrapper<User>()
            .orderByDesc(User::getCreatedAt);
    if (!normalizedStatus.isEmpty()) {
        queryWrapper.eq(User::getStatus, normalizedStatus);
    }
    IPage<User> result = userService.page(new Page<>(page, size), queryWrapper);
    result.getRecords().forEach(u -> u.setPassword(null));
    return PageResult.success(result);
}
```

Add this private helper before `detail`:

```java
private String normalizeStatus(String status) {
    if (status == null || status.trim().isEmpty()) {
        return "";
    }
    String normalized = status.trim();
    if ("pending".equals(normalized) || "active".equals(normalized) || "disabled".equals(normalized)) {
        return normalized;
    }
    throw new BusinessException("用户状态不合法");
}
```

- [ ] **Step 4: Run backend tests and verify green**

Run:

```powershell
cd D:\03PerProject\blog-backend
mvn -q -Dtest=AdminUserControllerTest test
```

Expected: PASS, including default pagination, `status=pending`, blank status, invalid status, and oversized page validation.

- [ ] **Step 5: Commit backend filter**

Check status:

```powershell
cd D:\03PerProject
git status --short
```

Commit only the backend controller and test:

```powershell
git add -- blog-backend/src/main/java/com/blog/controller/admin/AdminUserController.java blog-backend/src/test/java/com/blog/controller/admin/AdminUserControllerTest.java
git commit -m "feat: 支持后台用户状态筛选"
```

---

### Task 2: Admin Users Page Status Filter

**Files:**
- Modify: `blog-frontend/src/utils/listPaginationSource.test.mjs`
- Modify: `blog-frontend/src/views/admin/Users.vue`

- [ ] **Step 1: Write failing frontend source tests**

In `listPaginationSource.test.mjs`, replace the assertion that expects inline `getUsers({ page, size })`:

```js
assert.match(source, /getUsers\(\{\s*page:\s*page\.value,\s*size:\s*size\.value\s*\}\)/)
```

with:

```js
assert.match(source, /const params = \{ page: page\.value, size: size\.value \}/)
assert.match(source, /if \(status\.value\) params\.status = status\.value/)
assert.match(source, /getUsers\(params\)/)
```

Add this test after `admin users page consumes backend pagination`:

```js
test('admin users page reads status query and passes status to backend params', () => {
  const source = read('views/admin/Users.vue')

  assert.match(source, /import \{ computed, onMounted, ref, watch \} from 'vue'/)
  assert.match(source, /import \{ useRoute \} from 'vue-router'/)
  assert.match(source, /const route = useRoute\(\)/)
  assert.match(source, /const userStatusOptions = \[/)
  assert.match(source, /value: 'pending', label: '待审核'/)
  assert.match(source, /value: 'active', label: '已启用'/)
  assert.match(source, /value: 'disabled', label: '已禁用'/)
  assert.match(source, /const status = ref\(normalizeUserStatusQuery\(route\.query\.status\)\)/)
  assert.match(source, /function normalizeUserStatusQuery\(value\)/)
  assert.match(source, /watch\(\s*\(\) => route\.query\.status/)
  assert.match(source, /class="filter-field user-status-filter"/)
  assert.match(source, /aria-label="用户状态"/)
  assert.match(source, /@change="handleFilterChange"/)
})
```

- [ ] **Step 2: Run frontend tests and verify red**

Run:

```powershell
cd D:\03PerProject\blog-frontend
node --test src/utils/listPaginationSource.test.mjs
```

Expected: FAIL because `Users.vue` has no `useRoute`, no `status` filter state, no `watch`, and still calls `getUsers` with an inline object.

- [ ] **Step 3: Implement users page filter**

Modify the template inside `Users.vue` `.panel-head`. Replace the current right-side chip:

```vue
<span class="chip" :class="{ 'is-warning': pendingCount > 0, 'is-success': pendingCount === 0 }">
  本页 {{ pendingCount }} 个待审核
</span>
```

with:

```vue
<div class="user-panel-actions">
  <label class="filter-field user-status-filter">
    状态
    <select v-model="status" aria-label="用户状态" @change="handleFilterChange">
      <option value="">全部状态</option>
      <option v-for="item in userStatusOptions" :key="item.value" :value="item.value">
        {{ item.label }}
      </option>
    </select>
  </label>
  <span class="chip" :class="{ 'is-warning': pendingCount > 0, 'is-success': pendingCount === 0 }">
    本页 {{ pendingCount }} 个待审核
  </span>
</div>
```

Modify the script imports and setup:

```js
import { computed, onMounted, ref, watch } from 'vue'
import { useRoute } from 'vue-router'
```

Add after existing imports:

```js
const userStatusOptions = [
  { value: 'pending', label: '待审核' },
  { value: 'active', label: '已启用' },
  { value: 'disabled', label: '已禁用' }
]
```

Add before `const pageCopyStore = usePageCopyStore()`:

```js
const route = useRoute()
```

Add status state with the existing refs:

```js
const status = ref(normalizeUserStatusQuery(route.query.status))
```

Add this watcher after `onMounted`:

```js
watch(
  () => route.query.status,
  nextStatus => {
    const normalized = normalizeUserStatusQuery(nextStatus)
    if (normalized === status.value) return
    status.value = normalized
    page.value = 1
    void loadUsers()
  }
)
```

Add helpers before `loadUsers`:

```js
function firstQueryValue(value) {
  return Array.isArray(value) ? value[0] : value
}

function normalizeUserStatusQuery(value) {
  const normalized = firstQueryValue(value)
  return userStatusOptions.some(item => item.value === normalized) ? normalized : ''
}
```

Replace the request in `loadUsers`:

```js
const params = { page: page.value, size: size.value }
if (status.value) params.status = status.value
const result = await getUsers(params)
```

Add the filter handler before `handlePageChange`:

```js
function handleFilterChange() {
  page.value = 1
  void loadUsers()
}
```

Add scoped styles before the media query:

```css
.user-panel-actions {
  display: flex;
  align-items: flex-end;
  justify-content: flex-end;
  flex-wrap: wrap;
  gap: 8px;
}

.user-status-filter select {
  width: 132px;
}
```

- [ ] **Step 4: Run frontend users tests and verify green**

Run:

```powershell
cd D:\03PerProject\blog-frontend
node --test src/utils/listPaginationSource.test.mjs
```

Expected: PASS, including the pagination source tests and the new user status query test.

- [ ] **Step 5: Commit users page filter**

Check status:

```powershell
cd D:\03PerProject
git status --short
```

Commit only the users page and test:

```powershell
git add -- blog-frontend/src/views/admin/Users.vue blog-frontend/src/utils/listPaginationSource.test.mjs
git commit -m "feat: 支持后台用户页状态筛选"
```

---

### Task 3: Admin Articles Page Query Landing

**Files:**
- Modify: `blog-frontend/src/utils/visualRefreshSource.test.mjs`
- Modify: `blog-frontend/src/views/admin/Articles.vue`

- [ ] **Step 1: Write failing article query source test**

Add this test after `admin article management keeps filters and row actions compact`:

```js
test('admin article management initializes filters from dashboard route query', () => {
  const source = read('../views/admin/Articles.vue')

  assert.match(source, /import \{ useRoute \} from 'vue-router'/)
  assert.match(source, /const route = useRoute\(\)/)
  assert.match(source, /const status = ref\(normalizeArticleStatusQuery\(route\.query\.status\)\)/)
  assert.match(source, /const visibility = ref\(normalizeArticleVisibilityQuery\(route\.query\.visibility\)\)/)
  assert.match(source, /function normalizeArticleStatusQuery\(value\)/)
  assert.match(source, /Object\.prototype\.hasOwnProperty\.call\(statusMap, normalized\)/)
  assert.match(source, /function normalizeArticleVisibilityQuery\(value\)/)
  assert.match(source, /normalized === 'public' \|\| normalized === 'private'/)
  assert.match(source, /watch\(\s*\(\) => \[route\.query\.status, route\.query\.visibility\]/)
})
```

- [ ] **Step 2: Run frontend source test and verify red**

Run:

```powershell
cd D:\03PerProject\blog-frontend
node --test src/utils/visualRefreshSource.test.mjs
```

Expected: FAIL because `Articles.vue` does not import `useRoute`, does not initialize filters from query, and has no query watcher.

- [ ] **Step 3: Implement article query initialization**

Modify the script import:

```js
import { computed, onMounted, ref, watch } from 'vue'
import { useRoute } from 'vue-router'
```

Add after `const pageCopyStore = usePageCopyStore()`:

```js
const route = useRoute()
```

Replace status and visibility refs:

```js
const status = ref(normalizeArticleStatusQuery(route.query.status))
const visibility = ref(normalizeArticleVisibilityQuery(route.query.visibility))
```

Add this watcher after `onMounted`:

```js
watch(
  () => [route.query.status, route.query.visibility],
  () => {
    const nextStatus = normalizeArticleStatusQuery(route.query.status)
    const nextVisibility = normalizeArticleVisibilityQuery(route.query.visibility)
    if (nextStatus === status.value && nextVisibility === visibility.value) return
    status.value = nextStatus
    visibility.value = nextVisibility
    page.value = 1
    void load()
  }
)
```

Add these helpers before `statusText`:

```js
function firstQueryValue(value) {
  return Array.isArray(value) ? value[0] : value
}

function normalizeArticleStatusQuery(value) {
  const normalized = firstQueryValue(value)
  return Object.prototype.hasOwnProperty.call(statusMap, normalized) ? normalized : ''
}

function normalizeArticleVisibilityQuery(value) {
  const normalized = firstQueryValue(value)
  return normalized === 'public' || normalized === 'private' ? normalized : ''
}
```

- [ ] **Step 4: Run article source test and verify green**

Run:

```powershell
cd D:\03PerProject\blog-frontend
node --test src/utils/visualRefreshSource.test.mjs
```

Expected: PASS, including existing visual refresh source tests and the new dashboard route query landing test.

- [ ] **Step 5: Commit article query landing**

Check status:

```powershell
cd D:\03PerProject
git status --short
```

Commit only the article page and test:

```powershell
git add -- blog-frontend/src/views/admin/Articles.vue blog-frontend/src/utils/visualRefreshSource.test.mjs
git commit -m "feat: 支持后台文章页查询落点"
```

---

### Task 4: Dashboard Interaction Enhancements

**Files:**
- Modify: `blog-frontend/src/utils/adminDashboardTechSource.test.mjs`
- Modify: `blog-frontend/src/views/admin/Dashboard.vue`

- [ ] **Step 1: Write failing dashboard interaction tests**

Add these tests to `adminDashboardTechSource.test.mjs` after the existing tests:

```js
test('admin dashboard keeps current panels while adding actionable route entries', () => {
  const source = read('views/admin/Dashboard.vue')

  assert.match(source, /class="signal-row"/)
  assert.match(source, /class="[^"]*content-structure-panel"/)
  assert.match(source, /class="[^"]*reading-activity-panel"/)
  assert.match(source, /class="[^"]*heat-panel"/)
  assert.match(source, /class="[^"]*favorite-feedback-panel"/)
  assert.match(source, /class="[^"]*resource-health-panel"/)
  assert.match(source, /class="[^"]*priority-queue-panel"/)
  assert.match(source, /class="signal-card is-actionable"/)
  assert.match(source, /@click="goAdminArticles"/)
  assert.match(source, /@click="goPendingQueue"/)
  assert.match(source, /@click="goPublishedPublicArticles"/)
  assert.match(source, /@click="goResources"/)
  assert.match(source, /router\.push\(\{ path: '\/admin\/articles' \}\)/)
  assert.match(source, /router\.push\(\{ path: '\/admin\/articles', query: \{ status: 'pending' \} \}\)/)
  assert.match(source, /router\.push\(\{ path: '\/admin\/users', query: \{ status: 'pending' \} \}\)/)
  assert.match(source, /router\.push\(\{ path: '\/admin\/articles', query: \{ status: 'published', visibility: 'public' \} \}\)/)
  assert.match(source, /router\.push\(\{ path: '\/admin\/resources' \}\)/)
  assert.match(source, /if \(!article\?\.id\) return/)
  assert.doesNotMatch(source, /getAdminArticles/)
  assert.doesNotMatch(source, /getUsers/)
  assert.doesNotMatch(source, /getTags/)
})

test('admin dashboard reading chart range switch uses existing dailyReads data', () => {
  const source = read('views/admin/Dashboard.vue')

  assert.match(source, /const readingRange = ref\(14\)/)
  assert.match(source, /const readingRangeOptions = \[/)
  assert.match(source, /\{ value: 7, label: '7 天' \}/)
  assert.match(source, /\{ value: 14, label: '14 天' \}/)
  assert.match(source, /\{ value: 30, label: '30 天' \}/)
  assert.match(source, /readingRangeOptions/)
  assert.match(source, /@click="readingRange = option\.value"/)
  assert.match(source, /stats\.value\.readingSummary\.dailyReads\.slice\(-readingRange\.value\)/)
  assert.match(source, /'--bar-count': sampleRows\.length \|\| readingRange/)
  assert.match(source, /grid-template-columns:\s*repeat\(var\(--bar-count\),\s*minmax\(0,\s*1fr\)\)/)
  assert.doesNotMatch(source, /getAdminReading/)
})

test('admin dashboard exposes custom hover and focus details for reading and favorite bars', () => {
  const source = read('views/admin/Dashboard.vue')

  assert.match(source, /class="bar-cell"/)
  assert.match(source, /tabindex="0"/)
  assert.match(source, /aria-label="\`\$\{bar\.date\}：\$\{bar\.count\} 次阅读\`"/)
  assert.match(source, /\.bar-cell:hover \.bar-tip/)
  assert.match(source, /\.bar-cell:focus-visible \.bar-tip/)
  assert.doesNotMatch(source, /:title="\`\$\{bar\.date\}：\$\{bar\.count\} 次\`"/)
  assert.match(source, /class="spark-cell"/)
  assert.match(source, /class="spark-bar"/)
  assert.match(source, /class="spark-tip"/)
  assert.match(source, /aria-label="\`\$\{item\.date\}：\$\{item\.count\} 次收藏\`"/)
  assert.match(source, /\.spark-cell:hover \.spark-tip/)
  assert.match(source, /\.spark-cell:focus-visible \.spark-tip/)
  assert.doesNotMatch(source, /:title="\`\$\{item\.date\}：\$\{item\.count\} 次\`"/)
})

test('admin dashboard interactive feedback is theme-token based and motion aware', () => {
  const source = read('views/admin/Dashboard.vue')

  assert.match(source, /\.signal-card\.is-actionable:hover/)
  assert.match(source, /\.signal-card\.is-actionable:focus-visible/)
  assert.match(source, /\.rank-item\.is-actionable:hover/)
  assert.match(source, /\.rank-item\.is-actionable:focus-visible/)
  assert.match(source, /\.queue-item:hover,\s*\.queue-item:focus-visible/)
  assert.match(source, /\.range-tabs button:hover,\s*\.range-tabs button:focus-visible/)
  assert.match(source, /\.legend-row:hover,\s*\.legend-row:focus-visible/)
  assert.match(source, /cursor:\s*pointer;/)
  assert.match(source, /@media \(prefers-reduced-motion: reduce\)/)
  assert.match(source, /transform:\s*none;/)
  assert.match(source, /var\(--primary-color\)/)
  assert.match(source, /var\(--accent-color\)/)
  assert.match(source, /var\(--theme-glow-color\)/)
  assert.doesNotMatch(source, /#[0-9a-fA-F]{6}/)
  assert.doesNotMatch(source, /rgba\(255,\s*255,\s*255/)
})
```

Update the existing reading tooltip test expectations from `.bar:hover .bar-tip` to `.bar-cell:hover .bar-tip` and add `.bar-cell:focus-visible .bar-tip`:

```js
assert.match(source, /\.bar-cell:hover \.bar-tip/)
assert.match(source, /\.bar-cell:focus-visible \.bar-tip/)
```

Update the existing chart rhythm test to accept the dynamic CSS variable while keeping the same chart structure:

```js
assert.match(source, /sampleRows/)
assert.match(source, /\.chart-bars\s*\{[^}]*grid-template-columns:\s*repeat\(var\(--bar-count\),\s*minmax\(0,\s*1fr\)\)/)
assert.match(source, /\.chart-bars \.bar\s*\{[^}]*min-height:\s*16px;/)
assert.doesNotMatch(source, /\.chart-bars\s*\{[^}]*grid-template-columns:\s*repeat\(30/)
```

- [ ] **Step 2: Run dashboard source tests and verify red**

Run:

```powershell
cd D:\03PerProject\blog-frontend
node --test src/utils/adminDashboardTechSource.test.mjs
```

Expected: FAIL because the current dashboard signal cards are static `section` elements, content heat rows are static `div` elements, favorite bars rely on `title`, route helpers are missing, and range switching does not exist.

- [ ] **Step 3: Implement dashboard routes without changing layout**

In `Dashboard.vue`, keep `signal-row` order and card text. Convert only actionable signal cards from `section` to `button`:

```vue
<button class="signal-card is-actionable" type="button" @click="goAdminArticles">
  <div class="signal-topline">
    <span class="signal-icon">文</span>
    <span class="signal-code">A-01</span>
  </div>
  <small>全站文章</small>
  <strong>{{ stats.metrics.totalArticles }}</strong>
  <span>已发布 {{ stats.metrics.publishedArticles }} / 待审核 {{ stats.metrics.pendingArticles }}</span>
</button>
```

Use the same conversion for `Q-02`, `V-03`, and `S-05`. Keep `R-04` as:

```vue
<section class="signal-card">
  <div class="signal-topline">
    <span class="signal-icon">读</span>
    <span class="signal-code">R-04</span>
  </div>
  <small>30 日阅读活跃</small>
  <strong>{{ stats.readingSummary.recent30Days }}</strong>
  <span>登录用户阅读记录</span>
</section>
```

Add route helpers before `goArticle`:

```js
function goAdminArticles() {
  router.push({ path: '/admin/articles' })
}

function goPendingQueue() {
  if (stats.value.metrics.pendingArticles > 0 || stats.value.metrics.pendingUsers === 0) {
    router.push({ path: '/admin/articles', query: { status: 'pending' } })
    return
  }
  router.push({ path: '/admin/users', query: { status: 'pending' } })
}

function goPublishedPublicArticles() {
  router.push({ path: '/admin/articles', query: { status: 'published', visibility: 'public' } })
}

function goResources() {
  router.push({ path: '/admin/resources' })
}
```

Update existing helpers:

```js
function goArticle(article) {
  if (!article?.id) return
  router.push(`/admin/articles/edit/${article.id}`)
}

function goUsers() {
  router.push({ path: '/admin/users', query: { status: 'pending' } })
}
```

- [ ] **Step 4: Implement reading range switch and focusable bars**

Add after `const hasWork`:

```js
const readingRange = ref(14)
const readingRangeOptions = [
  { value: 7, label: '7 天' },
  { value: 14, label: '14 天' },
  { value: 30, label: '30 天' }
]
```

Replace the `reading-activity-panel` head right side with an additive control that keeps the existing `阅读历史` chip:

```vue
<div class="reading-head-tools">
  <span class="micro-pill">阅读历史</span>
  <div class="range-tabs" aria-label="阅读范围">
    <button
      v-for="option in readingRangeOptions"
      :key="option.value"
      type="button"
      :class="{ 'is-active': readingRange === option.value }"
      @click="readingRange = option.value"
    >
      {{ option.label }}
    </button>
  </div>
</div>
```

Replace chart bars with focusable cells and a dynamic bar count:

```vue
<div
  v-if="sampleRows.length"
  class="chart-bars"
  :style="{ '--bar-count': sampleRows.length || readingRange }"
>
  <span
    v-for="bar in sampleRows"
    :key="bar.date"
    class="bar-cell"
    tabindex="0"
    :aria-label="`${bar.date}：${bar.count} 次阅读`"
  >
    <span class="bar" :style="{ height: `${bar.height}%` }">
      <span class="bar-tip" aria-hidden="true">{{ bar.date }} · {{ bar.count }} 次</span>
    </span>
  </span>
</div>
```

Replace `sampleRows`:

```js
const sampleRows = computed(() => {
  const rows = stats.value.readingSummary.dailyReads.slice(-readingRange.value)
  const max = Math.max(1, ...rows.map(item => item.count))
  return rows.map(item => ({
    date: item.date,
    count: item.count,
    height: item.count ? Math.max(6, Math.round((item.count / max) * 100)) : 0
  }))
})
```

Replace `dailyAxis`:

```js
const dailyAxis = computed(() => {
  const rows = sampleRows.value
  if (!rows.length) return ['01', '04', '07', '10', '14']
  const last = rows.length - 1
  const indexes = [0, Math.floor(last * 0.25), Math.floor(last * 0.5), Math.floor(last * 0.75), last]
  return [...new Set(indexes)]
    .map(index => rows[index]?.date?.slice(5) || '')
    .filter(Boolean)
})
```

- [ ] **Step 5: Implement favorite feedback tooltip and clickable heat rows**

Replace the favorite sparkline:

```vue
<div v-if="hasFavoriteTrend" class="sparkline">
  <span
    v-for="(item, index) in favoriteSpark"
    :key="item.date || index"
    class="spark-cell"
    tabindex="0"
    :aria-label="`${item.date}：${item.count} 次收藏`"
  >
    <span class="spark-bar" :style="{ height: `${item.height}%` }">
      <span class="spark-tip" aria-hidden="true">{{ item.date }} · {{ item.count }} 次收藏</span>
    </span>
  </span>
</div>
```

Replace the heat ranking list body with actionable rows only when an article id exists:

```vue
<div v-if="stats.trafficSummary.topArticles.length" class="rank-list">
  <template v-for="(item, index) in stats.trafficSummary.topArticles" :key="item.id || index">
    <button
      v-if="item.id"
      class="rank-item is-actionable"
      type="button"
      @click="goArticle(item)"
    >
      <span class="rank-no">{{ pad(index + 1) }}</span>
      <div>
        <strong>{{ item.title }}</strong>
        <small>浏览 {{ formatCompact(item.viewCount) }}</small>
      </div>
      <span class="rank-value">{{ formatCompact(item.viewCount) }}</span>
    </button>
    <div v-else class="rank-item">
      <span class="rank-no">{{ pad(index + 1) }}</span>
      <div>
        <strong>{{ item.title }}</strong>
        <small>浏览 {{ formatCompact(item.viewCount) }}</small>
      </div>
      <span class="rank-value">{{ formatCompact(item.viewCount) }}</span>
    </div>
  </template>
</div>
```

- [ ] **Step 6: Implement legend focus and theme-compatible feedback styles**

Add `tabindex` and labels to status legend rows:

```vue
<div
  v-for="item in stats.articleStatus"
  :key="item.key"
  class="legend-row"
  tabindex="0"
  :aria-label="`${item.label}：${item.percent}%`"
>
  <span><i class="legend-dot" :style="{ background: statusColor(item.key) }"></i>{{ item.label }}</span>
  <strong>{{ item.percent }}%</strong>
</div>
```

Update visibility rows in the same `ring-block`:

```vue
<div class="legend-row" tabindex="0" :aria-label="`公开文章：${visibilityPercent.public}%`"><span><i class="legend-dot" style="background:var(--dash-teal)"></i>公开文章</span><strong>{{ visibilityPercent.public }}%</strong></div>
<div class="legend-row" tabindex="0" :aria-label="`私密文章：${visibilityPercent.private}%`"><span><i class="legend-dot" style="background:var(--dash-plum)"></i>私密文章</span><strong>{{ visibilityPercent.private }}%</strong></div>
<div class="legend-row" tabindex="0" :aria-label="`内容维护压力：${maintenanceLevel}`"><span><i class="legend-dot" style="background:var(--dash-amber)"></i>内容维护压力</span><strong>{{ maintenanceLevel }}</strong></div>
```

Add dashboard-scoped CSS without changing grid definitions:

```css
button.signal-card,
button.rank-item {
  width: 100%;
  color: inherit;
  font: inherit;
  text-align: left;
  appearance: none;
}

.signal-card.is-actionable,
.rank-item.is-actionable,
.queue-item,
.range-tabs button {
  cursor: pointer;
  transition: border-color 0.16s ease, background 0.16s ease, box-shadow 0.16s ease, transform 0.16s ease;
}

.signal-card.is-actionable:hover,
.signal-card.is-actionable:focus-visible,
.rank-item.is-actionable:hover,
.rank-item.is-actionable:focus-visible,
.queue-item:hover,
.queue-item:focus-visible {
  border-color: color-mix(in srgb, var(--primary-color) 42%, var(--border-color));
  background:
    linear-gradient(135deg, color-mix(in srgb, var(--primary-color) 10%, transparent), transparent 72%),
    color-mix(in srgb, var(--panel-bg) 86%, var(--bg-color));
  box-shadow: 0 18px 36px var(--theme-glow-color);
  transform: translateY(-2px);
  outline: 0;
}

.reading-head-tools {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  flex-wrap: wrap;
  gap: 8px;
}

.range-tabs {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 3px;
  border: 1px solid var(--dash-border);
  border-radius: 999px;
  background: color-mix(in srgb, var(--panel-bg) 70%, transparent);
}

.range-tabs button {
  min-width: 48px;
  min-height: 26px;
  border: 1px solid transparent;
  border-radius: 999px;
  color: var(--muted-text-color);
  background: transparent;
  font-size: 12px;
  font-weight: 760;
}

.range-tabs button:hover,
.range-tabs button:focus-visible,
.range-tabs button.is-active {
  border-color: color-mix(in srgb, var(--primary-color) 34%, transparent);
  color: var(--text-color);
  background: color-mix(in srgb, var(--primary-color) 10%, transparent);
  box-shadow: 0 8px 18px var(--theme-glow-color);
  outline: 0;
}

.legend-row {
  position: relative;
  min-height: 22px;
  margin: 0 -6px;
  padding: 2px 6px 2px 8px;
  border-radius: 7px;
  outline: 0;
  transition: color 0.16s ease, background 0.16s ease, box-shadow 0.16s ease;
}

.legend-row:hover,
.legend-row:focus-visible {
  color: var(--text-color);
  background: color-mix(in srgb, var(--primary-color) 8%, transparent);
  box-shadow: inset 2px 0 0 color-mix(in srgb, var(--primary-color) 58%, transparent);
}
```

Replace chart and sparkline styles:

```css
.chart-bars {
  position: absolute;
  inset: 42px 18px 34px;
  display: grid;
  grid-template-columns: repeat(var(--bar-count), minmax(0, 1fr));
  align-items: end;
  gap: 8px;
}

.chart-bars > .bar-cell,
.spark-cell {
  position: relative;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  min-width: 0;
  height: 100%;
  outline: 0;
}

.chart-bars > .bar-cell:focus-visible .bar,
.spark-cell:focus-visible .spark-bar {
  box-shadow: 0 0 0 3px var(--primary-ring-color), 0 8px 18px color-mix(in srgb, var(--dash-teal) 18%, transparent);
}

.bar-cell:hover .bar-tip,
.bar-cell:focus-visible .bar-tip,
.spark-cell:hover .spark-tip,
.spark-cell:focus-visible .spark-tip {
  opacity: 1;
  visibility: visible;
  transform: translate(-50%, -2px);
}

.sparkline {
  display: flex;
  align-items: end;
  gap: 6px;
  height: 48px;
  margin-top: 10px;
  padding: 0 2px;
}

.spark-cell {
  flex: 1;
}

.spark-bar {
  position: relative;
  width: 100%;
  min-width: 5px;
  min-height: 8px;
  border-radius: 999px 999px 3px 3px;
  background: linear-gradient(180deg, var(--dash-plum), var(--dash-teal));
}

.bar-tip,
.spark-tip {
  position: absolute;
  bottom: calc(100% + 8px);
  left: 50%;
  z-index: 5;
  padding: 5px 8px;
  border: 1px solid color-mix(in srgb, var(--dash-teal) 24%, transparent);
  border-radius: 7px;
  color: color-mix(in srgb, var(--dash-teal) 86%, var(--text-color));
  background: color-mix(in srgb, var(--panel-bg) 96%, transparent);
  box-shadow: 0 8px 18px color-mix(in srgb, var(--text-color) 14%, transparent);
  font-size: 12px;
  font-weight: 780;
  line-height: 1;
  white-space: nowrap;
  pointer-events: none;
  opacity: 0;
  visibility: hidden;
  transform: translateX(-50%);
  transition: opacity 0.15s ease, visibility 0.15s ease, transform 0.15s ease;
}
```

Add reduced motion styles before container queries:

```css
@media (prefers-reduced-motion: reduce) {
  .signal-card.is-actionable,
  .rank-item.is-actionable,
  .queue-item,
  .range-tabs button,
  .legend-row,
  .bar-tip,
  .spark-tip {
    transition-duration: 0.01ms;
  }

  .signal-card.is-actionable:hover,
  .signal-card.is-actionable:focus-visible,
  .rank-item.is-actionable:hover,
  .rank-item.is-actionable:focus-visible,
  .queue-item:hover,
  .queue-item:focus-visible {
    transform: none;
  }
}
```

- [ ] **Step 7: Run dashboard source tests and verify green**

Run:

```powershell
cd D:\03PerProject\blog-frontend
node --test src/utils/adminDashboardTechSource.test.mjs
```

Expected: PASS, including existing tech control tests and new interaction tests.

- [ ] **Step 8: Commit dashboard interaction changes**

Check status:

```powershell
cd D:\03PerProject
git status --short
```

Commit only the dashboard component and dashboard source test:

```powershell
git add -- blog-frontend/src/views/admin/Dashboard.vue blog-frontend/src/utils/adminDashboardTechSource.test.mjs
git commit -m "feat: 增强后台仪表盘交互入口"
```

---

### Task 5: Full Verification And Status Documentation

**Files:**
- Modify: `docs/项目状态与目标.md`

- [ ] **Step 1: Run backend full tests**

Run:

```powershell
cd D:\03PerProject\blog-backend
mvn -q test
```

Expected: exit code 0. Read the output and confirm no failures or errors.

- [ ] **Step 2: Run frontend full tests**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test
```

Expected: exit code 0 and a final node:test summary with all tests passing.

- [ ] **Step 3: Run frontend build**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd run build
```

Expected: exit code 0. A Vite chunk-size warning is acceptable if it matches the existing warning pattern and there are no build errors.

- [ ] **Step 4: Confirm protected files and DB scripts are untouched**

Run:

```powershell
cd D:\03PerProject
git status --short
git diff --name-only
```

Expected changed files at this point are only implementation files from Tasks 1-4 plus `docs/项目状态与目标.md` after the next step. Confirm these paths are absent:

```text
blog-backend/src/main/java/com/blog/controller/admin/ArticleGroupController.java
blog-backend/src/main/resources/db/migration/
```

- [ ] **Step 5: Update project status**

In `docs/项目状态与目标.md`, move the dashboard interaction line from `已排期但未开始` to the completed/current progress area with this wording:

```markdown
后台仪表盘交互增强（A+B）已完成开发，待页面验收：在不改变现有仪表盘布局、模块顺序、标题、内容结构和统计口径的前提下，补齐指标卡、内容热度排行、待处理队列、阅读活跃、收藏反馈和图例的 hover/focus 反馈、悬停明细、阅读范围切换和快捷跳转；后台用户列表新增最小 `status` 筛选以支持 `/admin/users?status=pending` 精准落点。本轮未新增数据库结构，未扩展 dashboard overview 响应。
```

- [ ] **Step 6: Commit status documentation**

Check status:

```powershell
cd D:\03PerProject
git status --short
git diff -- docs/项目状态与目标.md
```

Commit only the status document:

```powershell
git add -- docs/项目状态与目标.md
git commit -m "docs: 更新仪表盘交互增强状态"
```

- [ ] **Step 7: Final clean-state check**

Run:

```powershell
cd D:\03PerProject
git status --short
git log -5 --oneline
```

Expected: `git status --short` has no output. The last commits include the backend filter, users page filter, article query landing, dashboard interaction changes, and status documentation.
