# 阅读位置与进度 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为登录用户保存公开文章最近阅读位置和阅读进度，并在文章详情页由用户点击恢复，在“我的阅读”和“阅读历史”展示进度。

**Architecture:** 扩展现有 `article_reading_history` 表和阅读历史服务，不新增独立位置表。文章详情公开接口在登录用户成功记录历史后附带当前用户的阅读位置状态；`/api/my/reading/history/{articleId}/position` 负责保存位置。前端新增阅读位置纯函数和 API，文章详情页节流保存并展示轻量恢复提示，阅读概览和历史页复用增强后的历史条目。

**Tech Stack:** Java 8、Spring Boot 2.7、Spring Security、MyBatis-Plus、MySQL 8、JUnit 5、Mockito、Vue 3、Vue Router、Element Plus、Axios、Node.js test runner、Vite

---

## 实施约束

- 在当前分支 `dev_202607_FunctionEnhancementV3` 开发，不创建新分支或 worktree。
- 严格按 TDD 顺序：先写失败测试，确认失败，再写实现，再确认通过。
- 数据库只生成、校验和提交增量脚本，不执行数据库脚本，不直接修改数据库。
- 新增业务时间字段使用独立字段，不能复用 `created_at` 或 `updated_at`。
- 不启动前后端服务；完成自动化测试和构建即可。
- 每个任务提交前运行 `git status --short`，只暂存该任务相关文件。
- commit 使用英文规范类型前缀，冒号后的摘要和详细说明使用中文。

## 文件结构

### 后端新增

- `blog-backend/src/main/resources/db/migration/2026-07-21-新增阅读位置与进度字段.sql`：为 `article_reading_history` 增加阅读位置和进度字段。
- `blog-backend/src/main/java/com/blog/dto/ReadingPositionRequest.java`：保存位置请求体。
- `blog-backend/src/main/java/com/blog/dto/ReadingPositionState.java`：文章详情返回的当前用户阅读位置状态。

### 后端修改

- `blog-backend/src/main/resources/db/init.sql`：同步完整表结构。
- `blog-backend/src/main/resources/mapper/ArticleReadingHistoryMapper.xml`：增加位置字段查询、保存位置 SQL、重新激活时位置重置。
- `blog-backend/src/main/java/com/blog/entity/ArticleReadingHistory.java`：增加位置字段。
- `blog-backend/src/main/java/com/blog/entity/Article.java`：增加不映射数据库的 `readingPosition`。
- `blog-backend/src/main/java/com/blog/dto/ReadingHistoryRelationRow.java`：增加位置字段和当前文章更新时间。
- `blog-backend/src/main/java/com/blog/dto/ReadingHistoryItem.java`：增加进度和恢复状态输出字段。
- `blog-backend/src/main/java/com/blog/mapper/ArticleReadingHistoryMapper.java`：声明保存位置和单条历史查询方法。
- `blog-backend/src/main/java/com/blog/service/ReadingHistoryService.java`：声明保存位置和读取位置状态方法。
- `blog-backend/src/main/java/com/blog/service/impl/ReadingHistoryServiceImpl.java`：实现位置保存、输出和恢复判断。
- `blog-backend/src/main/java/com/blog/controller/ReadingController.java`：暴露保存位置接口。
- `blog-backend/src/main/java/com/blog/controller/ArticleController.java`：登录用户详情响应附带阅读位置状态。
- `blog-backend/src/test/java/com/blog/config/DatabaseConfigTest.java`：锁定新增字段和增量脚本。
- `blog-backend/src/test/java/com/blog/service/ReadingHistoryServiceImplTest.java`：覆盖位置保存与恢复判断。
- `blog-backend/src/test/java/com/blog/controller/ReadingControllerTest.java`：覆盖新接口映射和调用。
- `blog-backend/src/test/java/com/blog/controller/ArticleControllerTest.java`：覆盖文章详情附带位置状态。

### 前端新增

- `blog-frontend/src/utils/readingPosition.js`：进度计算、payload 归一化、恢复提示判断、保存节流判断。
- `blog-frontend/src/utils/readingPosition.test.mjs`：阅读位置纯函数测试；Task 4 再扩展文章详情源码契约测试。

### 前端修改

- `blog-frontend/src/api/reading.js`：增加保存阅读位置 API。
- `blog-frontend/src/utils/readingHistory.js`：增加进度展示格式化函数。
- `blog-frontend/src/utils/readingHistory.test.mjs`：增加 API 和列表展示契约。
- `blog-frontend/src/views/ArticleDetail.vue`：展示恢复提示、点击恢复、节流保存、离开前补保存。
- `blog-frontend/src/views/ReadingSpace.vue`：展示“读到 xx%”。
- `blog-frontend/src/components/ReadingHistoryItem.vue`：展示“读到 xx%”。
- `docs/项目状态与目标.md`：实现后同步状态。

---

### Task 1: 建立阅读位置数据库和 Mapper 契约

**Files:**
- Create: `blog-backend/src/main/resources/db/migration/2026-07-21-新增阅读位置与进度字段.sql`
- Modify: `blog-backend/src/main/resources/db/init.sql`
- Modify: `blog-backend/src/main/resources/mapper/ArticleReadingHistoryMapper.xml`
- Modify: `blog-backend/src/main/java/com/blog/entity/ArticleReadingHistory.java`
- Modify: `blog-backend/src/main/java/com/blog/dto/ReadingHistoryRelationRow.java`
- Modify: `blog-backend/src/main/java/com/blog/mapper/ArticleReadingHistoryMapper.java`
- Modify: `blog-backend/src/test/java/com/blog/config/DatabaseConfigTest.java`

- [ ] **Step 1: 写数据库与 Mapper 失败测试**

在 `DatabaseConfigTest` 增加测试。保留已有测试，将 `readingHistorySchemaIncludesBusinessAndCommonFields` 从“旧迁移与 init 完全相等”调整为“旧迁移保留基础字段，init 包含基础字段和位置字段”，并新增以下测试：

```java
@Test
void readingPositionMigrationAddsOnlyPositionColumns() throws IOException {
    String migration = readString(Paths.get(
            "src/main/resources/db/migration/2026-07-21-新增阅读位置与进度字段.sql"));
    String normalized = normalizeSql(migration);

    assertTrue(normalized.startsWith("alter table `article_reading_history`"));
    for (String columnDefinition : Arrays.asList(
            "add column `progress_percent` int not null default 0 comment '阅读进度百分比'",
            "add column `scroll_y` int not null default 0 comment '最近阅读滚动位置'",
            "add column `anchor_id` varchar(160) default null comment '最近阅读标题锚点'",
            "add column `anchor_offset` int default null comment '相对锚点偏移'",
            "add column `article_updated_at_snapshot` datetime default null comment '保存位置时文章更新时间快照'",
            "add column `position_updated_at` datetime default null comment '最近保存阅读位置时间'")) {
        assertTrue(normalized.contains(columnDefinition), "Missing position migration column " + columnDefinition);
    }
    assertTrue(!normalized.contains("drop "));
    assertTrue(!normalized.contains("delete from"));
    assertTrue(!normalized.contains("truncate "));
    assertTrue(!normalized.contains("created_at datetime not null"));
    assertTrue(!normalized.contains("updated_at datetime not null"));
}

@Test
void readingHistoryInitSchemaIncludesPositionFieldsWithoutAuditReuse() throws IOException {
    String initSql = readString(Paths.get("src/main/resources/db/init.sql"));
    String initDefinition = normalizeSql(extractReadingHistoryTableDefinition(initSql, "init.sql"));

    for (String columnDefinition : Arrays.asList(
            "`progress_percent` int not null default 0 comment '阅读进度百分比',",
            "`scroll_y` int not null default 0 comment '最近阅读滚动位置',",
            "`anchor_id` varchar(160) default null comment '最近阅读标题锚点',",
            "`anchor_offset` int default null comment '相对锚点偏移',",
            "`article_updated_at_snapshot` datetime default null comment '保存位置时文章更新时间快照',",
            "`position_updated_at` datetime default null comment '最近保存阅读位置时间',")) {
        assertTrue(initDefinition.contains(columnDefinition), "Missing init position column " + columnDefinition);
    }
    assertTrue(initDefinition.indexOf("`read_count` int not null default 1 comment '阅读次数',")
            < initDefinition.indexOf("`progress_percent` int not null default 0 comment '阅读进度百分比',"));
    assertTrue(initDefinition.indexOf("`position_updated_at` datetime default null comment '最近保存阅读位置时间',")
            < initDefinition.indexOf("`created_by` varchar(50) default null comment '创建人',"));
}

@Test
void readingHistoryMapperPersistsAndResetsPositionContract() throws IOException {
    String xml = readString(Paths.get("src/main/resources/mapper/ArticleReadingHistoryMapper.xml"));
    String upsert = extractMapperStatement(xml, "insert", "upsertHistory");
    String savePosition = extractMapperStatement(xml, "update", "updatePosition");
    String activeHistory = extractMapperStatement(xml, "select", "selectActiveHistory");

    assertTrue(upsert.contains("progress_percent = if(deleted = 1, 0, progress_percent)"));
    assertTrue(upsert.contains("scroll_y = if(deleted = 1, 0, scroll_y)"));
    assertTrue(upsert.contains("anchor_id = if(deleted = 1, null, anchor_id)"));
    assertTrue(upsert.contains("anchor_offset = if(deleted = 1, null, anchor_offset)"));
    assertTrue(upsert.contains("article_updated_at_snapshot = if(deleted = 1, null, article_updated_at_snapshot)"));
    assertTrue(upsert.contains("position_updated_at = if(deleted = 1, null, position_updated_at)"));

    assertTrue(savePosition.contains(normalizeWhitespace(
            "UPDATE article_reading_history h INNER JOIN article a ON a.id = h.article_id "
                    + "AND a.deleted = 0 AND a.status = 'published' "
                    + "AND (a.visibility = 'public' OR a.visibility IS NULL)")));
    assertTrue(savePosition.contains(normalizeWhitespace(
            "WHERE h.user_id = #{userId} AND h.article_id = #{articleId} AND h.deleted = 0")));
    assertTrue(savePosition.contains("h.progress_percent = #{progressPercent}"));
    assertTrue(savePosition.contains("h.article_updated_at_snapshot = #{articleUpdatedAt}"));
    assertTrue(savePosition.contains("h.position_updated_at = #{now}"));

    assertTrue(activeHistory.contains(normalizeWhitespace(
            "WHERE h.user_id = #{userId} AND h.article_id = #{articleId} AND h.deleted = 0")));
    assertTrue(activeHistory.contains("a.updated_at AS article_updated_at"));
}
```

- [ ] **Step 2: 运行数据库契约测试并确认失败**

Run:

```powershell
cd D:\03PerProject\blog-backend
mvn.cmd -q -Dtest=DatabaseConfigTest test
```

Expected: FAIL，提示 `2026-07-21-新增阅读位置与进度字段.sql` 不存在或 Mapper 缺少 `updatePosition`。

- [ ] **Step 3: 创建增量脚本**

创建 `blog-backend/src/main/resources/db/migration/2026-07-21-新增阅读位置与进度字段.sql`：

```sql
ALTER TABLE `article_reading_history`
    ADD COLUMN `progress_percent` INT NOT NULL DEFAULT 0 COMMENT '阅读进度百分比' AFTER `read_count`,
    ADD COLUMN `scroll_y` INT NOT NULL DEFAULT 0 COMMENT '最近阅读滚动位置' AFTER `progress_percent`,
    ADD COLUMN `anchor_id` VARCHAR(160) DEFAULT NULL COMMENT '最近阅读标题锚点' AFTER `scroll_y`,
    ADD COLUMN `anchor_offset` INT DEFAULT NULL COMMENT '相对锚点偏移' AFTER `anchor_id`,
    ADD COLUMN `article_updated_at_snapshot` DATETIME DEFAULT NULL COMMENT '保存位置时文章更新时间快照' AFTER `anchor_offset`,
    ADD COLUMN `position_updated_at` DATETIME DEFAULT NULL COMMENT '最近保存阅读位置时间' AFTER `article_updated_at_snapshot`;
```

- [ ] **Step 4: 同步 init 表结构和实体字段**

在 `init.sql` 的 `article_reading_history` 表中，将新增字段放在 `read_count` 和 `created_by` 之间：

```sql
    `read_count` INT NOT NULL DEFAULT 1 COMMENT '阅读次数',
    `progress_percent` INT NOT NULL DEFAULT 0 COMMENT '阅读进度百分比',
    `scroll_y` INT NOT NULL DEFAULT 0 COMMENT '最近阅读滚动位置',
    `anchor_id` VARCHAR(160) DEFAULT NULL COMMENT '最近阅读标题锚点',
    `anchor_offset` INT DEFAULT NULL COMMENT '相对锚点偏移',
    `article_updated_at_snapshot` DATETIME DEFAULT NULL COMMENT '保存位置时文章更新时间快照',
    `position_updated_at` DATETIME DEFAULT NULL COMMENT '最近保存阅读位置时间',
    `created_by` VARCHAR(50) DEFAULT NULL COMMENT '创建人',
```

在 `ArticleReadingHistory` 增加字段：

```java
    private Integer progressPercent;
    private Integer scrollY;
    private String anchorId;
    private Integer anchorOffset;
    private LocalDateTime articleUpdatedAtSnapshot;
    private LocalDateTime positionUpdatedAt;
```

在 `ReadingHistoryRelationRow` 增加字段：

```java
    private Integer progressPercent;
    private Integer scrollY;
    private String anchorId;
    private Integer anchorOffset;
    private LocalDateTime articleUpdatedAtSnapshot;
    private LocalDateTime positionUpdatedAt;
    private LocalDateTime articleUpdatedAt;
```

- [ ] **Step 5: 扩展 Mapper 接口**

在 `ArticleReadingHistoryMapper` 增加：

```java
    ReadingHistoryRelationRow selectActiveHistory(@Param("userId") Long userId,
                                                  @Param("articleId") Long articleId);

    int updatePosition(@Param("userId") Long userId,
                       @Param("articleId") Long articleId,
                       @Param("progressPercent") Integer progressPercent,
                       @Param("scrollY") Integer scrollY,
                       @Param("anchorId") String anchorId,
                       @Param("anchorOffset") Integer anchorOffset,
                       @Param("articleUpdatedAt") LocalDateTime articleUpdatedAt,
                       @Param("username") String username,
                       @Param("now") LocalDateTime now);
```

- [ ] **Step 6: 更新 Mapper XML**

在 `upsertHistory` 的 insert 字段中加入位置字段，初始值写为默认值；在重复键更新中，当 `deleted = 1` 时重置位置：

```xml
<insert id="upsertHistory">
    INSERT INTO article_reading_history
        (id, user_id, article_id, title_snapshot, first_read_at, last_read_at, read_count,
         progress_percent, scroll_y, anchor_id, anchor_offset, article_updated_at_snapshot,
         position_updated_at, created_by, created_at, updated_by, updated_at, deleted, version)
    VALUES
        (#{id}, #{userId}, #{articleId}, #{titleSnapshot}, #{now}, #{now}, 1,
         0, 0, NULL, NULL, NULL, NULL, #{username}, #{now}, #{username}, #{now}, 0, 0)
    ON DUPLICATE KEY UPDATE
        title_snapshot = IF(deleted = 1 OR VALUES(last_read_at) >= last_read_at,
                            VALUES(title_snapshot), title_snapshot),
        first_read_at = IF(deleted = 1, VALUES(first_read_at), first_read_at),
        last_read_at = IF(deleted = 1, VALUES(last_read_at),
                          GREATEST(last_read_at, VALUES(last_read_at))),
        read_count = IF(deleted = 1, 1, read_count + 1),
        progress_percent = IF(deleted = 1, 0, progress_percent),
        scroll_y = IF(deleted = 1, 0, scroll_y),
        anchor_id = IF(deleted = 1, NULL, anchor_id),
        anchor_offset = IF(deleted = 1, NULL, anchor_offset),
        article_updated_at_snapshot = IF(deleted = 1, NULL, article_updated_at_snapshot),
        position_updated_at = IF(deleted = 1, NULL, position_updated_at),
        updated_by = VALUES(updated_by),
        updated_at = VALUES(updated_at),
        version = version + 1,
        deleted = 0
</insert>
```

在 `selectHistoryPage`、`selectLastAvailable`、新增 `selectActiveHistory` 中返回位置字段：

```xml
h.progress_percent,
h.scroll_y,
h.anchor_id,
h.anchor_offset,
h.article_updated_at_snapshot,
h.position_updated_at,
a.updated_at AS article_updated_at
```

新增保存语句：

```xml
<update id="updatePosition">
    UPDATE article_reading_history h
    INNER JOIN article a ON a.id = h.article_id
        AND a.deleted = 0
        AND a.status = 'published'
        AND (a.visibility = 'public' OR a.visibility IS NULL)
    SET h.progress_percent = #{progressPercent},
        h.scroll_y = #{scrollY},
        h.anchor_id = #{anchorId},
        h.anchor_offset = #{anchorOffset},
        h.article_updated_at_snapshot = #{articleUpdatedAt},
        h.position_updated_at = #{now},
        h.updated_by = #{username},
        h.updated_at = #{now},
        h.version = h.version + 1
    WHERE h.user_id = #{userId}
      AND h.article_id = #{articleId}
      AND h.deleted = 0
</update>
```

- [ ] **Step 7: 运行数据库契约测试**

Run:

```powershell
cd D:\03PerProject\blog-backend
mvn.cmd -q -Dtest=DatabaseConfigTest test
```

Expected: PASS。

- [ ] **Step 8: 提交数据层契约**

Run:

```powershell
cd D:\03PerProject
git status --short
git add -- blog-backend/src/main/resources/db/migration/2026-07-21-新增阅读位置与进度字段.sql blog-backend/src/main/resources/db/init.sql blog-backend/src/main/resources/mapper/ArticleReadingHistoryMapper.xml blog-backend/src/main/java/com/blog/entity/ArticleReadingHistory.java blog-backend/src/main/java/com/blog/dto/ReadingHistoryRelationRow.java blog-backend/src/main/java/com/blog/mapper/ArticleReadingHistoryMapper.java blog-backend/src/test/java/com/blog/config/DatabaseConfigTest.java
git commit -m "feat: 扩展阅读历史位置数据" -m "新增阅读进度、滚动位置、锚点和文章更新时间快照字段，锁定增量脚本与 Mapper 契约。"
```

---

### Task 2: 实现后端阅读位置保存与详情返回

**Files:**
- Create: `blog-backend/src/main/java/com/blog/dto/ReadingPositionRequest.java`
- Create: `blog-backend/src/main/java/com/blog/dto/ReadingPositionState.java`
- Modify: `blog-backend/src/main/java/com/blog/dto/ReadingHistoryItem.java`
- Modify: `blog-backend/src/main/java/com/blog/entity/Article.java`
- Modify: `blog-backend/src/main/java/com/blog/service/ReadingHistoryService.java`
- Modify: `blog-backend/src/main/java/com/blog/service/impl/ReadingHistoryServiceImpl.java`
- Modify: `blog-backend/src/main/java/com/blog/controller/ReadingController.java`
- Modify: `blog-backend/src/main/java/com/blog/controller/ArticleController.java`
- Modify: `blog-backend/src/test/java/com/blog/service/ReadingHistoryServiceImplTest.java`
- Modify: `blog-backend/src/test/java/com/blog/controller/ReadingControllerTest.java`
- Modify: `blog-backend/src/test/java/com/blog/controller/ArticleControllerTest.java`

- [ ] **Step 1: 写服务层失败测试**

在 `ReadingHistoryServiceImplTest` 增加以下测试：

```java
@Test
void savePosition_shouldValidateAndUpdateOnlyCurrentUserHistory() {
    when(userService.getCurrentUser("alice")).thenReturn(user(10L, "alice"));
    ReadingPositionRequest request = positionRequest(42, 3810, "section-2", 120,
            LocalDateTime.of(2026, 7, 21, 16, 40));
    when(historyMapper.updatePosition(eq(10L), eq(20L), eq(42), eq(3810), eq("section-2"),
            eq(120), eq(request.getArticleUpdatedAt()), eq("alice"), any(LocalDateTime.class)))
            .thenReturn(1);

    readingHistoryService.savePosition(20L, request, "alice");

    verify(userService).getCurrentUser("alice");
    verify(historyMapper).updatePosition(eq(10L), eq(20L), eq(42), eq(3810), eq("section-2"),
            eq(120), eq(request.getArticleUpdatedAt()), eq("alice"), any(LocalDateTime.class));
    verifyNoInteractions(articleService);
}

@Test
void savePosition_shouldRejectInvalidValuesBeforeWriting() {
    assertInvalidPosition(positionRequest(-1, 10, "section", 0, LocalDateTime.now()));
    assertInvalidPosition(positionRequest(101, 10, "section", 0, LocalDateTime.now()));
    assertInvalidPosition(positionRequest(10, -1, "section", 0, LocalDateTime.now()));
    assertInvalidPosition(positionRequest(10, 0, repeated("a", 161), 0, LocalDateTime.now()));
    assertInvalidPosition(positionRequest(10, 0, "section", -1, LocalDateTime.now()));
    assertInvalidPosition(positionRequest(10, 0, "section", 0, null));
}

@Test
void savePosition_shouldRejectMissingOrUnavailableHistory() {
    when(userService.getCurrentUser("alice")).thenReturn(user(10L, "alice"));
    ReadingPositionRequest request = positionRequest(42, 3810, "section-2", 120,
            LocalDateTime.of(2026, 7, 21, 16, 40));
    when(historyMapper.updatePosition(eq(10L), eq(20L), eq(42), eq(3810), eq("section-2"),
            eq(120), eq(request.getArticleUpdatedAt()), eq("alice"), any(LocalDateTime.class)))
            .thenReturn(0);

    assertThatThrownBy(() -> readingHistoryService.savePosition(20L, request, "alice"))
            .isInstanceOf(BusinessException.class)
            .hasMessage("阅读历史不存在或文章暂不可用");
}

@Test
void getPositionState_shouldAllowResumeOnlyWhenProgressAndArticleSnapshotMatch() {
    when(userService.getCurrentUser("alice")).thenReturn(user(10L, "alice"));
    ReadingHistoryRelationRow row = relation(1L, 20L, "Saved title",
            LocalDateTime.of(2026, 7, 20, 8, 0),
            LocalDateTime.of(2026, 7, 21, 9, 0), 3, true);
    row.setProgressPercent(42);
    row.setScrollY(3810);
    row.setAnchorId("section-2");
    row.setAnchorOffset(120);
    row.setArticleUpdatedAtSnapshot(LocalDateTime.of(2026, 7, 21, 16, 40));
    row.setArticleUpdatedAt(LocalDateTime.of(2026, 7, 21, 16, 40));
    row.setPositionUpdatedAt(LocalDateTime.of(2026, 7, 21, 17, 0));
    when(historyMapper.selectActiveHistory(10L, 20L)).thenReturn(row);
    Article article = article(20L, "Current title", "Summary", "cover.png", "Author", 42);

    ReadingPositionState state = readingHistoryService.getPositionState(article, "alice");

    assertThat(state.getProgressPercent()).isEqualTo(42);
    assertThat(state.isCanResume()).isTrue();
    assertThat(state.getResumeScrollY()).isEqualTo(3810);
    assertThat(state.getResumeAnchorId()).isEqualTo("section-2");
    assertThat(state.getResumeAnchorOffset()).isEqualTo(120);
    assertThat(state.getPositionUpdatedAt()).isEqualTo(LocalDateTime.of(2026, 7, 21, 17, 0));
}

@Test
void getPositionState_shouldExposeProgressButNotResumeBelowThreshold() {
    when(userService.getCurrentUser("alice")).thenReturn(user(10L, "alice"));
    ReadingHistoryRelationRow row = relation(1L, 20L, "Saved title",
            LocalDateTime.of(2026, 7, 20, 8, 0),
            LocalDateTime.of(2026, 7, 21, 9, 0), 3, true);
    row.setProgressPercent(4);
    row.setScrollY(120);
    row.setArticleUpdatedAtSnapshot(LocalDateTime.of(2026, 7, 21, 16, 40));
    row.setArticleUpdatedAt(LocalDateTime.of(2026, 7, 21, 16, 40));
    when(historyMapper.selectActiveHistory(10L, 20L)).thenReturn(row);

    ReadingPositionState state = readingHistoryService.getPositionState(article(20L, "Current title",
            "Summary", "cover.png", "Author", 42), "alice");

    assertThat(state.getProgressPercent()).isEqualTo(4);
    assertThat(state.isCanResume()).isFalse();
    assertThat(state.getResumeScrollY()).isNull();
}

@Test
void getHistory_shouldExposeProgressButNotResumeWhenSnapshotDoesNotMatch() {
    when(userService.getCurrentUser("alice")).thenReturn(user(10L, "alice"));
    ReadingHistoryRelationRow row = relation(1L, 20L, "Saved title",
            LocalDateTime.of(2026, 7, 20, 8, 0),
            LocalDateTime.of(2026, 7, 21, 9, 0), 3, true);
    row.setProgressPercent(42);
    row.setScrollY(3810);
    row.setArticleUpdatedAtSnapshot(LocalDateTime.of(2026, 7, 20, 16, 40));
    row.setArticleUpdatedAt(LocalDateTime.of(2026, 7, 21, 16, 40));
    Page<ReadingHistoryRelationRow> rows = new Page<>(1, 10);
    rows.setTotal(1);
    rows.setRecords(Collections.singletonList(row));
    when(historyMapper.selectHistoryPage(any(Page.class), eq(10L))).thenReturn(rows);
    when(articleService.getPublicArticleSummaries(Collections.singletonList(20L)))
            .thenReturn(Collections.singletonList(article(20L, "Current title", "Summary", "cover.png",
                    "Author", 42)));

    IPage<ReadingHistoryItem> result = readingHistoryService.getHistory(query(1, 10), "alice");

    ReadingHistoryItem item = result.getRecords().get(0);
    assertThat(item.getProgressPercent()).isEqualTo(42);
    assertThat(item.isCanResume()).isFalse();
    assertThat(item.getResumeScrollY()).isNull();
}

@Test
void readingHistoryItem_shouldExposeOnlyTheHistoryPresentationFields() {
    List<String> fieldNames = Arrays.asList(ReadingHistoryItem.class.getDeclaredFields()).stream()
            .map(field -> field.getName())
            .collect(java.util.stream.Collectors.toList());

    assertThat(fieldNames).containsExactlyInAnyOrder(
            "articleId", "title", "summary", "coverImage", "authorName", "viewCount",
            "firstReadAt", "lastReadAt", "readCount", "available", "unavailableMessage",
            "progressPercent", "positionUpdatedAt", "canResume", "resumeScrollY",
            "resumeAnchorId", "resumeAnchorOffset");
    assertThat(ReadingHistoryItem.class.getDeclaredFields())
            .filteredOn(field -> field.getName().equals("articleId"))
            .singleElement()
            .extracting(field -> field.getType())
            .isEqualTo(Long.class);
    assertThat(ReadingHistoryItem.class.getDeclaredFields())
            .filteredOn(field -> field.getName().equals("firstReadAt"))
            .singleElement()
            .extracting(field -> field.getType())
            .isEqualTo(LocalDateTime.class);
    assertThat(ReadingHistoryItem.class.getDeclaredFields())
            .filteredOn(field -> field.getName().equals("readCount"))
            .singleElement()
            .extracting(field -> field.getType())
            .isEqualTo(Integer.class);
    assertThat(ReadingHistoryItem.class.getDeclaredFields())
            .filteredOn(field -> field.getName().equals("progressPercent"))
            .singleElement()
            .extracting(field -> field.getType())
            .isEqualTo(Integer.class);
}
```

补充 imports：

```java
import com.blog.dto.ReadingPositionRequest;
import com.blog.dto.ReadingPositionState;
```

在测试类底部增加 helper：

```java
private static ReadingPositionRequest positionRequest(Integer progressPercent, Integer scrollY,
                                                      String anchorId, Integer anchorOffset,
                                                      LocalDateTime articleUpdatedAt) {
    ReadingPositionRequest request = new ReadingPositionRequest();
    request.setProgressPercent(progressPercent);
    request.setScrollY(scrollY);
    request.setAnchorId(anchorId);
    request.setAnchorOffset(anchorOffset);
    request.setArticleUpdatedAt(articleUpdatedAt);
    return request;
}

private void assertInvalidPosition(ReadingPositionRequest request) {
    assertThatThrownBy(() -> readingHistoryService.savePosition(20L, request, "alice"))
            .isInstanceOf(BusinessException.class)
            .hasMessage("阅读位置参数不合法");
    verifyNoInteractions(userService, historyMapper, articleService);
}

private static String repeated(String value, int count) {
    return String.join("", java.util.Collections.nCopies(count, value));
}
```

- [ ] **Step 2: 写 Controller 失败测试**

在 `ReadingControllerTest` 增加：

```java
@Test
void savePosition_shouldUseCurrentPrincipalAndReturnEmptySuccess() {
    ReadingPositionRequest request = new ReadingPositionRequest();
    request.setProgressPercent(42);

    Result<Void> result = controller.savePosition(42L, request, principal);

    assertThat(result.getCode()).isEqualTo(200);
    assertThat(result.getData()).isNull();
    verify(readingHistoryService).savePosition(42L, request, "alice");
    verifyNoMoreInteractions(readingSpaceService, readingHistoryService);
}
```

补充 imports：

```java
import com.blog.dto.ReadingPositionRequest;
import org.springframework.web.bind.annotation.PutMapping;
```

更新映射测试：

```java
assertMapping("savePosition", new Class[]{Long.class, ReadingPositionRequest.class, Principal.class},
        PutMapping.class, "/history/{articleId}/position");
```

在 `assertMapping` 中同时支持 `PutMapping`：

```java
assertThat(method.getAnnotationsByType(PutMapping.class)).hasSize(mappingType == PutMapping.class ? 1 : 0);
if (mappingType == PutMapping.class) {
    assertThat(method.getAnnotation(PutMapping.class).value()).containsExactly(path);
}
```

在 `ArticleControllerTest` 增加：

```java
@Test
void detailAttachesReadingPositionForAuthenticatedReader() {
    Article article = article(1L);
    ReadingPositionState state = new ReadingPositionState();
    state.setProgressPercent(42);
    Principal principal = () -> "alice";
    when(articleService.getPublicDetail(1L)).thenReturn(article);
    when(readingHistoryService.getPositionState(article, "alice")).thenReturn(state);

    Result<Article> result = articleController.detail(1L, principal);

    assertEquals(200, result.getCode());
    assertEquals(state, result.getData().getReadingPosition());
    verify(readingHistoryService).record(article, "alice");
    verify(readingHistoryService).getPositionState(article, "alice");
}
```

补充 import：

```java
import com.blog.dto.ReadingPositionState;
```

- [ ] **Step 3: 运行后端服务和控制器测试确认失败**

Run:

```powershell
cd D:\03PerProject\blog-backend
mvn.cmd -q -Dtest=ReadingHistoryServiceImplTest,ReadingControllerTest,ArticleControllerTest test
```

Expected: FAIL，提示 `ReadingPositionRequest`、`ReadingPositionState` 或新接口不存在。

- [ ] **Step 4: 创建 DTO 并扩展输出对象**

创建 `ReadingPositionRequest.java`：

```java
package com.blog.dto;

import lombok.Data;

import java.time.LocalDateTime;

@Data
public class ReadingPositionRequest {
    private Integer progressPercent;
    private Integer scrollY;
    private String anchorId;
    private Integer anchorOffset;
    private LocalDateTime articleUpdatedAt;
}
```

创建 `ReadingPositionState.java`：

```java
package com.blog.dto;

import lombok.Data;

import java.time.LocalDateTime;

@Data
public class ReadingPositionState {
    private Integer progressPercent;
    private LocalDateTime positionUpdatedAt;
    private boolean canResume;
    private Integer resumeScrollY;
    private String resumeAnchorId;
    private Integer resumeAnchorOffset;
}
```

在 `ReadingHistoryItem` 增加：

```java
    private Integer progressPercent;
    private LocalDateTime positionUpdatedAt;
    private boolean canResume;
    private Integer resumeScrollY;
    private String resumeAnchorId;
    private Integer resumeAnchorOffset;
```

在 `Article` 增加不映射字段：

```java
    /** 当前登录读者的阅读位置状态，不直接映射数据库列。 */
    @TableField(exist = false)
    private ReadingPositionState readingPosition;
```

并补充 import：

```java
import com.blog.dto.ReadingPositionState;
```

- [ ] **Step 5: 扩展服务接口和实现**

在 `ReadingHistoryService` 增加：

```java
    void savePosition(Long articleId, ReadingPositionRequest request, String username);

    ReadingPositionState getPositionState(Article article, String username);
```

在 `ReadingHistoryServiceImpl` 增加常量：

```java
    private static final int MIN_RESUME_PROGRESS = 5;
    private static final int MAX_PROGRESS = 100;
    private static final int MAX_ANCHOR_ID_LENGTH = 160;
```

增加方法：

```java
    @Override
    @Transactional
    public void savePosition(Long articleId, ReadingPositionRequest request, String username) {
        validatePositionRequest(request);
        User user = requireUser(username);
        LocalDateTime now = LocalDateTime.now();
        int updated = historyMapper.updatePosition(user.getId(), articleId,
                request.getProgressPercent(), request.getScrollY(),
                normalizeAnchorId(request.getAnchorId()), request.getAnchorOffset(),
                request.getArticleUpdatedAt(), username, now);
        if (updated == 0) {
            throw new BusinessException("阅读历史不存在或文章暂不可用");
        }
    }

    @Override
    @Transactional(readOnly = true)
    public ReadingPositionState getPositionState(Article article, String username) {
        User user = requireUser(username);
        ReadingHistoryRelationRow row = historyMapper.selectActiveHistory(user.getId(), article.getId());
        return row == null ? emptyPositionState() : toPositionState(row);
    }
```

增加 helper：

```java
    private void validatePositionRequest(ReadingPositionRequest request) {
        if (request == null || request.getProgressPercent() == null || request.getScrollY() == null
                || request.getArticleUpdatedAt() == null
                || request.getProgressPercent() < 0 || request.getProgressPercent() > MAX_PROGRESS
                || request.getScrollY() < 0
                || request.getAnchorOffset() != null && request.getAnchorOffset() < 0) {
            throw new BusinessException("阅读位置参数不合法");
        }
        String anchorId = request.getAnchorId();
        if (anchorId != null && anchorId.trim().length() > MAX_ANCHOR_ID_LENGTH) {
            throw new BusinessException("阅读位置参数不合法");
        }
    }

    private String normalizeAnchorId(String anchorId) {
        if (anchorId == null) {
            return null;
        }
        String trimmed = anchorId.trim();
        return trimmed.isEmpty() ? null : trimmed;
    }

    private ReadingPositionState emptyPositionState() {
        ReadingPositionState state = new ReadingPositionState();
        state.setProgressPercent(0);
        state.setCanResume(false);
        return state;
    }

    private ReadingPositionState toPositionState(ReadingHistoryRelationRow row) {
        ReadingPositionState state = new ReadingPositionState();
        int progress = normalizeProgress(row.getProgressPercent());
        state.setProgressPercent(progress);
        state.setPositionUpdatedAt(row.getPositionUpdatedAt());
        boolean canResume = row.isAvailable()
                && progress >= MIN_RESUME_PROGRESS
                && row.getArticleUpdatedAtSnapshot() != null
                && row.getArticleUpdatedAtSnapshot().equals(row.getArticleUpdatedAt());
        state.setCanResume(canResume);
        if (canResume) {
            state.setResumeScrollY(row.getScrollY());
            state.setResumeAnchorId(row.getAnchorId());
            state.setResumeAnchorOffset(row.getAnchorOffset());
        }
        return state;
    }

    private int normalizeProgress(Integer progressPercent) {
        if (progressPercent == null) {
            return 0;
        }
        return Math.max(0, Math.min(MAX_PROGRESS, progressPercent));
    }
```

在 `toReadingHistoryItem` 的开头设置位置字段：

```java
        ReadingPositionState position = toPositionState(row);
        item.setProgressPercent(position.getProgressPercent());
        item.setPositionUpdatedAt(position.getPositionUpdatedAt());
        item.setCanResume(position.isCanResume());
        item.setResumeScrollY(position.getResumeScrollY());
        item.setResumeAnchorId(position.getResumeAnchorId());
        item.setResumeAnchorOffset(position.getResumeAnchorOffset());
```

- [ ] **Step 6: 扩展 Controller**

在 `ReadingController` 增加：

```java
    @PutMapping("/history/{articleId}/position")
    public Result<Void> savePosition(@PathVariable Long articleId,
                                     @RequestBody ReadingPositionRequest request,
                                     Principal principal) {
        readingHistoryService.savePosition(articleId, request, principal.getName());
        return Result.success();
    }
```

补充 imports：

```java
import com.blog.dto.ReadingPositionRequest;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
```

在 `ArticleController.detail` 登录分支中，记录历史后附带位置状态；两段逻辑分别捕获异常，避免位置读取失败被误记为历史记录失败：

```java
        if (principal != null) {
            try {
                readingHistoryService.record(article, principal.getName());
            } catch (RuntimeException exception) {
                log.error("Failed to record reading history for article {} and user {}",
                        article.getId(), principal.getName(), exception);
            }
            try {
                article.setReadingPosition(readingHistoryService.getPositionState(article, principal.getName()));
            } catch (RuntimeException exception) {
                log.error("Failed to attach reading position for article {} and user {}",
                        article.getId(), principal.getName(), exception);
            }
        }
```

- [ ] **Step 7: 运行后端目标测试**

Run:

```powershell
cd D:\03PerProject\blog-backend
mvn.cmd -q -Dtest=ReadingHistoryServiceImplTest,ReadingControllerTest,ArticleControllerTest test
```

Expected: PASS。

- [ ] **Step 8: 提交后端业务接口**

Run:

```powershell
cd D:\03PerProject
git status --short
git add -- blog-backend/src/main/java/com/blog/dto/ReadingPositionRequest.java blog-backend/src/main/java/com/blog/dto/ReadingPositionState.java blog-backend/src/main/java/com/blog/dto/ReadingHistoryItem.java blog-backend/src/main/java/com/blog/entity/Article.java blog-backend/src/main/java/com/blog/service/ReadingHistoryService.java blog-backend/src/main/java/com/blog/service/impl/ReadingHistoryServiceImpl.java blog-backend/src/main/java/com/blog/controller/ReadingController.java blog-backend/src/main/java/com/blog/controller/ArticleController.java blog-backend/src/test/java/com/blog/service/ReadingHistoryServiceImplTest.java blog-backend/src/test/java/com/blog/controller/ReadingControllerTest.java blog-backend/src/test/java/com/blog/controller/ArticleControllerTest.java
git commit -m "feat: 支持保存阅读位置状态" -m "新增需登录的阅读位置保存接口，并在文章详情响应中附带可恢复状态。"
```

---

### Task 3: 建立前端阅读位置工具和 API

**Files:**
- Create: `blog-frontend/src/utils/readingPosition.js`
- Create: `blog-frontend/src/utils/readingPosition.test.mjs`
- Modify: `blog-frontend/src/api/reading.js`
- Modify: `blog-frontend/src/utils/readingHistory.js`
- Modify: `blog-frontend/src/utils/readingHistory.test.mjs`

- [ ] **Step 1: 写前端工具和 API 失败测试**

创建 `readingPosition.test.mjs`：

```javascript
import test from 'node:test'
import assert from 'node:assert/strict'
import {
  buildReadingPositionPayload,
  calculateReadingProgress,
  shouldSaveReadingPosition,
  shouldShowResumePrompt
} from './readingPosition.js'

test('calculateReadingProgress clamps progress between 0 and 100', () => {
  assert.equal(calculateReadingProgress({ scrollY: 0, articleTop: 100, articleHeight: 1000, viewportHeight: 500 }), 0)
  assert.equal(calculateReadingProgress({ scrollY: 350, articleTop: 100, articleHeight: 1000, viewportHeight: 500 }), 50)
  assert.equal(calculateReadingProgress({ scrollY: 700, articleTop: 100, articleHeight: 1000, viewportHeight: 500 }), 100)
})

test('buildReadingPositionPayload normalizes safe payload fields', () => {
  assert.deepEqual(buildReadingPositionPayload({
    progressPercent: 42.8,
    scrollY: 3810.9,
    anchorId: ' section-2 ',
    anchorOffset: 120.2,
    articleUpdatedAt: '2026-07-21T16:40:00'
  }), {
    progressPercent: 43,
    scrollY: 3811,
    anchorId: 'section-2',
    anchorOffset: 120,
    articleUpdatedAt: '2026-07-21T16:40:00'
  })
})

test('resume prompt requires backend permission and at least five percent progress', () => {
  assert.equal(shouldShowResumePrompt({ canResume: true, progressPercent: 5 }), true)
  assert.equal(shouldShowResumePrompt({ canResume: true, progressPercent: 4 }), false)
  assert.equal(shouldShowResumePrompt({ canResume: false, progressPercent: 80 }), false)
})

test('save gate throttles non-forced saves and always allows forced saves', () => {
  assert.equal(shouldSaveReadingPosition({ now: 10000, lastSavedAt: 2000, intervalMs: 9000 }), false)
  assert.equal(shouldSaveReadingPosition({ now: 12000, lastSavedAt: 2000, intervalMs: 9000 }), true)
  assert.equal(shouldSaveReadingPosition({ now: 10000, lastSavedAt: 9999, intervalMs: 9000, force: true }), true)
})
```

在 `readingHistory.test.mjs` 的 API 测试中加入 `saveReadingPosition`：

```javascript
import {
  clearReadingHistory,
  deleteReadingHistory,
  getReadingHistory,
  getReadingOverview,
  saveReadingPosition
} from '../api/reading.js'
```

并在 captured requests 中加入：

```javascript
await saveReadingPosition('758902345678901401', {
  progressPercent: 42,
  scrollY: 3810,
  articleUpdatedAt: '2026-07-21T16:40:00'
})
```

期望数组中加入：

```javascript
{ method: 'put', url: '/my/reading/history/758902345678901401/position', params: undefined }
```

增加进度格式化测试：

```javascript
import { formatReadingProgress } from './readingHistory.js'

test('format reading progress returns compact Chinese progress text', () => {
  assert.equal(formatReadingProgress(42), '读到 42%')
  assert.equal(formatReadingProgress('42.8'), '读到 43%')
  assert.equal(formatReadingProgress(-1), '')
  assert.equal(formatReadingProgress(101), '')
  assert.equal(formatReadingProgress(null), '')
})
```

- [ ] **Step 2: 运行前端目标测试确认失败**

Run:

```powershell
cd D:\03PerProject\blog-frontend
node --test src/utils/readingPosition.test.mjs src/utils/readingHistory.test.mjs
```

Expected: FAIL，提示 `readingPosition.js` 或 `saveReadingPosition` 不存在。

- [ ] **Step 3: 实现阅读位置工具**

创建 `readingPosition.js`：

```javascript
export function calculateReadingProgress({
  scrollY = 0,
  articleTop = 0,
  articleHeight = 0,
  viewportHeight = 0
} = {}) {
  const top = Number(articleTop)
  const height = Number(articleHeight)
  const viewport = Number(viewportHeight)
  const scroll = Number(scrollY)

  if (![top, height, viewport, scroll].every(Number.isFinite) || height <= 0) return 0

  const travel = Math.max(1, height - Math.max(0, viewport))
  return clampProgress(((scroll - top) / travel) * 100)
}

export function clampProgress(value) {
  const number = Number(value)
  if (!Number.isFinite(number)) return 0
  return Math.max(0, Math.min(100, Math.round(number)))
}

export function buildReadingPositionPayload({
  progressPercent,
  scrollY,
  anchorId = '',
  anchorOffset = null,
  articleUpdatedAt
} = {}) {
  if (!articleUpdatedAt) return null
  const progress = clampProgress(progressPercent)
  const scroll = Math.max(0, Math.round(Number(scrollY) || 0))
  const normalizedAnchorId = typeof anchorId === 'string' ? anchorId.trim().slice(0, 160) : ''
  const offsetNumber = Number(anchorOffset)

  return {
    progressPercent: progress,
    scrollY: scroll,
    anchorId: normalizedAnchorId || null,
    anchorOffset: Number.isFinite(offsetNumber) && offsetNumber >= 0 ? Math.round(offsetNumber) : null,
    articleUpdatedAt
  }
}

export function shouldShowResumePrompt(position = {}) {
  return !!position?.canResume && Number(position.progressPercent) >= 5
}

export function shouldSaveReadingPosition({
  now = Date.now(),
  lastSavedAt = 0,
  intervalMs = 9000,
  force = false
} = {}) {
  if (force) return true
  const current = Number(now)
  const previous = Number(lastSavedAt)
  const interval = Number(intervalMs)
  return [current, previous, interval].every(Number.isFinite) && current - previous >= interval
}
```

- [ ] **Step 4: 实现 API 与进度格式化**

在 `api/reading.js` 增加：

```javascript
export const saveReadingPosition = (articleId, data, config = {}) =>
  request.put(`/my/reading/history/${articleId}/position`, data, config)
```

在 `utils/readingHistory.js` 增加：

```javascript
export function formatReadingProgress(value) {
  if (value === null || value === undefined || value === '') return ''
  const number = Number(value)
  if (!Number.isFinite(number) || number < 0 || number > 100) return ''
  return `读到 ${Math.round(number)}%`
}
```

- [ ] **Step 5: 运行前端工具测试**

Run:

```powershell
cd D:\03PerProject\blog-frontend
node --test src/utils/readingPosition.test.mjs src/utils/readingHistory.test.mjs
```

Expected: PASS。

- [ ] **Step 6: 提交前端工具和 API**

Run:

```powershell
cd D:\03PerProject
git status --short
git add -- blog-frontend/src/utils/readingPosition.js blog-frontend/src/utils/readingPosition.test.mjs blog-frontend/src/api/reading.js blog-frontend/src/utils/readingHistory.js blog-frontend/src/utils/readingHistory.test.mjs
git commit -m "feat: 增加阅读位置前端工具" -m "新增阅读进度计算、保存节流、恢复提示判断和阅读位置保存 API。"
```

---

### Task 4: 接入文章详情恢复提示和自动保存

**Files:**
- Modify: `blog-frontend/src/views/ArticleDetail.vue`
- Modify: `blog-frontend/src/utils/readingPosition.test.mjs`

- [ ] **Step 1: 扩展文章详情源码契约测试**

在 `readingPosition.test.mjs` 增加：

```javascript
import { readFileSync } from 'node:fs'

test('article detail source wires resume prompt and silent position saving', () => {
  const source = readFileSync(new URL('../views/ArticleDetail.vue', import.meta.url), 'utf8')

  assert.match(source, /saveReadingPosition/)
  assert.match(source, /resume-reading-panel/)
  assert.match(source, /上次读到/)
  assert.match(source, /continueReadingFromSavedPosition/)
  assert.match(source, /resumePromptDismissed/)
  assert.match(source, /skipErrorMessage:\s*true/)
  assert.match(source, /document\.addEventListener\('visibilitychange'/)
  assert.match(source, /onBeforeRouteLeave/)
})

test('article detail uses current article updated time and active heading when saving position', () => {
  const source = readFileSync(new URL('../views/ArticleDetail.vue', import.meta.url), 'utf8')

  assert.match(source, /article\.value\.updatedAt/)
  assert.match(source, /activeHeadingId\.value/)
  assert.match(source, /buildReadingPositionPayload/)
  assert.match(source, /calculateReadingProgress/)
  assert.match(source, /shouldSaveReadingPosition/)
})

test('article detail does not auto jump and lets the reader dismiss the resume prompt', () => {
  const source = readFileSync(new URL('../views/ArticleDetail.vue', import.meta.url), 'utf8')

  assert.doesNotMatch(source, /watch\([\s\S]*?continueReadingFromSavedPosition/)
  assert.match(source, /@click="continueReadingFromSavedPosition"/)
  assert.match(source, /@click="dismissResumePrompt"/)
})
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
cd D:\03PerProject\blog-frontend
node --test src/utils/readingPosition.test.mjs
```

Expected: FAIL，文章详情还没有恢复提示和保存逻辑。

- [ ] **Step 3: 更新 ArticleDetail imports 和状态**

修改 imports：

```javascript
import { computed, nextTick, onBeforeUnmount, onMounted, ref, watch } from 'vue'
import { onBeforeRouteLeave, useRoute, useRouter } from 'vue-router'
import { getArticle, getArticleNeighbors, getRelatedArticles } from '../api/article'
import { saveReadingPosition } from '../api/reading'
import {
  buildReadingPositionPayload,
  calculateReadingProgress,
  shouldSaveReadingPosition,
  shouldShowResumePrompt
} from '../utils/readingPosition'
```

新增状态：

```javascript
const readingPosition = ref(null)
const resumePromptDismissed = ref(false)
let lastPositionSavedAt = 0
```

新增 computed：

```javascript
const showResumePrompt = computed(() =>
  authStore.isLoggedIn &&
  !resumePromptDismissed.value &&
  shouldShowResumePrompt(readingPosition.value)
)
```

- [ ] **Step 4: 加入恢复提示模板**

放在 `reading-summary` 后、`reading-layout` 前：

```vue
<section
  v-if="showResumePrompt"
  class="resume-reading-panel"
  role="status"
  aria-live="polite"
>
  <span>上次读到 {{ readingPosition.progressPercent }}%</span>
  <div class="resume-actions">
    <el-button type="primary" size="small" @click="continueReadingFromSavedPosition">
      继续阅读
    </el-button>
    <el-button text size="small" @click="dismissResumePrompt">
      关闭
    </el-button>
  </div>
</section>
```

- [ ] **Step 5: 加入加载、保存和恢复逻辑**

在 `loadArticle` 成功设置文章后加入：

```javascript
readingPosition.value = r.data?.readingPosition || null
resumePromptDismissed.value = false
lastPositionSavedAt = 0
```

在 `onMounted` 中增加：

```javascript
document.addEventListener('visibilitychange', handleVisibilityChange)
```

在 `onBeforeUnmount` 中先补保存，再移除监听：

```javascript
flushReadingPosition()
document.removeEventListener('visibilitychange', handleVisibilityChange)
```

在 setup 顶层增加路由离开补保存：

```javascript
onBeforeRouteLeave(() => {
  flushReadingPosition()
})
```

新增函数：

```javascript
function getArticleBodyMetrics() {
  const body = document.querySelector('.article-body')
  if (!body) return null
  const rect = body.getBoundingClientRect()
  return {
    articleTop: rect.top + window.scrollY,
    articleHeight: body.scrollHeight,
    viewportHeight: window.innerHeight
  }
}

function getAnchorOffset(anchorId) {
  if (!anchorId) return null
  const element = document.getElementById(anchorId)
  if (!element) return null
  return Math.max(0, Math.round(window.scrollY - (element.getBoundingClientRect().top + window.scrollY)))
}

function buildCurrentReadingPositionPayload() {
  if (!article.value?.id || !article.value?.updatedAt) return null
  const metrics = getArticleBodyMetrics()
  if (!metrics) return null
  const progressPercent = calculateReadingProgress({
    scrollY: window.scrollY,
    articleTop: metrics.articleTop,
    articleHeight: metrics.articleHeight,
    viewportHeight: metrics.viewportHeight
  })
  return buildReadingPositionPayload({
    progressPercent,
    scrollY: window.scrollY,
    anchorId: activeHeadingId.value,
    anchorOffset: getAnchorOffset(activeHeadingId.value),
    articleUpdatedAt: article.value.updatedAt
  })
}

function scheduleReadingPositionSave(force = false) {
  if (!authStore.isLoggedIn || !componentActive || !article.value?.id) return
  const now = Date.now()
  if (!shouldSaveReadingPosition({ now, lastSavedAt: lastPositionSavedAt, intervalMs: 9000, force })) {
    return
  }
  const payload = buildCurrentReadingPositionPayload()
  if (!payload) return
  lastPositionSavedAt = now
  void saveReadingPosition(article.value.id, payload, {
    skipErrorMessage: true,
    skipAuthRedirect: true
  }).catch(() => {})
}

function flushReadingPosition() {
  scheduleReadingPositionSave(true)
}

function handleVisibilityChange() {
  if (document.visibilityState === 'hidden') {
    flushReadingPosition()
  }
}

function continueReadingFromSavedPosition() {
  const scrollY = Number(readingPosition.value?.resumeScrollY)
  if (!Number.isFinite(scrollY)) return
  resumePromptDismissed.value = true
  window.scrollTo({ top: Math.max(0, scrollY), behavior: 'smooth' })
}

function dismissResumePrompt() {
  resumePromptDismissed.value = true
}
```

在 `updateScrollState` 末尾加入：

```javascript
scheduleReadingPositionSave(false)
```

- [ ] **Step 6: 增加恢复提示样式**

在 `ArticleDetail.vue` 样式中加入：

```css
.resume-reading-panel {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 12px 14px;
  border: 1px solid color-mix(in srgb, var(--primary-color) 28%, var(--soft-border-color));
  border-radius: var(--radius-md);
  background: color-mix(in srgb, var(--primary-color) 7%, var(--panel-bg));
  color: var(--text-color);
  font-size: 14px;
}

.resume-actions {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  flex: 0 0 auto;
}

@media (max-width: 640px) {
  .resume-reading-panel {
    align-items: flex-start;
    flex-direction: column;
  }
}
```

- [ ] **Step 7: 运行文章详情前端测试和构建**

Run:

```powershell
cd D:\03PerProject\blog-frontend
node --test src/utils/readingPosition.test.mjs
npm.cmd run build
```

Expected: 测试 PASS，Vite build 成功。

- [ ] **Step 8: 提交文章详情接入**

Run:

```powershell
cd D:\03PerProject
git status --short
git add -- blog-frontend/src/views/ArticleDetail.vue blog-frontend/src/utils/readingPosition.test.mjs
git commit -m "feat: 接入文章阅读位置恢复" -m "文章详情页展示可关闭的继续阅读提示，滚动时节流保存位置，并在离开前补保存。"
```

---

### Task 5: 在我的阅读和阅读历史展示进度

**Files:**
- Modify: `blog-frontend/src/views/ReadingSpace.vue`
- Modify: `blog-frontend/src/components/ReadingHistoryItem.vue`
- Modify: `blog-frontend/src/utils/readingHistory.test.mjs`

- [ ] **Step 1: 写列表展示失败测试**

在 `readingHistory.test.mjs` 增加：

```javascript
test('reading space and history item display compact reading progress', () => {
  assert.match(readingSpaceSource, /formatReadingProgress/)
  assert.match(itemSource, /formatReadingProgress/)
  assert.match(readingSpaceSource, /读到/)
  assert.match(itemSource, /读到/)
})
```

更新不可用字段允许列表，加入 `progressPercent`：

```javascript
allowedFields: ['lastReadAt', 'progressPercent', 'title']
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
cd D:\03PerProject\blog-frontend
node --test src/utils/readingHistory.test.mjs
```

Expected: FAIL，页面还没有使用 `formatReadingProgress`。

- [ ] **Step 3: 更新 ReadingSpace 展示**

修改 import：

```javascript
import { formatReadingProgress, formatReadingTime } from '../utils/readingHistory'
```

在“上次阅读”时间旁加入：

```vue
<span v-if="formatReadingProgress(overview.lastRead.progressPercent)" class="reading-progress">
  {{ formatReadingProgress(overview.lastRead.progressPercent) }}
</span>
```

在最近阅读可用和不可用分支的 `preview-meta` 中加入：

```vue
<span v-if="formatReadingProgress(item.progressPercent)">
  {{ formatReadingProgress(item.progressPercent) }}
</span>
```

新增样式：

```css
.reading-progress {
  color: var(--accent-color);
  font-weight: 750;
}
```

- [ ] **Step 4: 更新 ReadingHistoryItem 展示**

修改 import：

```javascript
import { formatReadingProgress, formatReadingTime } from '../utils/readingHistory'
```

在 `.reading-meta` 中加入：

```vue
<span v-if="formatReadingProgress(item.progressPercent)">
  {{ formatReadingProgress(item.progressPercent) }}
</span>
```

- [ ] **Step 5: 运行前端测试和构建**

Run:

```powershell
cd D:\03PerProject\blog-frontend
node --test src/utils/readingHistory.test.mjs
npm.cmd run build
```

Expected: 测试 PASS，Vite build 成功。

- [ ] **Step 6: 提交阅读页进度展示**

Run:

```powershell
cd D:\03PerProject
git status --short
git add -- blog-frontend/src/views/ReadingSpace.vue blog-frontend/src/components/ReadingHistoryItem.vue blog-frontend/src/utils/readingHistory.test.mjs
git commit -m "feat: 展示阅读进度" -m "在我的阅读和阅读历史中展示读到百分比，同时保持不可公开记录的安全输出。"
```

---

### Task 6: 全量验证并同步项目状态

**Files:**
- Modify: `docs/项目状态与目标.md`

- [ ] **Step 1: 运行后端全量测试**

Run:

```powershell
cd D:\03PerProject\blog-backend
mvn.cmd -q test
```

Expected: BUILD SUCCESS，146 个以上测试通过，0 failures，0 errors。测试数量可能因本期新增测试增加。

- [ ] **Step 2: 运行前端全量测试**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test
```

Expected: 所有 Node 测试 PASS，0 failures。

- [ ] **Step 3: 运行前端生产构建**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd run build
```

Expected: Vite build 成功；允许保留项目已有大 chunk 警告。

- [ ] **Step 4: 校验数据库脚本和工作区**

Run:

```powershell
cd D:\03PerProject
Select-String -Encoding utf8 -LiteralPath 'blog-backend\src\main\resources\db\migration\2026-07-21-新增阅读位置与进度字段.sql' -Pattern 'progress_percent|scroll_y|anchor_id|anchor_offset|article_updated_at_snapshot|position_updated_at'
git diff --check
git status --short
```

Expected: 六个新增字段均可见，`git diff --check` 无错误，`git status --short` 只包含本任务文档状态文件。

- [ ] **Step 5: 更新项目状态为代码完成待验收**

在 `docs/项目状态与目标.md` 中将“已排期但未开始”的登录用户阅读位置与阅读进度移动到新增“已实现待验收”段落：

```markdown
## 已实现待验收

- 登录用户阅读位置与阅读进度：代码已实现，包含阅读位置增量字段、需登录的位置保存接口、文章详情点击恢复提示、滚动节流保存，以及“我的阅读”和“阅读历史”的进度展示。增量脚本 `2026-07-21-新增阅读位置与进度字段.sql` 需要由用户手动执行。
```

将“当前阶段目标”改为：

```markdown
登录用户阅读位置与阅读进度已完成代码实现，等待用户手动执行增量脚本并进行真实数据库和页面验收。
```

保留“游客本地阅读历史”“公开首页文章卡片的已读状态”“阅读趋势统计”“热门排行”“评论”“点赞”在延期列表。

- [ ] **Step 6: 提交状态文档**

Run:

```powershell
cd D:\03PerProject
git status --short
git add -- docs/项目状态与目标.md
git commit -m "docs: 更新阅读位置待验收状态" -m "记录阅读位置与进度代码实现范围、自动化验证结果和需手动执行的增量脚本。"
```

- [ ] **Step 7: 交付手动验收清单**

向用户说明只需手动执行数据库脚本：

```text
blog-backend/src/main/resources/db/migration/2026-07-21-新增阅读位置与进度字段.sql
```

手动验收覆盖：

1. 游客阅读公开文章不显示恢复提示，不保存阅读位置。
2. 登录用户阅读长文章并滚动后，刷新或重新进入详情页显示“上次读到 xx%，继续阅读”。
3. 点击“继续阅读”后跳到保存位置；关闭提示后当前页面不再显示。
4. 低于 5% 的进度可以展示，但不显示恢复提示。
5. 文章内容更新后仍可展示进度，但不提示恢复旧位置。
6. “我的阅读”和“阅读历史”显示“读到 xx%”。
7. 不可公开历史仍只显示标题快照、阅读时间、次数、进度和不可用提示，不可点击。
8. 删除历史后再次阅读，不恢复删除前的位置。
9. 桌面端和移动端恢复提示不遮挡正文，无横向溢出。

---

## 最终验证矩阵

| 范围 | 自动验证 | 手动验证 |
| --- | --- | --- |
| 数据库字段 | `DatabaseConfigTest`、脚本字段扫描、`git diff --check` | 用户手动执行增量脚本 |
| 位置保存 | `ReadingHistoryServiceImplTest`、`ReadingControllerTest` | 登录用户滚动后刷新再进入文章 |
| 恢复判断 | 服务测试覆盖进度阈值和文章更新时间快照 | 内容更新后不显示恢复提示 |
| 文章详情 | `readingPosition.test.mjs` 源码契约、Vite build | 点击恢复、关闭提示、移动端布局 |
| 阅读列表 | `readingHistory.test.mjs` 源码契约 | “我的阅读”和“阅读历史”展示进度 |
| 安全边界 | 后端用户限定和不可公开校验测试 | 不可公开历史不可点击且不泄露详情 |
| 回归 | 后端全量测试、前端全量测试、前端构建 | 收藏、阅读历史删除清空、公开阅读路径 |
