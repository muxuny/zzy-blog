# 视觉体验个性化优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不新增后端和数据库变更的前提下，为公开首页、文章详情、我的阅读和创作空间落地现代私人知识空间视觉基调、固定主题包切换和轻量互动反馈。

**Architecture:** 先把主题选择抽成纯前端工具和 Pinia store，再用 CSS variables 承载三套主题包和明暗模式。页面改造沿用现有 Vue 3、Element Plus、路由和 API，只调整组件结构、样式和少量派生展示数据，不改变业务流程。

**Tech Stack:** Vue 3, Pinia, Element Plus, Vite, CSS variables, `localStorage`, `window.matchMedia`, Node `node:test`

---

## File Structure

- Create: `blog-frontend/src/utils/themeAppearance.js`
  - 负责主题包、明暗模式、默认值、旧 `theme` 存储兼容、本地存储读写和系统明暗解析。
- Create: `blog-frontend/src/utils/themeAppearance.test.mjs`
  - 覆盖主题配置归一化、旧存储迁移、本地存储异常降级和跟随系统解析。
- Modify: `blog-frontend/src/stores/theme.js`
  - 从二值 `isDark` 扩展为 `palette + mode + isDark`，继续暴露 `toggle()` 兼容旧调用。
- Modify: `blog-frontend/src/components/ThemeToggle.vue`
  - 保留文件名，改成“外观”弹出面板，选择主题包和明暗模式。
- Create: `blog-frontend/src/utils/visualRefreshSource.test.mjs`
  - 使用源码断言覆盖外观入口、首页内容信号、文章详情进度条、阅读/创作页面互动样式的关键落点。
- Modify: `blog-frontend/src/styles/theme.css`
  - 增加三套主题包的浅色和深色 CSS token，并补充 header、motion、surface、grid、status token。
- Modify: `blog-frontend/src/styles/global.css`
  - 将 Element Plus 全局覆盖中的硬编码蓝色替换为主题变量，补充减少动态效果基础规则。
- Modify: `blog-frontend/src/components/AppHeader.vue`
  - 使用磨砂吸附层、外观入口和移动端安全布局。
- Modify: `blog-frontend/src/views/Home.vue`
  - 将首页重塑为内容索引，加入“本周内容信号”，保留搜索、标签筛选和文章列表逻辑。
- Modify: `blog-frontend/src/components/ArticleCard.vue`
  - 增加文章卡片 hover/focus 聚焦线、主题化视觉标记和减少动态效果规则。
- Modify: `blog-frontend/src/views/ArticleDetail.vue`
  - 增加顶部阅读进度线、目录当前项吸附反馈，主题化继续阅读弹窗和阅读工具条。
- Modify: `blog-frontend/src/views/ReadingSpace.vue`
  - 将我的阅读视觉调整为阅读续接台，优化继续阅读、历史和收藏项反馈。
- Modify: `blog-frontend/src/views/creator/MyArticles.vue`
  - 将创作空间视觉调整为创作控制台，优化分组、筛选、表格行和状态标签。
- Modify: `docs/项目状态与目标.md`
  - 实现完成后将本阶段从“已排期但未开始”调整到“已实现待验收”。

---

### Task 1: Theme Appearance Utilities

**Files:**
- Create: `blog-frontend/src/utils/themeAppearance.js`
- Create: `blog-frontend/src/utils/themeAppearance.test.mjs`

- [ ] **Step 1: Write the failing tests**

Create `blog-frontend/src/utils/themeAppearance.test.mjs`:

```js
import test from 'node:test'
import assert from 'node:assert/strict'
import {
  DEFAULT_APPEARANCE,
  THEME_MODES,
  THEME_PALETTES,
  normalizeAppearance,
  readStoredAppearance,
  resolveThemeName,
  writeStoredAppearance
} from './themeAppearance.js'

function createStorage(initial = {}) {
  const data = new Map(Object.entries(initial))
  return {
    getItem(key) {
      return data.has(key) ? data.get(key) : null
    },
    setItem(key, value) {
      data.set(key, String(value))
    },
    removeItem(key) {
      data.delete(key)
    },
    snapshot() {
      return Object.fromEntries(data.entries())
    }
  }
}

test('theme constants expose three palettes and three modes', () => {
  assert.deepEqual(THEME_PALETTES.map(item => item.value), ['juniper', 'fog', 'copper'])
  assert.deepEqual(THEME_MODES.map(item => item.value), ['light', 'dark', 'system'])
  assert.deepEqual(DEFAULT_APPEARANCE, { palette: 'juniper', mode: 'light' })
})

test('normalizeAppearance falls back to defaults for unknown values', () => {
  assert.deepEqual(normalizeAppearance({ palette: 'unknown', mode: 'future' }), DEFAULT_APPEARANCE)
  assert.deepEqual(normalizeAppearance({ palette: 'fog', mode: 'dark' }), { palette: 'fog', mode: 'dark' })
  assert.deepEqual(normalizeAppearance(null), DEFAULT_APPEARANCE)
})

test('readStoredAppearance prefers new storage and migrates legacy theme value', () => {
  const current = createStorage({
    themeAppearance: JSON.stringify({ palette: 'copper', mode: 'system' }),
    theme: 'dark'
  })
  const legacy = createStorage({ theme: 'dark' })

  assert.deepEqual(readStoredAppearance(current), { palette: 'copper', mode: 'system' })
  assert.deepEqual(readStoredAppearance(legacy), { palette: 'juniper', mode: 'dark' })
})

test('readStoredAppearance handles invalid JSON and unavailable storage', () => {
  const invalid = createStorage({ themeAppearance: '{bad json' })
  const broken = {
    getItem() {
      throw new Error('blocked')
    }
  }

  assert.deepEqual(readStoredAppearance(invalid), DEFAULT_APPEARANCE)
  assert.deepEqual(readStoredAppearance(broken), DEFAULT_APPEARANCE)
})

test('writeStoredAppearance persists normalized appearance and ignores storage errors', () => {
  const storage = createStorage()
  const broken = {
    setItem() {
      throw new Error('blocked')
    }
  }

  assert.equal(writeStoredAppearance(storage, { palette: 'fog', mode: 'dark' }), true)
  assert.deepEqual(JSON.parse(storage.snapshot().themeAppearance), { palette: 'fog', mode: 'dark' })
  assert.equal(writeStoredAppearance(broken, { palette: 'copper', mode: 'system' }), false)
})

test('resolveThemeName respects explicit and system modes', () => {
  assert.equal(resolveThemeName({ mode: 'light', systemPrefersDark: true }), 'light')
  assert.equal(resolveThemeName({ mode: 'dark', systemPrefersDark: false }), 'dark')
  assert.equal(resolveThemeName({ mode: 'system', systemPrefersDark: true }), 'dark')
  assert.equal(resolveThemeName({ mode: 'system', systemPrefersDark: false }), 'light')
})
```

- [ ] **Step 2: Run the new test to verify it fails**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/themeAppearance.test.mjs
```

Expected: FAIL with a module-not-found error for `./themeAppearance.js`.

- [ ] **Step 3: Implement the utility**

Create `blog-frontend/src/utils/themeAppearance.js`:

```js
export const THEME_PALETTES = [
  { value: 'juniper', label: '松针绿', description: '清爽克制，默认主题' },
  { value: 'fog', label: '雾蓝灰', description: '冷静耐读，适合长文' },
  { value: 'copper', label: '暖铜绿', description: '更有温度，适合创作' }
]

export const THEME_MODES = [
  { value: 'light', label: '浅色' },
  { value: 'dark', label: '深色' },
  { value: 'system', label: '跟随系统' }
]

export const DEFAULT_APPEARANCE = {
  palette: 'juniper',
  mode: 'light'
}

export const APPEARANCE_STORAGE_KEY = 'themeAppearance'
export const LEGACY_THEME_STORAGE_KEY = 'theme'

const paletteValues = new Set(THEME_PALETTES.map(item => item.value))
const modeValues = new Set(THEME_MODES.map(item => item.value))

export function normalizeAppearance(value) {
  const source = value && typeof value === 'object' ? value : {}
  return {
    palette: paletteValues.has(source.palette) ? source.palette : DEFAULT_APPEARANCE.palette,
    mode: modeValues.has(source.mode) ? source.mode : DEFAULT_APPEARANCE.mode
  }
}

export function readStoredAppearance(storage) {
  try {
    const raw = storage?.getItem?.(APPEARANCE_STORAGE_KEY)
    if (raw) return normalizeAppearance(JSON.parse(raw))

    const legacyTheme = storage?.getItem?.(LEGACY_THEME_STORAGE_KEY)
    if (legacyTheme === 'dark' || legacyTheme === 'light') {
      return normalizeAppearance({ palette: DEFAULT_APPEARANCE.palette, mode: legacyTheme })
    }
  } catch {
    return DEFAULT_APPEARANCE
  }

  return DEFAULT_APPEARANCE
}

export function writeStoredAppearance(storage, appearance) {
  try {
    storage?.setItem?.(APPEARANCE_STORAGE_KEY, JSON.stringify(normalizeAppearance(appearance)))
    return true
  } catch {
    return false
  }
}

export function resolveThemeName({ mode, systemPrefersDark }) {
  const normalized = normalizeAppearance({ mode }).mode
  if (normalized === 'system') return systemPrefersDark ? 'dark' : 'light'
  return normalized
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/themeAppearance.test.mjs
```

Expected: PASS for all tests in `themeAppearance.test.mjs`.

- [ ] **Step 5: Commit**

```powershell
git status --short
git add -- blog-frontend/src/utils/themeAppearance.js blog-frontend/src/utils/themeAppearance.test.mjs
git commit -m "test: 补充外观主题工具测试"
```

---

### Task 2: Theme Store Palette And Mode State

**Files:**
- Modify: `blog-frontend/src/stores/theme.js`
- Modify: `blog-frontend/src/utils/themeAppearance.test.mjs`

- [ ] **Step 1: Add source-level tests for store expectations**

Append to `blog-frontend/src/utils/themeAppearance.test.mjs`:

```js
import { readFileSync } from 'node:fs'

test('theme store applies palette and resolved theme attributes', () => {
  const source = readFileSync(new URL('../stores/theme.js', import.meta.url), 'utf8')

  assert.match(source, /palette:\s*DEFAULT_APPEARANCE\.palette/)
  assert.match(source, /mode:\s*DEFAULT_APPEARANCE\.mode/)
  assert.match(source, /setPalette\(palette\)/)
  assert.match(source, /setMode\(mode\)/)
  assert.match(source, /document\.documentElement\.setAttribute\('data-palette'/)
  assert.match(source, /document\.documentElement\.setAttribute\('data-theme'/)
  assert.match(source, /resolveThemeName/)
})
```

- [ ] **Step 2: Run the test to verify it fails**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/themeAppearance.test.mjs
```

Expected: FAIL because `theme.js` still has only `isDark` and `toggle()`.

- [ ] **Step 3: Replace the theme store implementation**

Replace `blog-frontend/src/stores/theme.js` with:

```js
import { defineStore } from 'pinia'
import {
  DEFAULT_APPEARANCE,
  THEME_MODES,
  THEME_PALETTES,
  normalizeAppearance,
  readStoredAppearance,
  resolveThemeName,
  writeStoredAppearance
} from '../utils/themeAppearance'

function getSystemMediaQuery() {
  if (typeof window === 'undefined' || typeof window.matchMedia !== 'function') return null
  return window.matchMedia('(prefers-color-scheme: dark)')
}

export const useThemeStore = defineStore('theme', {
  state: () => ({
    palette: DEFAULT_APPEARANCE.palette,
    mode: DEFAULT_APPEARANCE.mode,
    isDark: false,
    systemPrefersDark: false,
    palettes: THEME_PALETTES,
    modes: THEME_MODES,
    mediaQuery: null,
    mediaQueryHandler: null
  }),
  getters: {
    activePalette(state) {
      return state.palettes.find(item => item.value === state.palette) || state.palettes[0]
    },
    activeMode(state) {
      return state.modes.find(item => item.value === state.mode) || state.modes[0]
    }
  },
  actions: {
    init() {
      const stored = readStoredAppearance(typeof localStorage === 'undefined' ? null : localStorage)
      const next = normalizeAppearance(stored)
      this.palette = next.palette
      this.mode = next.mode
      this.bindSystemPreference()
      this.apply()
    },
    bindSystemPreference() {
      const query = getSystemMediaQuery()
      this.mediaQuery = query
      this.systemPrefersDark = !!query?.matches
      if (!query || this.mediaQueryHandler) return

      this.mediaQueryHandler = event => {
        this.systemPrefersDark = !!event.matches
        if (this.mode === 'system') this.apply(false)
      }

      if (typeof query.addEventListener === 'function') {
        query.addEventListener('change', this.mediaQueryHandler)
      } else if (typeof query.addListener === 'function') {
        query.addListener(this.mediaQueryHandler)
      }
    },
    persist() {
      writeStoredAppearance(typeof localStorage === 'undefined' ? null : localStorage, {
        palette: this.palette,
        mode: this.mode
      })
    },
    setPalette(palette) {
      this.palette = normalizeAppearance({ palette, mode: this.mode }).palette
      this.apply()
      this.persist()
    },
    setMode(mode) {
      this.mode = normalizeAppearance({ palette: this.palette, mode }).mode
      this.apply()
      this.persist()
    },
    toggle() {
      this.setMode(this.isDark ? 'light' : 'dark')
    },
    apply() {
      const theme = resolveThemeName({
        mode: this.mode,
        systemPrefersDark: this.systemPrefersDark
      })
      this.isDark = theme === 'dark'
      document.documentElement.setAttribute('data-palette', this.palette)
      document.documentElement.setAttribute('data-theme', theme)
    }
  }
})
```

- [ ] **Step 4: Run the test to verify it passes**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/themeAppearance.test.mjs
```

Expected: PASS.

- [ ] **Step 5: Commit**

```powershell
git status --short
git add -- blog-frontend/src/stores/theme.js blog-frontend/src/utils/themeAppearance.test.mjs
git commit -m "feat: 扩展外观主题状态"
```

---

### Task 3: Appearance Popover Entry

**Files:**
- Modify: `blog-frontend/src/components/ThemeToggle.vue`
- Create: `blog-frontend/src/utils/visualRefreshSource.test.mjs`

- [ ] **Step 1: Write source tests for the appearance entry**

Create `blog-frontend/src/utils/visualRefreshSource.test.mjs`:

```js
import test from 'node:test'
import assert from 'node:assert/strict'
import { readFileSync } from 'node:fs'

function read(relativePath) {
  return readFileSync(new URL(relativePath, import.meta.url), 'utf8')
}

test('theme toggle renders an appearance popover with palette and mode choices', () => {
  const source = read('../components/ThemeToggle.vue')

  assert.match(source, /class="appearance-trigger"/)
  assert.match(source, /外观/)
  assert.match(source, /themeStore\.palettes/)
  assert.match(source, /themeStore\.modes/)
  assert.match(source, /setPalette/)
  assert.match(source, /setMode/)
  assert.match(source, /class="palette-swatch"/)
})
```

- [ ] **Step 2: Run the source test to verify it fails**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
```

Expected: FAIL because `ThemeToggle.vue` is still icon-only.

- [ ] **Step 3: Replace `ThemeToggle.vue` with an appearance popover**

Replace `blog-frontend/src/components/ThemeToggle.vue` with:

```vue
<template>
  <el-popover
    popper-class="appearance-popover"
    placement="bottom-end"
    trigger="click"
    width="320"
  >
    <template #reference>
      <button type="button" class="appearance-trigger" aria-label="打开外观设置">
        <span class="appearance-dot" aria-hidden="true" />
        <span>外观</span>
      </button>
    </template>

    <section class="appearance-section" aria-label="明暗模式">
      <span class="appearance-label">明暗模式</span>
      <div class="mode-grid">
        <button
          v-for="mode in themeStore.modes"
          :key="mode.value"
          type="button"
          class="mode-option"
          :class="{ 'is-active': themeStore.mode === mode.value }"
          :aria-pressed="themeStore.mode === mode.value"
          @click="themeStore.setMode(mode.value)"
        >
          {{ mode.label }}
        </button>
      </div>
    </section>

    <section class="appearance-section" aria-label="固定配色">
      <span class="appearance-label">固定配色</span>
      <div class="palette-list">
        <button
          v-for="palette in themeStore.palettes"
          :key="palette.value"
          type="button"
          class="palette-option"
          :class="{ 'is-active': themeStore.palette === palette.value }"
          :aria-pressed="themeStore.palette === palette.value"
          @click="themeStore.setPalette(palette.value)"
        >
          <span class="palette-swatch" :data-palette-swatch="palette.value" aria-hidden="true">
            <i />
            <i />
            <i />
          </span>
          <span class="palette-copy">
            <strong>{{ palette.label }}</strong>
            <small>{{ palette.description }}</small>
          </span>
        </button>
      </div>
    </section>
  </el-popover>
</template>

<script setup>
import { useThemeStore } from '../stores/theme'

const themeStore = useThemeStore()
</script>

<style scoped>
.appearance-trigger {
  display: inline-flex;
  align-items: center;
  gap: 7px;
  min-height: 36px;
  padding: 0 12px;
  border: 1px solid var(--soft-border-color);
  border-radius: 999px;
  background: color-mix(in srgb, var(--panel-bg) 86%, transparent);
  color: var(--muted-text-color);
  font: inherit;
  font-weight: 700;
  cursor: pointer;
  transition: border-color 0.18s ease, background-color 0.18s ease, color 0.18s ease, transform 0.18s ease;
}

.appearance-trigger:hover {
  border-color: color-mix(in srgb, var(--primary-color) 34%, var(--soft-border-color));
  background: var(--panel-bg);
  color: var(--text-color);
  transform: translateY(-1px);
}

.appearance-dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--primary-color), var(--accent-color), var(--warning-color));
  box-shadow: 0 0 0 4px var(--primary-ring-color);
}

.appearance-section + .appearance-section {
  margin-top: 14px;
  padding-top: 14px;
  border-top: 1px solid var(--soft-border-color);
}

.appearance-label {
  display: block;
  margin-bottom: 8px;
  color: var(--muted-text-color);
  font-size: 12px;
  font-weight: 750;
}

.mode-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 8px;
}

.mode-option,
.palette-option {
  border: 1px solid var(--soft-border-color);
  border-radius: var(--radius-sm);
  background: var(--panel-bg);
  color: var(--muted-text-color);
  font: inherit;
  cursor: pointer;
}

.mode-option {
  min-height: 34px;
}

.mode-option.is-active,
.palette-option.is-active {
  border-color: color-mix(in srgb, var(--primary-color) 42%, var(--soft-border-color));
  background: color-mix(in srgb, var(--primary-color) 9%, var(--panel-bg));
  color: var(--text-color);
}

.palette-list {
  display: grid;
  gap: 8px;
}

.palette-option {
  display: grid;
  grid-template-columns: 52px minmax(0, 1fr);
  gap: 10px;
  align-items: center;
  min-height: 52px;
  padding: 8px 10px;
  text-align: left;
}

.palette-swatch {
  display: flex;
  gap: 4px;
}

.palette-swatch i {
  width: 14px;
  height: 30px;
  border-radius: 999px;
  background: var(--primary-color);
}

.palette-swatch i:nth-child(2) {
  background: var(--accent-color);
}

.palette-swatch i:nth-child(3) {
  background: var(--warning-color);
}

.palette-copy strong,
.palette-copy small {
  display: block;
}

.palette-copy small {
  margin-top: 2px;
  color: var(--muted-text-color);
  font-size: 12px;
}

@media (max-width: 640px) {
  .appearance-trigger span:last-child {
    display: none;
  }
}
</style>
```

- [ ] **Step 4: Add global popover styling**

Append to `blog-frontend/src/styles/global.css`:

```css
.appearance-popover {
  border-color: var(--soft-border-color) !important;
  border-radius: var(--radius-md) !important;
  background: color-mix(in srgb, var(--panel-bg) 96%, transparent) !important;
  color: var(--text-color) !important;
  box-shadow: var(--shadow-md) !important;
  backdrop-filter: blur(16px);
}
```

- [ ] **Step 5: Run the test and build**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
npm.cmd run build
```

Expected: source test PASS and Vite build succeeds.

- [ ] **Step 6: Commit**

```powershell
git status --short
git add -- blog-frontend/src/components/ThemeToggle.vue blog-frontend/src/styles/global.css blog-frontend/src/utils/visualRefreshSource.test.mjs
git commit -m "feat: 增加外观主题选择入口"
```

---

### Task 4: Theme Tokens And Global Visual Baseline

**Files:**
- Modify: `blog-frontend/src/styles/theme.css`
- Modify: `blog-frontend/src/styles/global.css`
- Modify: `blog-frontend/src/utils/visualRefreshSource.test.mjs`

- [ ] **Step 1: Extend source tests for theme CSS**

Append to `blog-frontend/src/utils/visualRefreshSource.test.mjs`:

```js
test('theme css defines fixed palette packages and reduced motion support', () => {
  const theme = read('../styles/theme.css')
  const global = read('../styles/global.css')

  assert.match(theme, /\[data-palette="juniper"\]/)
  assert.match(theme, /\[data-palette="fog"\]/)
  assert.match(theme, /\[data-palette="copper"\]/)
  assert.match(theme, /--theme-grid-x/)
  assert.match(theme, /--header-backdrop-bg/)
  assert.match(global, /prefers-reduced-motion:\s*reduce/)
  assert.doesNotMatch(global, /rgba\(47,\s*128,\s*237,\s*0\.08\)/)
})
```

- [ ] **Step 2: Run the source test to verify it fails**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
```

Expected: FAIL because the palette packages and reduced motion global rule do not exist yet.

- [ ] **Step 3: Add palette token blocks to `theme.css`**

At the top of `blog-frontend/src/styles/theme.css`, replace the current `:root` color token values with this structure, preserving the existing Element Plus token mappings below the color variables:

```css
:root,
[data-palette="juniper"] {
  color-scheme: light;
  --bg-color: #f5f6f1;
  --text-color: #252722;
  --muted-text-color: #6f746d;
  --card-bg: #ffffff;
  --panel-bg: #ffffff;
  --elevated-bg: #ffffff;
  --code-bg: #eef2ec;
  --border-color: #d6dbd1;
  --soft-border-color: #e6eae1;
  --primary-color: #496b59;
  --primary-hover-color: #365445;
  --accent-color: #526f8d;
  --warning-color: #c89b3c;
  --danger-color: #b35b46;
  --info-color: #6f746d;
  --surface-wash-color: color-mix(in srgb, var(--primary-color) 8%, transparent);
  --theme-grid-x: rgba(73, 107, 89, 0.08);
  --theme-grid-y: rgba(82, 111, 141, 0.07);
  --theme-glow-color: rgba(73, 107, 89, 0.28);
  --header-backdrop-bg: color-mix(in srgb, var(--panel-bg) 82%, transparent);
  --header-divider-color: color-mix(in srgb, var(--border-color) 64%, transparent);
  --primary-ring-color: color-mix(in srgb, var(--primary-color) 16%, transparent);
  --danger-ring-color: color-mix(in srgb, var(--danger-color) 14%, transparent);
  --shadow-sm: 0 8px 22px rgba(37, 39, 34, 0.07);
  --shadow-md: 0 18px 44px rgba(37, 39, 34, 0.1);
  --radius-sm: 6px;
  --radius-md: 8px;
  --radius-lg: 8px;
  --app-header-height: 64px;
  --content-width: 1180px;
  --reading-width: 820px;
}

[data-palette="fog"] {
  --bg-color: #f3f6f8;
  --text-color: #23282c;
  --muted-text-color: #66717a;
  --card-bg: #ffffff;
  --panel-bg: #ffffff;
  --elevated-bg: #ffffff;
  --code-bg: #e9eef2;
  --border-color: #d1d9df;
  --soft-border-color: #e1e7ec;
  --primary-color: #4f6979;
  --primary-hover-color: #3e5868;
  --accent-color: #6e83a4;
  --warning-color: #b3924a;
  --danger-color: #8f6f5f;
  --info-color: #66717a;
  --theme-grid-x: rgba(79, 105, 121, 0.08);
  --theme-grid-y: rgba(110, 131, 164, 0.07);
  --theme-glow-color: rgba(79, 105, 121, 0.28);
}

[data-palette="copper"] {
  --bg-color: #f4f3ee;
  --text-color: #2b2925;
  --muted-text-color: #736f67;
  --card-bg: #fffdf8;
  --panel-bg: #fffdf8;
  --elevated-bg: #fffdf8;
  --code-bg: #eeefea;
  --border-color: #ded7cd;
  --soft-border-color: #e7ded3;
  --primary-color: #5c6758;
  --primary-hover-color: #475142;
  --accent-color: #6b7f91;
  --warning-color: #c08f35;
  --danger-color: #b76142;
  --info-color: #736f67;
  --theme-grid-x: rgba(92, 103, 88, 0.08);
  --theme-grid-y: rgba(183, 97, 66, 0.06);
  --theme-glow-color: rgba(183, 97, 66, 0.26);
}
```

Then replace the existing `[data-theme="dark"]` block with palette-aware dark blocks:

```css
[data-theme="dark"] {
  color-scheme: dark;
  --bg-color: #151916;
  --text-color: #edf0e9;
  --muted-text-color: #aeb8ad;
  --card-bg: #1d231f;
  --panel-bg: #1d231f;
  --elevated-bg: #242c27;
  --code-bg: #202b26;
  --border-color: #344138;
  --soft-border-color: #2a352e;
  --primary-color: #9bbf9c;
  --primary-hover-color: #b7d3b5;
  --accent-color: #9db7d3;
  --warning-color: #dfbd68;
  --danger-color: #e08a73;
  --info-color: #aeb8ad;
  --surface-wash-color: color-mix(in srgb, var(--primary-color) 13%, transparent);
  --theme-grid-x: rgba(155, 191, 156, 0.08);
  --theme-grid-y: rgba(157, 183, 211, 0.07);
  --theme-glow-color: rgba(155, 191, 156, 0.26);
  --header-backdrop-bg: color-mix(in srgb, var(--panel-bg) 76%, transparent);
  --header-divider-color: color-mix(in srgb, var(--border-color) 70%, transparent);
  --primary-ring-color: color-mix(in srgb, var(--primary-color) 24%, transparent);
  --danger-ring-color: color-mix(in srgb, var(--danger-color) 20%, transparent);
  --shadow-sm: 0 10px 26px rgba(0, 0, 0, 0.22);
  --shadow-md: 0 20px 52px rgba(0, 0, 0, 0.3);
}

[data-palette="fog"][data-theme="dark"] {
  --bg-color: #13181d;
  --text-color: #edf1f3;
  --muted-text-color: #a8b3bd;
  --card-bg: #1b2228;
  --panel-bg: #1b2228;
  --elevated-bg: #222b33;
  --code-bg: #202b33;
  --border-color: #33414b;
  --soft-border-color: #2a3640;
  --primary-color: #9bb3c4;
  --primary-hover-color: #b8cad6;
  --accent-color: #b5b7de;
  --warning-color: #d4bc76;
  --danger-color: #cf9181;
  --info-color: #a8b3bd;
  --theme-grid-x: rgba(155, 179, 196, 0.08);
  --theme-grid-y: rgba(181, 183, 222, 0.07);
  --theme-glow-color: rgba(155, 179, 196, 0.26);
}

[data-palette="copper"][data-theme="dark"] {
  --bg-color: #181612;
  --text-color: #f0ece3;
  --muted-text-color: #b8afa2;
  --card-bg: #221f1a;
  --panel-bg: #221f1a;
  --elevated-bg: #2a251f;
  --code-bg: #2a251f;
  --border-color: #3f382f;
  --soft-border-color: #352f28;
  --primary-color: #a5b58e;
  --primary-hover-color: #bdc9aa;
  --accent-color: #a5b0bb;
  --warning-color: #deb45b;
  --danger-color: #dd8c64;
  --info-color: #b8afa2;
  --theme-grid-x: rgba(165, 181, 142, 0.07);
  --theme-grid-y: rgba(221, 140, 100, 0.07);
  --theme-glow-color: rgba(221, 140, 100, 0.26);
}
```

Keep the Element Plus token mapping below these blocks, but change hard-coded `white` and `black` mixes only where readability breaks. Do not change CSS variable names already used by pages.

- [ ] **Step 4: Replace hard-coded global blue hover colors**

In `blog-frontend/src/styles/global.css`, replace:

```css
.el-button.is-text:hover {
  color: var(--primary-color);
  background: rgba(47, 128, 237, 0.08);
}
```

with:

```css
.el-button.is-text:hover {
  color: var(--primary-color);
  background: color-mix(in srgb, var(--primary-color) 8%, transparent);
}
```

Replace Element table hard-coded primary mixes:

```css
--el-table-header-bg-color: rgba(47, 128, 237, 0.06);
--el-table-row-hover-bg-color: rgba(47, 128, 237, 0.06);
```

with:

```css
--el-table-header-bg-color: color-mix(in srgb, var(--primary-color) 6%, var(--panel-bg));
--el-table-row-hover-bg-color: color-mix(in srgb, var(--primary-color) 6%, var(--panel-bg));
```

Append:

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    scroll-behavior: auto !important;
    transition-duration: 0.01ms !important;
  }
}
```

- [ ] **Step 5: Run tests and build**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
npm.cmd run build
```

Expected: PASS and build succeeds.

- [ ] **Step 6: Commit**

```powershell
git status --short
git add -- blog-frontend/src/styles/theme.css blog-frontend/src/styles/global.css blog-frontend/src/utils/visualRefreshSource.test.mjs
git commit -m "feat: 增加固定外观主题变量"
```

---

### Task 5: Sticky Header Visual Refinement

**Files:**
- Modify: `blog-frontend/src/components/AppHeader.vue`
- Modify: `blog-frontend/src/utils/visualRefreshSource.test.mjs`

- [ ] **Step 1: Add header source assertions**

Append to `blog-frontend/src/utils/visualRefreshSource.test.mjs`:

```js
test('app header uses a full-width translucent sticky backdrop without top gap', () => {
  const source = read('../components/AppHeader.vue')

  assert.match(source, /\.app-header::before/)
  assert.match(source, /width:\s*100vw/)
  assert.match(source, /backdrop-filter:\s*blur/)
  assert.match(source, /mask-image:\s*linear-gradient/)
  assert.match(source, /top:\s*0/)
  assert.doesNotMatch(source, /border-radius:\s*0 0 24px 24px/)
})
```

- [ ] **Step 2: Run the source test to verify it fails**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
```

Expected: FAIL because `AppHeader.vue` has a direct panel background and no `::before` backdrop.

- [ ] **Step 3: Update header CSS**

In `blog-frontend/src/components/AppHeader.vue`, replace the `.app-header` style block with:

```css
.app-header {
  position: sticky;
  top: 0;
  z-index: 100;
  isolation: isolate;
  height: var(--app-header-height);
  padding: 0;
  border-bottom: 0;
  background: transparent;
}

.app-header::before {
  position: absolute;
  left: 50%;
  top: 0;
  bottom: -18px;
  z-index: -2;
  width: 100vw;
  background:
    linear-gradient(
      180deg,
      var(--header-backdrop-bg) 0%,
      color-mix(in srgb, var(--header-backdrop-bg) 84%, transparent) 62%,
      transparent 100%
    );
  backdrop-filter: blur(16px) saturate(1.08);
  content: '';
  mask-image: linear-gradient(180deg, #000 0%, #000 64%, transparent 100%);
  transform: translateX(-50%);
}

.app-header::after {
  position: absolute;
  left: 50%;
  bottom: 0;
  z-index: -1;
  width: 100vw;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--header-divider-color), transparent);
  content: '';
  transform: translateX(-50%);
}
```

Keep `.header-inner`, `.logo`, `.header-right`, and mobile rules. Replace `.logo-mark` box shadow:

```css
box-shadow: 0 10px 24px var(--theme-glow-color);
```

Replace `.user-info:hover` background:

```css
background: color-mix(in srgb, var(--primary-color) 8%, transparent);
```

- [ ] **Step 4: Run tests and build**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
npm.cmd run build
```

Expected: PASS and build succeeds.

- [ ] **Step 5: Commit**

```powershell
git status --short
git add -- blog-frontend/src/components/AppHeader.vue blog-frontend/src/utils/visualRefreshSource.test.mjs
git commit -m "feat: 优化顶部吸附导航质感"
```

---

### Task 6: Home Content Signal And Index Layout

**Files:**
- Modify: `blog-frontend/src/views/Home.vue`
- Modify: `blog-frontend/src/utils/visualRefreshSource.test.mjs`

- [ ] **Step 1: Add home source assertions**

Append to `blog-frontend/src/utils/visualRefreshSource.test.mjs`:

```js
test('home page exposes weekly content signal instead of reading ranking', () => {
  const source = read('../views/Home.vue')

  assert.match(source, /contentSignal/)
  assert.match(source, /本周内容信号/)
  assert.match(source, /content-signal/)
  assert.match(source, /signal-bars/)
  assert.doesNotMatch(source, /本周阅读信号/)
})
```

- [ ] **Step 2: Run the source test to verify it fails**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
```

Expected: FAIL because `Home.vue` still uses the old `signal-strip` metrics.

- [ ] **Step 3: Add content signal computed data**

In `blog-frontend/src/views/Home.vue`, after `const recentArticles = computed(() => articles.value.slice(0, 3))`, add:

```js
const contentSignal = computed(() => {
  const days = Array.from({ length: 7 }, (_, index) => {
    const date = new Date()
    date.setHours(0, 0, 0, 0)
    date.setDate(date.getDate() - (6 - index))
    return {
      key: date.toISOString().slice(0, 10),
      label: `${date.getMonth() + 1}/${date.getDate()}`,
      count: 0
    }
  })
  const dayMap = new Map(days.map(day => [day.key, day]))
  const topicCounts = new Map()

  articles.value.forEach(article => {
    const time = article.updatedAt || article.createdAt
    if (time) {
      const date = new Date(time)
      if (!Number.isNaN(date.getTime())) {
        const key = date.toISOString().slice(0, 10)
        const day = dayMap.get(key)
        if (day) day.count += 1
      }
    }
    ;(article.tags || []).forEach(tag => {
      topicCounts.set(tag.name, (topicCounts.get(tag.name) || 0) + 1)
    })
  })

  const maxCount = Math.max(1, ...days.map(day => day.count))
  const activeTopic = [...topicCounts.entries()].sort((a, b) => b[1] - a[1])[0]?.[0] || topTags.value[0]?.name || '暂无主题'
  const updatedCount = days.reduce((sum, day) => sum + day.count, 0)
  return {
    activeTopic,
    latestTitle: featuredArticle.value?.title || '',
    updatedCount,
    summary: updatedCount
      ? `本周更新 ${updatedCount} 篇，集中在 ${activeTopic}`
      : '本周暂无新更新，可从专题继续阅读',
    bars: days.map(day => ({
      ...day,
      height: `${Math.max(16, Math.round((day.count / maxCount) * 100))}%`
    }))
  }
})
```

- [ ] **Step 4: Replace the signal strip template**

Replace the current `<section class="signal-strip">...</section>` in `Home.vue` with:

```vue
<section class="content-signal" aria-label="本周内容信号">
  <div>
    <span class="eyebrow">本周内容信号</span>
    <h2>{{ contentSignal.activeTopic }}</h2>
    <p>{{ contentSignal.summary }}</p>
    <button
      v-if="featuredArticle"
      type="button"
      class="signal-link"
      @click="goArticle(featuredArticle)"
    >
      最近更新：{{ contentSignal.latestTitle }}
    </button>
  </div>
  <div class="signal-bars" aria-hidden="true">
    <span
      v-for="day in contentSignal.bars"
      :key="day.key"
      class="signal-bar"
      :style="{ '--signal-height': day.height }"
    />
  </div>
</section>
```

- [ ] **Step 5: Add home signal styles**

Replace `.signal-strip`, `.signal-card`, and related `.signal-card` rules in `Home.vue` with:

```css
.content-signal {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(220px, 320px);
  gap: 18px;
  align-items: stretch;
  margin-bottom: 22px;
  padding: 20px;
  border: 1px solid var(--soft-border-color);
  border-radius: var(--radius-md);
  background:
    linear-gradient(90deg, var(--surface-wash-color), transparent 72%),
    var(--panel-bg);
  box-shadow: var(--shadow-sm);
}

.content-signal h2 {
  margin: 6px 0 8px;
  color: var(--text-color);
  font-size: 26px;
  line-height: 1.2;
}

.content-signal p {
  margin: 0;
  color: var(--muted-text-color);
}

.signal-link {
  margin-top: 14px;
  padding: 0;
  border: 0;
  background: transparent;
  color: var(--primary-color);
  font: inherit;
  font-weight: 750;
  cursor: pointer;
}

.signal-bars {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 9px;
  align-items: end;
  min-height: 130px;
}

.signal-bar {
  height: var(--signal-height);
  min-height: 16px;
  border-radius: 999px 999px 4px 4px;
  background: linear-gradient(180deg, var(--accent-color), var(--primary-color));
  opacity: 0.86;
}

.signal-bar:nth-child(4) {
  background: linear-gradient(180deg, var(--danger-color), var(--warning-color));
}
```

In the `@media (max-width: 980px)` block, replace `.signal-strip` rules with:

```css
.content-signal {
  grid-template-columns: 1fr;
}
```

In the `@media (max-width: 640px)` block, remove `.signal-strip` from the selector list and keep `.hero-search` and `.home-sidebar`.

- [ ] **Step 6: Run tests and build**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
npm.cmd run build
```

Expected: PASS and build succeeds.

- [ ] **Step 7: Commit**

```powershell
git status --short
git add -- blog-frontend/src/views/Home.vue blog-frontend/src/utils/visualRefreshSource.test.mjs
git commit -m "feat: 增加首页内容信号区"
```

---

### Task 7: Article Card Micro-Interactions

**Files:**
- Modify: `blog-frontend/src/components/ArticleCard.vue`
- Modify: `blog-frontend/src/utils/visualRefreshSource.test.mjs`

- [ ] **Step 1: Add card source assertions**

Append to `blog-frontend/src/utils/visualRefreshSource.test.mjs`:

```js
test('article cards include focused line and reduced motion styling', () => {
  const source = read('../components/ArticleCard.vue')

  assert.match(source, /\.article-card::before/)
  assert.match(source, /transform:\s*scaleY/)
  assert.match(source, /theme-glow-color/)
  assert.match(source, /prefers-reduced-motion:\s*reduce/)
})
```

- [ ] **Step 2: Run the source test to verify it fails**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
```

Expected: FAIL because `ArticleCard.vue` has no focus line pseudo-element.

- [ ] **Step 3: Update article card styles**

In `blog-frontend/src/components/ArticleCard.vue`, add `position: relative; overflow: hidden;` to `.article-card` and add:

```css
.article-card::before {
  position: absolute;
  top: 16px;
  bottom: 16px;
  left: -1px;
  width: 3px;
  border-radius: 999px;
  background: linear-gradient(180deg, var(--primary-color), var(--accent-color));
  content: '';
  opacity: 0;
  transform: scaleY(0.55);
  transform-origin: center;
  transition: opacity 0.18s ease, transform 0.18s ease;
}

.article-card:hover::before,
.article-card:focus-visible::before {
  opacity: 1;
  transform: scaleY(1);
}
```

Replace `.article-card:hover` box shadow with:

```css
box-shadow: 0 18px 42px var(--theme-glow-color);
```

Replace `.card-marker` background with:

```css
background:
  linear-gradient(135deg, color-mix(in srgb, var(--primary-color) 18%, transparent), transparent),
  color-mix(in srgb, var(--accent-color) 8%, var(--bg-color));
```

Append to the existing reduced motion media query or create one:

```css
@media (prefers-reduced-motion: reduce) {
  .article-card,
  .article-card::before,
  .tag-link,
  .tag-link :deep(.el-tag) {
    transition: none;
  }

  .article-card:hover,
  .article-card:focus-visible,
  .tag-link:hover {
    transform: none;
  }
}
```

- [ ] **Step 4: Run tests and build**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
npm.cmd run build
```

Expected: PASS and build succeeds.

- [ ] **Step 5: Commit**

```powershell
git status --short
git add -- blog-frontend/src/components/ArticleCard.vue blog-frontend/src/utils/visualRefreshSource.test.mjs
git commit -m "feat: 优化文章卡片互动反馈"
```

---

### Task 8: Article Detail Reading Feedback

**Files:**
- Modify: `blog-frontend/src/views/ArticleDetail.vue`
- Modify: `blog-frontend/src/utils/readingPosition.test.mjs`
- Modify: `blog-frontend/src/utils/visualRefreshSource.test.mjs`

- [ ] **Step 1: Add tests for visible reading feedback**

Append to `blog-frontend/src/utils/readingPosition.test.mjs`:

```js
test('article detail exposes a visible progress bar driven by current reading progress', () => {
  const source = readFileSync(new URL('../views/ArticleDetail.vue', import.meta.url), 'utf8')

  assert.match(source, /class="article-progress-bar"/)
  assert.match(source, /currentReadingProgress/)
  assert.match(source, /--article-read-progress/)
})
```

Append to `blog-frontend/src/utils/visualRefreshSource.test.mjs`:

```js
test('article detail styles toc active indicator and themed resume dialog', () => {
  const source = read('../views/ArticleDetail.vue')

  assert.match(source, /\.toc-link::before/)
  assert.match(source, /\.toc-link\.active::before/)
  assert.match(source, /resume-reading-dialog/)
  assert.match(source, /theme-glow-color/)
})
```

- [ ] **Step 2: Run the tests to verify they fail**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/readingPosition.test.mjs src/utils/visualRefreshSource.test.mjs
```

Expected: FAIL because `ArticleDetail.vue` lacks `article-progress-bar` and TOC pseudo indicator styles.

- [ ] **Step 3: Add the progress bar template and state**

In `blog-frontend/src/views/ArticleDetail.vue`, add this immediately after `<AppHeader />`:

```vue
<div
  v-if="article"
  class="article-progress-bar"
  aria-hidden="true"
  :style="{ '--article-read-progress': `${currentReadingProgress}%` }"
>
  <span />
</div>
```

In the script setup refs, add:

```js
const currentReadingProgress = ref(0)
```

In `loadArticle()`, after `activeHeadingId.value = ''`, add:

```js
currentReadingProgress.value = 0
```

In `updateScrollState()`, before `scheduleReadingPositionSave(false)`, set progress from the existing metrics:

```js
const metrics = getArticleBodyMetrics()
if (metrics) {
  currentReadingProgress.value = calculateReadingProgress({
    scrollY: window.scrollY,
    articleTop: metrics.articleTop,
    articleHeight: metrics.articleHeight,
    viewportHeight: metrics.viewportHeight
  })
}
```

Keep `buildCurrentReadingPositionPayload()` as the source of the save payload.

- [ ] **Step 4: Add progress bar and TOC styles**

In `ArticleDetail.vue`, add before `.main`:

```css
.article-progress-bar {
  position: fixed;
  top: 0;
  left: 0;
  z-index: 120;
  width: 100%;
  height: 3px;
  pointer-events: none;
}

.article-progress-bar span {
  display: block;
  width: var(--article-read-progress);
  height: 100%;
  background: linear-gradient(90deg, var(--primary-color), var(--accent-color), var(--danger-color));
  box-shadow: 0 0 16px var(--theme-glow-color);
  transition: width 0.12s linear;
}
```

Add `position: relative;` to `.toc-link`, then add:

```css
.toc-link::before {
  position: absolute;
  top: 50%;
  left: -7px;
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--primary-color);
  content: '';
  opacity: 0;
  transform: translateY(-50%) scale(0.45);
  transition: opacity 0.18s ease, transform 0.18s ease;
}

.toc-link.active::before {
  opacity: 1;
  transform: translateY(-50%) scale(1);
}
```

Replace `.floating-reading-tools` box shadow with:

```css
box-shadow:
  0 16px 36px var(--theme-glow-color),
  0 4px 12px rgba(15, 23, 42, 0.1);
```

Add to the reduced motion block:

```css
.article-progress-bar span,
.toc-link,
.toc-link::before {
  transition: none;
}
```

- [ ] **Step 5: Run tests and build**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/readingPosition.test.mjs src/utils/visualRefreshSource.test.mjs
npm.cmd run build
```

Expected: PASS and build succeeds.

- [ ] **Step 6: Commit**

```powershell
git status --short
git add -- blog-frontend/src/views/ArticleDetail.vue blog-frontend/src/utils/readingPosition.test.mjs blog-frontend/src/utils/visualRefreshSource.test.mjs
git commit -m "feat: 增强文章详情阅读反馈"
```

---

### Task 9: My Reading Visual Continuation

**Files:**
- Modify: `blog-frontend/src/views/ReadingSpace.vue`
- Modify: `blog-frontend/src/utils/visualRefreshSource.test.mjs`

- [ ] **Step 1: Add reading space source assertions**

Append to `blog-frontend/src/utils/visualRefreshSource.test.mjs`:

```js
test('reading space uses continuation-focused visual classes', () => {
  const source = read('../views/ReadingSpace.vue')

  assert.match(source, /reading-main::before/)
  assert.match(source, /\.last-read::before/)
  assert.match(source, /reading-progress-track/)
  assert.match(source, /theme-glow-color/)
})
```

- [ ] **Step 2: Run the source test to verify it fails**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
```

Expected: FAIL because `ReadingSpace.vue` does not include these visual classes yet.

- [ ] **Step 3: Add progress track markup**

Inside the available `overview.lastRead` block in `ReadingSpace.vue`, after the existing `reading-progress` span, add:

```vue
<span
  v-if="formatReadingProgress(overview.lastRead.progressPercent)"
  class="reading-progress-track"
  aria-hidden="true"
  :style="{ '--reading-progress': `${overview.lastRead.progressPercent || 0}%` }"
>
  <i />
</span>
```

- [ ] **Step 4: Add reading continuation styles**

In `ReadingSpace.vue`, add `position: relative;` to `.reading-main`, then add:

```css
.reading-main::before {
  position: fixed;
  inset: 0;
  z-index: -1;
  background:
    linear-gradient(90deg, var(--theme-grid-x) 1px, transparent 1px),
    linear-gradient(180deg, var(--theme-grid-y) 1px, transparent 1px);
  background-size: 44px 44px;
  content: '';
  opacity: 0.62;
  pointer-events: none;
}
```

Add `overflow: hidden;` to `.last-read`, then add:

```css
.last-read::before {
  position: absolute;
  top: 18px;
  bottom: 18px;
  left: -1px;
  width: 3px;
  border-radius: 999px;
  background: linear-gradient(180deg, var(--primary-color), var(--accent-color));
  content: '';
  opacity: 0;
  transform: scaleY(0.55);
  transition: opacity 0.18s ease, transform 0.18s ease;
}

.last-read:not(.is-unavailable):hover::before {
  opacity: 1;
  transform: scaleY(1);
}

.reading-progress-track {
  display: block;
  width: min(100%, 320px);
  height: 7px;
  margin-top: 10px;
  overflow: hidden;
  border-radius: 999px;
  background: color-mix(in srgb, var(--border-color) 70%, transparent);
}

.reading-progress-track i {
  display: block;
  width: var(--reading-progress);
  height: 100%;
  border-radius: inherit;
  background: linear-gradient(90deg, var(--primary-color), var(--accent-color));
}
```

Add to the existing reduced motion block:

```css
.last-read::before,
.reading-progress-track i {
  transition: none;
}
```

- [ ] **Step 5: Run tests and build**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
npm.cmd run build
```

Expected: PASS and build succeeds.

- [ ] **Step 6: Commit**

```powershell
git status --short
git add -- blog-frontend/src/views/ReadingSpace.vue blog-frontend/src/utils/visualRefreshSource.test.mjs
git commit -m "feat: 优化我的阅读视觉延续"
```

---

### Task 10: Creator Workspace Visual Control

**Files:**
- Modify: `blog-frontend/src/views/creator/MyArticles.vue`
- Modify: `blog-frontend/src/utils/visualRefreshSource.test.mjs`

- [ ] **Step 1: Add creator source assertions**

Append to `blog-frontend/src/utils/visualRefreshSource.test.mjs`:

```js
test('creator article workspace uses control-console visual treatment', () => {
  const source = read('../views/creator/MyArticles.vue')

  assert.match(source, /creator-shell/)
  assert.match(source, /\.group-item::before/)
  assert.match(source, /status-token/)
  assert.doesNotMatch(source, /#2f80ed/)
  assert.doesNotMatch(source, /#7c5cff/)
})
```

- [ ] **Step 2: Run the source test to verify it fails**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
```

Expected: FAIL because the creator page still has old hard-coded action colors.

- [ ] **Step 3: Add a creator shell class and status token class**

In `MyArticles.vue`, change:

```vue
<el-main class="main">
```

to:

```vue
<el-main class="main creator-shell">
```

Change the status tag column:

```vue
<el-tag :type="statusType(row.status)">{{ statusText(row.status) }}</el-tag>
```

to:

```vue
<el-tag class="status-token" :type="statusType(row.status)">{{ statusText(row.status) }}</el-tag>
```

- [ ] **Step 4: Replace hard-coded action colors**

In `MyArticles.vue`, replace the three light action color blocks with:

```css
.article-primary-action.article-primary-action--edit,
.article-primary-action.article-primary-action--edit:hover,
.article-primary-action.article-primary-action--edit:focus {
  --action-border-color: color-mix(in srgb, var(--accent-color) 54%, transparent);
  --action-bg-color: color-mix(in srgb, var(--accent-color) 13%, var(--panel-bg));
  --action-text-color: var(--accent-color);
}

.article-primary-action.article-primary-action--view,
.article-primary-action.article-primary-action--view:hover,
.article-primary-action.article-primary-action--view:focus {
  --action-border-color: color-mix(in srgb, var(--primary-color) 52%, transparent);
  --action-bg-color: color-mix(in srgb, var(--primary-color) 12%, var(--panel-bg));
  --action-text-color: var(--primary-color);
}

.article-primary-action.article-primary-action--withdraw,
.article-primary-action.article-primary-action--withdraw:hover,
.article-primary-action.article-primary-action--withdraw:focus {
  --action-border-color: color-mix(in srgb, var(--warning-color) 52%, transparent);
  --action-bg-color: color-mix(in srgb, var(--warning-color) 14%, var(--panel-bg));
  --action-text-color: var(--warning-color);
}
```

Replace the dark-specific action blocks with:

```css
[data-theme="dark"] .article-primary-action.article-primary-action--edit,
[data-theme="dark"] .article-primary-action.article-primary-action--edit:hover,
[data-theme="dark"] .article-primary-action.article-primary-action--edit:focus,
[data-theme="dark"] .article-primary-action.article-primary-action--view,
[data-theme="dark"] .article-primary-action.article-primary-action--view:hover,
[data-theme="dark"] .article-primary-action.article-primary-action--view:focus,
[data-theme="dark"] .article-primary-action.article-primary-action--withdraw,
[data-theme="dark"] .article-primary-action.article-primary-action--withdraw:hover,
[data-theme="dark"] .article-primary-action.article-primary-action--withdraw:focus {
  --action-bg-color: color-mix(in srgb, currentColor 18%, var(--panel-bg));
}
```

- [ ] **Step 5: Add creator visual styles**

Add to `MyArticles.vue` styles:

```css
.creator-shell {
  position: relative;
}

.creator-shell::before {
  position: fixed;
  inset: 0;
  z-index: -1;
  background:
    linear-gradient(90deg, var(--theme-grid-x) 1px, transparent 1px),
    linear-gradient(180deg, var(--theme-grid-y) 1px, transparent 1px);
  background-size: 44px 44px;
  content: '';
  opacity: 0.5;
  pointer-events: none;
}

.group-item {
  position: relative;
  overflow: hidden;
}

.group-item::before {
  position: absolute;
  top: 9px;
  bottom: 9px;
  left: 0;
  width: 3px;
  border-radius: 999px;
  background: linear-gradient(180deg, var(--primary-color), var(--accent-color));
  content: '';
  opacity: 0;
  transform: scaleY(0.55);
  transition: opacity 0.18s ease, transform 0.18s ease;
}

.group-item:hover::before,
.group-item.is-active::before {
  opacity: 1;
  transform: scaleY(1);
}

.status-token {
  font-weight: 750;
}

.article-table :deep(.el-table__body tr) {
  transition: background-color 0.18s ease;
}
```

Remove the old `.group-item.is-active { box-shadow: inset 3px 0 0 var(--accent-color); }` rule so the new pseudo-element owns the focus line.

Add to the media query:

```css
@media (prefers-reduced-motion: reduce) {
  .group-item::before,
  .article-table :deep(.el-table__body tr),
  .article-primary-action,
  .article-more-button {
    transition: none;
  }
}
```

- [ ] **Step 6: Run tests and build**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test -- src/utils/visualRefreshSource.test.mjs
npm.cmd run build
```

Expected: PASS and build succeeds.

- [ ] **Step 7: Commit**

```powershell
git status --short
git add -- blog-frontend/src/views/creator/MyArticles.vue blog-frontend/src/utils/visualRefreshSource.test.mjs
git commit -m "feat: 优化创作空间控制台视觉"
```

---

### Task 11: Final Regression, Docs, And Commit

**Files:**
- Modify: `docs/项目状态与目标.md`

- [ ] **Step 1: Run full frontend verification**

Run:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test
npm.cmd run build
```

Expected: all frontend tests pass and Vite build succeeds.

- [ ] **Step 2: Confirm backend was not touched**

Run:

```powershell
cd D:\03PerProject
git diff --name-only HEAD -- blog-backend
```

Expected: no output. If there is backend output, stop and inspect why backend files changed.

- [ ] **Step 3: Update project status document**

In `docs/项目状态与目标.md`, move the visual optimization entry from “已排期但未开始” to “已实现待验收”. Use this text:

```md
- 前端视觉体验个性化优化：已完成现代私人知识空间视觉基调、公开首页内容信号、文章详情阅读反馈、我的阅读和创作空间视觉统一、固定主题包切换和轻量互动反馈；等待用户页面验收。
```

Keep the design and plan links in the same section:

```md
  - 设计文档：`docs/superpowers/blog-frontend/specs/2026-07-22-视觉体验个性化优化设计.md`
  - 实施计划：`docs/superpowers/plans/2026-07-22-视觉体验个性化优化实施计划.md`
```

- [ ] **Step 4: Run document diff checks**

Run:

```powershell
cd D:\03PerProject
git diff --check
git status --short
```

Expected: `git diff --check` has no errors; `git status --short` shows only intended frontend and docs files.

- [ ] **Step 5: Commit final docs update**

```powershell
git add -- docs/项目状态与目标.md
git commit -m "docs: 标记视觉体验优化待验收"
```

---

## Final Verification Commands

Run before claiming implementation complete:

```powershell
cd D:\03PerProject\blog-frontend
npm.cmd test
npm.cmd run build
```

Backend tests are not required for this plan unless implementation unexpectedly changes `blog-backend` files.

## Self-Review Notes

- Spec coverage: tasks cover fixed theme packages, local persistence, system mode, header polish, public home content signal, article detail feedback, reading space, creator workspace, reduced motion, mobile-safe layout checks via build, and final status docs.
- Scope: plan is frontend-only and does not add database scripts, backend endpoints, visitor history, hot ranking, comments, likes, or public article “已读” labels.
- Type consistency: `palette`, `mode`, `isDark`, `setPalette`, `setMode`, `THEME_PALETTES`, `THEME_MODES`, and `DEFAULT_APPEARANCE` are introduced in Task 1 and reused consistently.
