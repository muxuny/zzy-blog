# 首页与详情页第一阶段内容增强 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不新增后端接口和数据库字段的前提下，让博客首页和文章详情页更完整、更适合浏览和阅读。

**Architecture:** 新增纯前端阅读工具函数，负责 Markdown 目录提取、标题锚点生成、字数和预计阅读时间计算。首页继续复用公开文章列表和标签接口，详情页继续复用公开文章详情接口，页面组件只做展示和交互组织。

**Tech Stack:** Vue 3、Vue Router、Element Plus、Marked、Node.js 内置测试、CSS 响应式布局。

---

### Task 1: 阅读工具函数

**Files:**
- Create: `blog-frontend/src/utils/reading.js`
- Create: `blog-frontend/src/utils/reading.test.mjs`

- [ ] **Step 1: Write failing tests**

Create `blog-frontend/src/utils/reading.test.mjs`:

```js
import test from 'node:test'
import assert from 'node:assert/strict'
import {
  createHeadingId,
  extractMarkdownToc,
  getReadingStats
} from './reading.js'

test('extractMarkdownToc returns level 2 and 3 headings with stable ids', () => {
  const markdown = [
    '# 标题',
    '## 第一部分',
    '正文',
    '### 细节 A',
    '```js',
    '## 代码里的标题',
    '```',
    '## 第一部分'
  ].join('\n')

  assert.deepEqual(extractMarkdownToc(markdown), [
    { id: '第一部分', level: 2, text: '第一部分' },
    { id: '细节-a', level: 3, text: '细节 A' },
    { id: '第一部分-2', level: 2, text: '第一部分' }
  ])
})

test('createHeadingId keeps duplicate headings stable', () => {
  const seen = new Map()

  assert.equal(createHeadingId('Vue 组件设计', seen), 'vue-组件设计')
  assert.equal(createHeadingId('Vue 组件设计', seen), 'vue-组件设计-2')
})

test('getReadingStats strips markdown and returns readable numbers', () => {
  const markdown = [
    '## 阅读体验',
    '这是一段中文内容，用来测试阅读时间。',
    'It also has frontend notes and markdown links.',
    '[链接](https://example.com)',
    '`code`'
  ].join('\n')

  const stats = getReadingStats(markdown)

  assert.equal(stats.wordCount > 0, true)
  assert.equal(stats.readingMinutes >= 1, true)
  assert.equal(stats.readingTimeText.endsWith('分钟'), true)
})
```

- [ ] **Step 2: Run test and verify it fails**

Run:

```powershell
node --test src/utils/reading.test.mjs
```

Expected: FAIL because `reading.js` does not exist.

- [ ] **Step 3: Implement utility functions**

Create `blog-frontend/src/utils/reading.js`:

```js
export function createHeadingId(text, seen = new Map()) {
  const base = String(text || '')
    .trim()
    .toLowerCase()
    .replace(/<[^>]*>/g, '')
    .replace(/[`*_~[\]()]/g, '')
    .replace(/[^\u4e00-\u9fa5a-z0-9\s-]/g, '')
    .replace(/\s+/g, '-')
    .replace(/-+/g, '-')
    .replace(/^-|-$/g, '') || 'section'

  const count = (seen.get(base) || 0) + 1
  seen.set(base, count)
  return count === 1 ? base : `${base}-${count}`
}

export function extractMarkdownToc(markdown = '') {
  const seen = new Map()
  const toc = []
  let inFence = false

  String(markdown).split(/\r?\n/).forEach(line => {
    if (/^\s*```/.test(line)) {
      inFence = !inFence
      return
    }
    if (inFence) return

    const match = /^(#{2,3})\s+(.+?)\s*#*\s*$/.exec(line)
    if (!match) return

    const text = match[2].replace(/[`*_~]/g, '').trim()
    toc.push({
      id: createHeadingId(text, seen),
      level: match[1].length,
      text
    })
  })

  return toc
}

export function getReadingStats(markdown = '') {
  const plain = String(markdown)
    .replace(/```[\s\S]*?```/g, ' ')
    .replace(/`[^`]*`/g, ' ')
    .replace(/!\[[^\]]*]\([^)]*\)/g, ' ')
    .replace(/\[([^\]]+)]\([^)]*\)/g, '$1')
    .replace(/<[^>]*>/g, ' ')
    .replace(/[#>*_~\-[\]()]/g, ' ')

  const cjkCount = (plain.match(/[\u4e00-\u9fa5]/g) || []).length
  const latinCount = (plain.replace(/[\u4e00-\u9fa5]/g, ' ').match(/[a-zA-Z0-9]+(?:[-'][a-zA-Z0-9]+)*/g) || []).length
  const wordCount = cjkCount + latinCount
  const readingMinutes = Math.max(1, Math.ceil(wordCount / 450))

  return {
    wordCount,
    readingMinutes,
    readingTimeText: `约 ${readingMinutes} 分钟`
  }
}
```

- [ ] **Step 4: Run test and verify it passes**

Run:

```powershell
node --test src/utils/reading.test.mjs
```

Expected: PASS with 3 tests passing.

### Task 2: Markdown 标题锚点

**Files:**
- Modify: `blog-frontend/src/components/MarkdownRenderer.vue`

- [ ] **Step 1: Add heading id rendering**

Update the script so rendered Markdown headings receive stable ids:

```js
import { computed } from 'vue'
import { marked } from 'marked'
import hljs from 'highlight.js'
import { createHeadingId } from '../utils/reading'
import 'highlight.js/styles/github.css'

function createRenderer() {
  const renderer = new marked.Renderer()
  const seen = new Map()

  renderer.heading = (text, level, raw) => {
    const id = createHeadingId(raw || text, seen)
    return `<h${level} id="${id}">${text}</h${level}>`
  }

  return renderer
}

marked.setOptions({
  highlight(code, lang) {
    if (lang && hljs.getLanguage(lang)) {
      try { return hljs.highlight(code, { language: lang }).value }
      catch {}
    }
    return hljs.highlightAuto(code).value
  }
})

const props = defineProps({ content: { type: String, default: '' } })
const rendered = computed(() => marked(props.content || '', { renderer: createRenderer() }))
```

- [ ] **Step 2: Run frontend build**

Run:

```powershell
npm.cmd run build
```

Expected: build succeeds.

### Task 3: 首页内容增强

**Files:**
- Modify: `blog-frontend/src/views/Home.vue`

- [ ] **Step 1: Add state and computed values**

Use `keywordInput`, `keyword`, `featuredArticle`, `listArticles`, `emptyDescription`, `activeTagName`, `articleTotalText`, and current year.

- [ ] **Step 2: Update data loading**

When loading articles, pass both `tagId` and `keyword` to `getArticles`. Search submits set `page = 1` and reload. Clearing search resets `keyword`.

- [ ] **Step 3: Update template**

Add:

- intro panel with blog name, description, article total, tag total
- search bar
- tag panel with “全部文章”
- current filter summary
- featured latest article
- improved empty state
- footer

- [ ] **Step 4: Add responsive styles**

Keep the current two-column layout on desktop and switch to single-column on mobile. Avoid nested cards; use full-width sections and individual cards only for repeated article items.

- [ ] **Step 5: Run frontend build**

Run:

```powershell
npm.cmd run build
```

Expected: build succeeds.

### Task 4: 详情页阅读增强

**Files:**
- Modify: `blog-frontend/src/views/ArticleDetail.vue`

- [ ] **Step 1: Add computed reading metadata**

Use `extractMarkdownToc` and `getReadingStats` to compute:

- `toc`
- `readingStats`
- `articleTags`
- fallback author

- [ ] **Step 2: Add interactions**

Add:

- `scrollToHeading(id)`
- `scrollToTop()`

- [ ] **Step 3: Update template**

Add:

- optional cover image
- article info sidebar
- table of contents
- mobile-friendly article info block
- back-to-top button

- [ ] **Step 4: Add responsive styles**

Desktop uses article body plus right sidebar. Mobile stacks title, cover, info, toc, and content.

- [ ] **Step 5: Run frontend build**

Run:

```powershell
npm.cmd run build
```

Expected: build succeeds.

### Task 5: Final verification and commit

**Files:**
- Verify: `blog-frontend/src/utils/reading.js`
- Verify: `blog-frontend/src/utils/reading.test.mjs`
- Verify: `blog-frontend/src/components/MarkdownRenderer.vue`
- Verify: `blog-frontend/src/views/Home.vue`
- Verify: `blog-frontend/src/views/ArticleDetail.vue`

- [ ] **Step 1: Run utility tests**

Run:

```powershell
node --test src/utils/reading.test.mjs
```

Expected: 3 tests pass.

- [ ] **Step 2: Run frontend build**

Run:

```powershell
npm.cmd run build
```

Expected: build succeeds. Existing large chunk warning is acceptable.

- [ ] **Step 3: Review diff**

Run:

```powershell
git diff --stat
```

Expected: only first-stage public page enhancement files and this plan are changed.

- [ ] **Step 4: Commit**

Run:

```powershell
git add blog-frontend/src/utils/reading.js blog-frontend/src/utils/reading.test.mjs blog-frontend/src/components/MarkdownRenderer.vue blog-frontend/src/views/Home.vue blog-frontend/src/views/ArticleDetail.vue docs/superpowers/blog-frontend/plans/2026-06-18-首页与详情页第一阶段内容增强.md
git commit -m "feat: enhance public home and article pages"
```

Expected: commit succeeds.

