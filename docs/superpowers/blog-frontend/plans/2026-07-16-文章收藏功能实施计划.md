# 文章收藏功能 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为登录读者提供幂等文章收藏、登录后自动续接、可筛选的“我的收藏”页面和不可公开文章标题快照提示。

**Architecture:** 使用独立 `article_favorite` 关系表保存用户、文章、标题快照和现有公共字段；Mapper XML 负责原子新增/恢复、逻辑取消和数据库级分页筛选。`FavoriteService` 只管理收藏关系，`ArticleService` 继续统一判断并装配公开文章摘要；前端通过独立 API、纯函数工具、详情按钮和收藏页面接入。

**Tech Stack:** Java 8、Spring Boot 2.7、Spring Security、MyBatis-Plus 3.5、MySQL、JUnit 5、Mockito、Vue 3、Pinia、Vue Router、Element Plus、Node Test Runner、Vite

---

## 文件结构

后端新增文件：

- `entity/ArticleFavorite.java`：收藏关系及公共字段继承。
- `dto/FavoritePageQuery.java`：收藏分页、关键词和标签参数。
- `dto/FavoriteRelationRow.java`：Mapper 分页查询的内部关系行。
- `dto/FavoriteArticleItem.java`：前端收藏列表条目。
- `dto/FavoriteStatus.java`：详情页收藏状态。
- `mapper/ArticleFavoriteMapper.java` 与 `resources/mapper/ArticleFavoriteMapper.xml`：原子关系写入和分页查询。
- `service/FavoriteService.java` 与 `service/impl/FavoriteServiceImpl.java`：当前用户收藏业务。
- `controller/FavoriteController.java`：`/api/my/favorites` 接口。
- `db/migration/2026-07-16-新增文章收藏.sql`：增量迁移。

前端新增文件：

- `api/favorite.js`：收藏接口封装。
- `utils/favorite.js` 与 `utils/favorite.test.mjs`：登录意图和筛选参数纯函数。
- `components/FavoriteArticleItem.vue`：可用/不可用收藏列表项。
- `views/Favorites.vue`：我的收藏页面。
- `scripts/verify-favorite-ui.mjs`：路由、入口、详情按钮和不可用状态静态校验。

现有文件只做对应职责内的修改，不拆分 `ArticleServiceImpl` 或重构无关页面。

### Task 1: 添加收藏表、公共字段实体和增量迁移

**Files:**
- Create: `blog-backend/src/main/resources/db/migration/2026-07-16-新增文章收藏.sql`
- Create: `blog-backend/src/main/java/com/blog/entity/ArticleFavorite.java`
- Modify: `blog-backend/src/main/resources/db/init.sql`
- Modify: `blog-backend/src/test/java/com/blog/config/DatabaseConfigTest.java`

- [ ] **Step 1: 写入迁移结构失败测试**

在 `DatabaseConfigTest` 增加：

```java
@Test
void favoriteSchemaIncludesBusinessAndCommonFields() throws IOException {
    String migration = readString(Paths.get(
            "src/main/resources/db/migration/2026-07-16-新增文章收藏.sql"));
    String normalized = migration.toLowerCase(Locale.ROOT);

    assertTrue(normalized.contains("create table if not exists `article_favorite`"));
    for (String column : Arrays.asList(
            "`user_id`", "`article_id`", "`title_snapshot`",
            "`created_by`", "`created_at`", "`updated_by`", "`updated_at`",
            "`deleted`", "`version`")) {
        assertTrue(normalized.contains(column), "Missing favorite column " + column);
    }
    assertTrue(normalized.contains("unique key `uk_article_favorite_user_article` (`user_id`, `article_id`)"));
    assertTrue(normalized.contains("key `idx_article_favorite_user_deleted_created_at`"));
    assertTrue(normalized.contains("key `idx_article_favorite_article_deleted`"));
    assertTrue(!normalized.contains("drop table"));
    assertTrue(!normalized.contains("delete from"));
}
```

同时增加 `import java.util.Arrays;`。

- [ ] **Step 2: 运行测试并确认红灯**

Run: `mvn -q -Dtest=DatabaseConfigTest#favoriteSchemaIncludesBusinessAndCommonFields test`

Expected: FAIL，原因是迁移脚本尚不存在。

- [ ] **Step 3: 添加迁移、初始化表和实体**

迁移脚本和 `init.sql` 使用同一张表定义：

```sql
CREATE TABLE IF NOT EXISTS `article_favorite` (
    `id` BIGINT NOT NULL COMMENT '雪花ID',
    `user_id` BIGINT NOT NULL COMMENT '收藏用户ID',
    `article_id` BIGINT NOT NULL COMMENT '文章ID',
    `title_snapshot` VARCHAR(200) NOT NULL COMMENT '收藏时公开标题快照',
    `created_by` VARCHAR(50) DEFAULT NULL COMMENT '创建人',
    `created_at` DATETIME DEFAULT NULL COMMENT '创建时间',
    `updated_by` VARCHAR(50) DEFAULT NULL COMMENT '更新人',
    `updated_at` DATETIME DEFAULT NULL COMMENT '更新时间',
    `deleted` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '逻辑删除：0未删除，1已删除',
    `version` INT NOT NULL DEFAULT 0 COMMENT '乐观锁版本号',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_article_favorite_user_article` (`user_id`, `article_id`),
    KEY `idx_article_favorite_user_deleted_created_at` (`user_id`, `deleted`, `created_at`),
    KEY `idx_article_favorite_article_deleted` (`article_id`, `deleted`),
    CONSTRAINT `fk_article_favorite_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`)
        ON DELETE CASCADE ON UPDATE CASCADE,
    CONSTRAINT `fk_article_favorite_article` FOREIGN KEY (`article_id`) REFERENCES `article` (`id`)
        ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='文章收藏关系表';
```

实体完整内容：

```java
package com.blog.entity;

import com.baomidou.mybatisplus.annotation.TableName;
import com.blog.common.BaseEntity;
import lombok.Data;
import lombok.EqualsAndHashCode;

/** 登录用户与公开文章的收藏关系。 */
@Data
@EqualsAndHashCode(callSuper = true)
@TableName("article_favorite")
public class ArticleFavorite extends BaseEntity {
    private Long userId;
    private Long articleId;
    private String titleSnapshot;
}
```

- [ ] **Step 4: 运行定向和数据库配置测试**

Run: `mvn -q -Dtest=DatabaseConfigTest test`

Expected: PASS，迁移脚本和初始化脚本检查全部通过。

- [ ] **Step 5: 提交数据模型**

```text
git add blog-backend/src/main/resources/db/init.sql blog-backend/src/main/resources/db/migration/2026-07-16-新增文章收藏.sql blog-backend/src/main/java/com/blog/entity/ArticleFavorite.java blog-backend/src/test/java/com/blog/config/DatabaseConfigTest.java
git commit -m "feat: 添加文章收藏数据模型"
```

### Task 2: 让文章服务提供不增加阅读数的公开摘要

**Files:**
- Modify: `blog-backend/src/main/java/com/blog/service/ArticleService.java`
- Modify: `blog-backend/src/main/java/com/blog/service/impl/ArticleServiceImpl.java`
- Modify: `blog-backend/src/test/java/com/blog/service/ArticleServiceImplTest.java`

- [ ] **Step 1: 写入公开摘要失败测试**

在 `ArticleServiceImplTest` 增加：

```java
@Test
void getPublicArticleSummary_shouldNotIncreaseViewCount() {
    Article article = article(1L, "alice", ArticleStatus.PUBLISHED);
    article.setViewCount(7);
    articleService.put(article);

    Article summary = articleService.getPublicArticleSummary(1L);

    assertThat(summary.getViewCount()).isEqualTo(7);
    assertThat(articleService.getById(1L).getViewCount()).isEqualTo(7);
}

@Test
void getPublicArticleSummaries_shouldFilterUnavailableArticles() {
    Article visible = article(1L, "alice", ArticleStatus.PUBLISHED);
    Article privateArticle = article(2L, "alice", ArticleStatus.PUBLISHED);
    privateArticle.setVisibility(ArticleVisibility.PRIVATE);
    articleService.put(visible);
    articleService.put(privateArticle);
    articleService.put(article(3L, "alice", ArticleStatus.DRAFT));

    List<Article> summaries = articleService.getPublicArticleSummaries(Arrays.asList(1L, 2L, 3L));

    assertThat(summaries).extracting(Article::getId).containsExactly(1L);
}
```

- [ ] **Step 2: 运行测试并确认红灯**

Run: `mvn -q '-Dtest=ArticleServiceImplTest#getPublicArticleSummary_shouldNotIncreaseViewCount+getPublicArticleSummaries_shouldFilterUnavailableArticles' test`

Expected: FAIL，`ArticleService` 尚无两个公开摘要方法。

- [ ] **Step 3: 添加接口和最小实现**

在 `ArticleService` 增加：

```java
Article getPublicArticleSummary(Long id);

List<Article> getPublicArticleSummaries(java.util.Collection<Long> articleIds);
```

在 `ArticleServiceImpl` 增加：

```java
@Override
public Article getPublicArticleSummary(Long id) {
    Article article = requirePublicArticle(id);
    attachTagsAndAuthor(article);
    return article;
}

@Override
public List<Article> getPublicArticleSummaries(Collection<Long> articleIds) {
    List<Article> articles = listPublicArticlesByIds(articleIds);
    articles.forEach(this::attachTagsAndAuthor);
    return articles;
}
```

这两个方法不得调用 `getPublicDetail`，否则会增加阅读数。

- [ ] **Step 4: 运行定向和文章服务测试**

Run: `mvn -q -Dtest=ArticleServiceImplTest test`

Expected: PASS，现有公开详情阅读数测试和新增摘要测试都通过。

- [ ] **Step 5: 提交文章摘要能力**

```text
git add blog-backend/src/main/java/com/blog/service/ArticleService.java blog-backend/src/main/java/com/blog/service/impl/ArticleServiceImpl.java blog-backend/src/test/java/com/blog/service/ArticleServiceImplTest.java
git commit -m "feat: 提供收藏所需公开文章摘要"
```

### Task 3: 实现收藏、状态查询和逻辑取消

**Files:**
- Create: `blog-backend/src/main/java/com/blog/dto/FavoriteStatus.java`
- Create: `blog-backend/src/main/java/com/blog/mapper/ArticleFavoriteMapper.java`
- Create: `blog-backend/src/main/resources/mapper/ArticleFavoriteMapper.xml`
- Create: `blog-backend/src/main/java/com/blog/service/FavoriteService.java`
- Create: `blog-backend/src/main/java/com/blog/service/impl/FavoriteServiceImpl.java`
- Create: `blog-backend/src/test/java/com/blog/service/FavoriteServiceImplTest.java`

- [ ] **Step 1: 写入关系操作失败测试**

测试使用 Mockito 构造 `ArticleFavoriteMapper`、`ArticleService` 和 `UserService`，覆盖以下方法：

```java
@Test
void favoriteArticle_shouldSavePublicTitleForCurrentUser() {
    when(userService.getCurrentUser("alice")).thenReturn(user(10L, "alice"));
    Article article = new Article();
    article.setId(20L);
    article.setTitle("Visible title");
    when(articleService.getPublicArticleSummary(20L)).thenReturn(article);

    favoriteService.favoriteArticle(20L, "alice");

    verify(favoriteMapper).upsertFavorite(
            anyLong(), eq(10L), eq(20L), eq("Visible title"), eq("alice"), any(LocalDateTime.class));
}

@Test
void favoriteArticle_shouldHidePublicLookupFailure() {
    when(userService.getCurrentUser("alice")).thenReturn(user(10L, "alice"));
    when(articleService.getPublicArticleSummary(20L))
            .thenThrow(new BusinessException("文章不存在"));

    assertThatThrownBy(() -> favoriteService.favoriteArticle(20L, "alice"))
            .isInstanceOf(BusinessException.class)
            .hasMessage("文章不存在或暂不可收藏");
}

@Test
void favoriteStatusAndCancel_shouldUseOnlyCurrentUserId() {
    when(userService.getCurrentUser("alice")).thenReturn(user(10L, "alice"));
    when(favoriteMapper.countActiveFavorite(10L, 20L)).thenReturn(1L);

    assertThat(favoriteService.getFavoriteStatus(20L, "alice").isFavorited()).isTrue();
    favoriteService.unfavoriteArticle(20L, "alice");

    verify(favoriteMapper).cancelFavorite(eq(10L), eq(20L), eq("alice"), any(LocalDateTime.class));
}
```

测试辅助用户方法返回设置了 ID 和用户名的 `User`。

- [ ] **Step 2: 运行测试并确认红灯**

Run: `mvn -q -Dtest=FavoriteServiceImplTest test`

Expected: FAIL，收藏 Mapper、Service 和 DTO 尚不存在。

- [ ] **Step 3: 添加关系接口、DTO 和原子 SQL**

`FavoriteStatus`：

```java
@Data
@AllArgsConstructor
public class FavoriteStatus {
    private boolean favorited;
}
```

`FavoriteService`：

```java
public interface FavoriteService {
    void favoriteArticle(Long articleId, String username);
    void unfavoriteArticle(Long articleId, String username);
    FavoriteStatus getFavoriteStatus(Long articleId, String username);
}
```

`ArticleFavoriteMapper` 先声明：

```java
public interface ArticleFavoriteMapper extends BaseMapper<ArticleFavorite> {
    int upsertFavorite(@Param("id") Long id,
                       @Param("userId") Long userId,
                       @Param("articleId") Long articleId,
                       @Param("titleSnapshot") String titleSnapshot,
                       @Param("username") String username,
                       @Param("now") LocalDateTime now);

    int cancelFavorite(@Param("userId") Long userId,
                       @Param("articleId") Long articleId,
                       @Param("username") String username,
                       @Param("now") LocalDateTime now);

    long countActiveFavorite(@Param("userId") Long userId,
                             @Param("articleId") Long articleId);
}
```

Mapper XML 写入完整关系 SQL：

```xml
<mapper namespace="com.blog.mapper.ArticleFavoriteMapper">
  <insert id="upsertFavorite">
    INSERT INTO article_favorite
      (id, user_id, article_id, title_snapshot, created_by, created_at,
       updated_by, updated_at, deleted, version)
    VALUES
      (#{id}, #{userId}, #{articleId}, #{titleSnapshot}, #{username}, #{now},
       #{username}, #{now}, 0, 0)
    ON DUPLICATE KEY UPDATE
      title_snapshot = IF(deleted = 1, VALUES(title_snapshot), title_snapshot),
      created_by = IF(deleted = 1, VALUES(created_by), created_by),
      created_at = IF(deleted = 1, VALUES(created_at), created_at),
      updated_by = IF(deleted = 1, VALUES(updated_by), updated_by),
      updated_at = IF(deleted = 1, VALUES(updated_at), updated_at),
      version = IF(deleted = 1, version + 1, version),
      deleted = 0
  </insert>

  <update id="cancelFavorite">
    UPDATE article_favorite
    SET deleted = 1,
        updated_by = #{username},
        updated_at = #{now},
        version = version + 1
    WHERE user_id = #{userId}
      AND article_id = #{articleId}
      AND deleted = 0
  </update>

  <select id="countActiveFavorite" resultType="long">
    SELECT COUNT(1)
    FROM article_favorite
    WHERE user_id = #{userId}
      AND article_id = #{articleId}
      AND deleted = 0
  </select>
</mapper>
```

`FavoriteServiceImpl` 使用以下完整关系方法；`requireUser` 在用户不存在时抛出 `BusinessException("用户不存在")`：

```java
@Service
public class FavoriteServiceImpl implements FavoriteService {
    private final ArticleFavoriteMapper favoriteMapper;
    private final ArticleService articleService;
    private final UserService userService;

    public FavoriteServiceImpl(ArticleFavoriteMapper favoriteMapper,
                               ArticleService articleService,
                               UserService userService) {
        this.favoriteMapper = favoriteMapper;
        this.articleService = articleService;
        this.userService = userService;
    }

    @Override
    @Transactional
    public void favoriteArticle(Long articleId, String username) {
        User user = requireUser(username);
        Article article;
        try {
            article = articleService.getPublicArticleSummary(articleId);
        } catch (BusinessException exception) {
            throw new BusinessException("文章不存在或暂不可收藏");
        }
        favoriteMapper.upsertFavorite(
                IdWorker.getId(), user.getId(), articleId, article.getTitle(),
                username, LocalDateTime.now());
    }

    @Override
    @Transactional
    public void unfavoriteArticle(Long articleId, String username) {
        User user = requireUser(username);
        favoriteMapper.cancelFavorite(user.getId(), articleId, username, LocalDateTime.now());
    }

    @Override
    public FavoriteStatus getFavoriteStatus(Long articleId, String username) {
        User user = requireUser(username);
        return new FavoriteStatus(favoriteMapper.countActiveFavorite(user.getId(), articleId) > 0);
    }

    private User requireUser(String username) {
        User user = userService.getCurrentUser(username);
        if (user == null) {
            throw new BusinessException("用户不存在");
        }
        return user;
    }
}
```

- [ ] **Step 4: 增加 Mapper XML 契约断言**

在 `DatabaseConfigTest` 增加读取 `src/main/resources/mapper/ArticleFavoriteMapper.xml` 的测试，断言同时包含：

```java
assertTrue(xml.contains("ON DUPLICATE KEY UPDATE"));
assertTrue(xml.contains("IF(deleted = 1, VALUES(title_snapshot), title_snapshot)"));
assertTrue(xml.contains("AND deleted = 0"));
assertTrue(xml.contains("version = version + 1"));
```

- [ ] **Step 5: 运行服务和 Mapper 契约测试**

Run: `mvn -q -Dtest=FavoriteServiceImplTest,DatabaseConfigTest test`

Expected: PASS，新增、幂等、恢复、取消和当前用户边界测试通过。

- [ ] **Step 6: 提交关系操作**

```text
git add blog-backend/src/main/java/com/blog/dto/FavoriteStatus.java blog-backend/src/main/java/com/blog/mapper/ArticleFavoriteMapper.java blog-backend/src/main/java/com/blog/service/FavoriteService.java blog-backend/src/main/java/com/blog/service/impl/FavoriteServiceImpl.java blog-backend/src/main/resources/mapper/ArticleFavoriteMapper.xml blog-backend/src/test/java/com/blog/service/FavoriteServiceImplTest.java blog-backend/src/test/java/com/blog/config/DatabaseConfigTest.java
git commit -m "feat: 实现文章收藏关系操作"
```

### Task 4: 实现带不可用占位的收藏分页查询

**Files:**
- Create: `blog-backend/src/main/java/com/blog/dto/FavoritePageQuery.java`
- Create: `blog-backend/src/main/java/com/blog/dto/FavoriteRelationRow.java`
- Create: `blog-backend/src/main/java/com/blog/dto/FavoriteArticleItem.java`
- Modify: `blog-backend/src/main/java/com/blog/mapper/ArticleFavoriteMapper.java`
- Modify: `blog-backend/src/main/resources/mapper/ArticleFavoriteMapper.xml`
- Modify: `blog-backend/src/main/java/com/blog/service/FavoriteService.java`
- Modify: `blog-backend/src/main/java/com/blog/service/impl/FavoriteServiceImpl.java`
- Modify: `blog-backend/src/test/java/com/blog/service/FavoriteServiceImplTest.java`

- [ ] **Step 1: 写入分页装配失败测试**

构造一条可用关系和一条不可用关系：

```java
@Test
void getMyFavorites_shouldReturnPublicArticleAndSafeUnavailableSnapshot() {
    when(userService.getCurrentUser("alice")).thenReturn(user(10L, "alice"));
    FavoriteRelationRow available = relation(1L, 20L, "Old public title", true);
    FavoriteRelationRow unavailable = relation(2L, 21L, "Saved private title", false);
    Page<FavoriteRelationRow> rows = new Page<>(1, 10);
    rows.setTotal(2);
    rows.setRecords(Arrays.asList(available, unavailable));
    when(favoriteMapper.selectFavoritePage(any(Page.class), eq(10L), eq("architecture"), eq(3L)))
            .thenReturn(rows);
    Article current = new Article();
    current.setId(20L);
    current.setTitle("Current public title");
    current.setSummary("Public summary");
    current.setTags(Collections.singletonList(tag(3L, "Java")));
    when(articleService.getPublicArticleSummaries(Collections.singletonList(20L)))
            .thenReturn(Collections.singletonList(current));
    FavoritePageQuery query = new FavoritePageQuery();
    query.setKeyword("architecture");
    query.setTagId(3L);

    IPage<FavoriteArticleItem> result = favoriteService.getMyFavorites(query, "alice");

    assertThat(result.getTotal()).isEqualTo(2);
    assertThat(result.getRecords().get(0).isAvailable()).isTrue();
    assertThat(result.getRecords().get(0).getTitle()).isEqualTo("Current public title");
    FavoriteArticleItem hidden = result.getRecords().get(1);
    assertThat(hidden.isAvailable()).isFalse();
    assertThat(hidden.getTitle()).isEqualTo("Saved private title");
    assertThat(hidden.getSummary()).isNull();
    assertThat(hidden.getTags()).isEmpty();
    assertThat(hidden.getUnavailableMessage()).isEqualTo("该文章暂未公开");
}
```

- [ ] **Step 2: 运行测试并确认红灯**

Run: `mvn -q -Dtest=FavoriteServiceImplTest#getMyFavorites_shouldReturnPublicArticleAndSafeUnavailableSnapshot test`

Expected: FAIL，分页 DTO 和查询方法尚不存在。

- [ ] **Step 3: 添加分页 DTO**

`FavoritePageQuery` 包含默认 `page = 1`、`size = 10`、`keyword` 和 `tagId`。

`FavoriteRelationRow` 字段：

```java
private Long favoriteId;
private Long articleId;
private String titleSnapshot;
private LocalDateTime favoritedAt;
private boolean available;
```

`FavoriteArticleItem` 字段：

```java
private Long articleId;
private String title;
private String summary;
private String coverImage;
private String authorName;
private List<Tag> tags = Collections.emptyList();
private Integer viewCount;
private LocalDateTime favoritedAt;
private boolean available;
private String unavailableMessage;
```

- [ ] **Step 4: 添加数据库级分页 SQL**

Mapper 增加：

```java
IPage<FavoriteRelationRow> selectFavoritePage(Page<FavoriteRelationRow> page,
                                               @Param("userId") Long userId,
                                               @Param("keyword") String keyword,
                                               @Param("tagId") Long tagId);
```

XML 查询必须完整包含以下结构：

```xml
<select id="selectFavoritePage" resultType="com.blog.dto.FavoriteRelationRow">
  SELECT f.id AS favorite_id,
         f.article_id,
         f.title_snapshot,
         f.created_at AS favorited_at,
         CASE WHEN a.id IS NOT NULL
                   AND a.deleted = 0
                   AND a.status = 'published'
                   AND (a.visibility = 'public' OR a.visibility IS NULL)
              THEN 1 ELSE 0 END AS available
  FROM article_favorite f
  LEFT JOIN article a ON a.id = f.article_id
  WHERE f.user_id = #{userId}
    AND f.deleted = 0
  <if test="keyword != null and keyword != ''">
    AND (
      (a.id IS NOT NULL AND a.deleted = 0 AND a.status = 'published'
       AND (a.visibility = 'public' OR a.visibility IS NULL)
       AND (a.title LIKE CONCAT('%', #{keyword}, '%')
            OR a.summary LIKE CONCAT('%', #{keyword}, '%')))
      OR
      ((a.id IS NULL OR a.deleted != 0 OR a.status != 'published'
        OR (a.visibility != 'public' AND a.visibility IS NOT NULL))
       AND f.title_snapshot LIKE CONCAT('%', #{keyword}, '%'))
    )
  </if>
  <if test="tagId != null">
    AND a.id IS NOT NULL
    AND a.deleted = 0
    AND a.status = 'published'
    AND (a.visibility = 'public' OR a.visibility IS NULL)
    AND EXISTS (
      SELECT 1 FROM article_tag at
      WHERE at.article_id = f.article_id
        AND at.tag_id = #{tagId}
        AND at.deleted = 0
    )
  </if>
  ORDER BY f.created_at DESC, f.id DESC
</select>
```

- [ ] **Step 5: 装配安全响应**

`FavoriteService` 增加：

```java
IPage<FavoriteArticleItem> getMyFavorites(FavoritePageQuery query, String username);
```

在 `FavoriteServiceImpl` 增加完整分页和装配方法：

```java
@Override
public IPage<FavoriteArticleItem> getMyFavorites(FavoritePageQuery query, String username) {
    User user = requireUser(username);
    Page<FavoriteRelationRow> requestPage = new Page<>(query.getPage(), query.getSize());
    IPage<FavoriteRelationRow> relationPage = favoriteMapper.selectFavoritePage(
            requestPage, user.getId(), query.getKeyword(), query.getTagId());

    List<Long> availableIds = relationPage.getRecords().stream()
            .filter(FavoriteRelationRow::isAvailable)
            .map(FavoriteRelationRow::getArticleId)
            .collect(Collectors.toList());
    Map<Long, Article> articlesById = articleService.getPublicArticleSummaries(availableIds).stream()
            .collect(Collectors.toMap(Article::getId, Function.identity()));
    List<FavoriteArticleItem> items = relationPage.getRecords().stream()
            .map(row -> toFavoriteItem(row, articlesById.get(row.getArticleId())))
            .collect(Collectors.toList());

    Page<FavoriteArticleItem> result = new Page<>(
            relationPage.getCurrent(), relationPage.getSize(), relationPage.getTotal());
    result.setRecords(items);
    return result;
}

private FavoriteArticleItem toFavoriteItem(FavoriteRelationRow row, Article article) {
    FavoriteArticleItem item = new FavoriteArticleItem();
    item.setArticleId(row.getArticleId());
    item.setFavoritedAt(row.getFavoritedAt());
    if (!row.isAvailable() || article == null) {
        item.setTitle(row.getTitleSnapshot());
        item.setAvailable(false);
        item.setTags(Collections.emptyList());
        item.setUnavailableMessage("该文章暂未公开");
        return item;
    }
    item.setTitle(article.getTitle());
    item.setSummary(article.getSummary());
    item.setCoverImage(article.getCoverImage());
    item.setAuthorName(article.getAuthorName());
    item.setTags(article.getTags() == null ? Collections.emptyList() : article.getTags());
    item.setViewCount(article.getViewCount());
    item.setAvailable(true);
    return item;
}
```

需要新增 `Function`、`Map` 和 `Collectors` 等对应导入。若文章在关系分页和公开摘要查询之间失效，`article == null` 分支会降级为安全快照。

- [ ] **Step 6: 运行收藏服务全量测试**

Run: `mvn -q -Dtest=FavoriteServiceImplTest test`

Expected: PASS，可用和不可用条目、关键词/标签参数、分页总数和信息边界测试通过。

- [ ] **Step 7: 提交分页查询**

```text
git add blog-backend/src/main/java/com/blog/dto/FavoritePageQuery.java blog-backend/src/main/java/com/blog/dto/FavoriteRelationRow.java blog-backend/src/main/java/com/blog/dto/FavoriteArticleItem.java blog-backend/src/main/java/com/blog/mapper/ArticleFavoriteMapper.java blog-backend/src/main/java/com/blog/service/FavoriteService.java blog-backend/src/main/java/com/blog/service/impl/FavoriteServiceImpl.java blog-backend/src/main/resources/mapper/ArticleFavoriteMapper.xml blog-backend/src/test/java/com/blog/service/FavoriteServiceImplTest.java
git commit -m "feat: 实现我的收藏分页查询"
```

### Task 5: 暴露认证收藏接口

**Files:**
- Create: `blog-backend/src/main/java/com/blog/controller/FavoriteController.java`
- Create: `blog-backend/src/test/java/com/blog/controller/FavoriteControllerTest.java`
- Modify: `blog-backend/src/test/java/com/blog/controller/ArticleRouteMappingTest.java`

- [ ] **Step 1: 写入 Controller 失败测试**

直接调用 Controller，验证 Principal 透传和响应：

```java
@Test
void endpointsUseCurrentPrincipal() {
    FavoritePageQuery query = new FavoritePageQuery();
    Page<FavoriteArticleItem> page = new Page<>(1, 10);
    when(favoriteService.getMyFavorites(query, "alice")).thenReturn(page);
    when(favoriteService.getFavoriteStatus(20L, "alice"))
            .thenReturn(new FavoriteStatus(true));

    assertEquals(200, controller.list(query, principal).getCode());
    assertTrue(controller.status(20L, principal).getData().isFavorited());
    assertEquals(200, controller.favorite(20L, principal).getCode());
    assertEquals(200, controller.unfavorite(20L, principal).getCode());

    verify(favoriteService).getMyFavorites(query, "alice");
    verify(favoriteService).getFavoriteStatus(20L, "alice");
    verify(favoriteService).favoriteArticle(20L, "alice");
    verify(favoriteService).unfavoriteArticle(20L, "alice");
}
```

反射断言 Controller 类存在 `@PreAuthorize("hasAnyRole('USER', 'ADMIN')")`。

- [ ] **Step 2: 运行测试并确认红灯**

Run: `mvn -q -Dtest=FavoriteControllerTest test`

Expected: FAIL，Controller 尚不存在。

- [ ] **Step 3: 添加 Controller**

```java
@RestController
@RequestMapping("/api/my/favorites")
@PreAuthorize("hasAnyRole('USER', 'ADMIN')")
public class FavoriteController {
    private final FavoriteService favoriteService;

    public FavoriteController(FavoriteService favoriteService) {
        this.favoriteService = favoriteService;
    }

    @GetMapping
    public PageResult<FavoriteArticleItem> list(FavoritePageQuery query, Principal principal) {
        return PageResult.success(favoriteService.getMyFavorites(query, principal.getName()));
    }

    @GetMapping("/{articleId}/status")
    public Result<FavoriteStatus> status(@PathVariable Long articleId, Principal principal) {
        return Result.success(favoriteService.getFavoriteStatus(articleId, principal.getName()));
    }

    @PutMapping("/{articleId}")
    public Result<Void> favorite(@PathVariable Long articleId, Principal principal) {
        favoriteService.favoriteArticle(articleId, principal.getName());
        return Result.success();
    }

    @DeleteMapping("/{articleId}")
    public Result<Void> unfavorite(@PathVariable Long articleId, Principal principal) {
        favoriteService.unfavoriteArticle(articleId, principal.getName());
        return Result.success();
    }
}
```

在 `ArticleRouteMappingTest` 增加 MockMvc 路由断言，未认证请求应被安全层拒绝，带 `ROLE_USER` 的请求可以到达 Controller。

- [ ] **Step 4: 运行 Controller 和安全测试**

Run: `mvn -q -Dtest=FavoriteControllerTest,ArticleRouteMappingTest test`

Expected: PASS，四个接口路径和用户角色边界正确。

- [ ] **Step 5: 运行后端全量测试并提交**

Run: `mvn -q test`

Expected: 所有后端测试通过，Failures 和 Errors 为 0。

```text
git add blog-backend/src/main/java/com/blog/controller/FavoriteController.java blog-backend/src/test/java/com/blog/controller/FavoriteControllerTest.java blog-backend/src/test/java/com/blog/controller/ArticleRouteMappingTest.java
git commit -m "feat: 添加文章收藏接口"
```

### Task 6: 添加前端收藏 API 和纯函数状态规则

**Files:**
- Create: `blog-frontend/src/api/favorite.js`
- Create: `blog-frontend/src/utils/favorite.js`
- Create: `blog-frontend/src/utils/favorite.test.mjs`

- [ ] **Step 1: 写入前端纯函数失败测试**

```javascript
import test from 'node:test'
import assert from 'node:assert/strict'
import {
  buildFavoriteListParams,
  buildFavoriteLoginRedirect,
  clearFavoriteIntentQuery,
  hasFavoriteIntent
} from './favorite.js'

test('favorite login intent returns to article and is consumed once', () => {
  assert.equal(buildFavoriteLoginRedirect('758902345678901401'),
    '/article/758902345678901401?favorite=1')
  assert.equal(hasFavoriteIntent({ favorite: '1' }), true)
  assert.deepEqual(clearFavoriteIntentQuery({ favorite: '1', from: 'home' }), { from: 'home' })
})

test('favorite list params trim filters and preserve snowflake tag ids', () => {
  assert.deepEqual(buildFavoriteListParams({
    page: 2,
    size: 10,
    keyword: '  architecture  ',
    tagId: '758902345678901301'
  }), {
    page: 2,
    size: 10,
    keyword: 'architecture',
    tagId: '758902345678901301'
  })
})
```

- [ ] **Step 2: 运行测试并确认红灯**

Run: `node --test src/utils/favorite.test.mjs`

Expected: FAIL，`favorite.js` 尚不存在。

- [ ] **Step 3: 添加 API 和纯函数**

`api/favorite.js`：

```javascript
import request from './request'

export const getFavorites = params => request.get('/my/favorites', { params })
export const getFavoriteStatus = articleId => request.get(`/my/favorites/${articleId}/status`, {
  skipErrorMessage: true
})
export const favoriteArticle = articleId => request.put(`/my/favorites/${articleId}`)
export const unfavoriteArticle = articleId => request.delete(`/my/favorites/${articleId}`)
```

`utils/favorite.js`：

```javascript
export function buildFavoriteLoginRedirect(articleId) {
  return `/article/${articleId}?favorite=1`
}

export function hasFavoriteIntent(query = {}) {
  return query.favorite === '1'
}

export function clearFavoriteIntentQuery(query = {}) {
  const next = { ...query }
  delete next.favorite
  return next
}

export function buildFavoriteListParams({ page = 1, size = 10, keyword = '', tagId = '' } = {}) {
  const params = { page, size }
  const normalizedKeyword = keyword.trim()
  if (normalizedKeyword) params.keyword = normalizedKeyword
  if (tagId !== '' && tagId !== null && tagId !== undefined) params.tagId = tagId
  return params
}
```

- [ ] **Step 4: 运行前端单元测试并提交**

Run: `npm.cmd test`

Expected: 新增收藏工具测试和现有 35 个测试全部通过。

```text
git add blog-frontend/src/api/favorite.js blog-frontend/src/utils/favorite.js blog-frontend/src/utils/favorite.test.mjs
git commit -m "feat: 添加前端收藏状态基础能力"
```

### Task 7: 接入文章详情收藏和登录自动续接

**Files:**
- Create: `blog-frontend/scripts/verify-favorite-ui.mjs`
- Modify: `blog-frontend/src/views/ArticleDetail.vue`

- [ ] **Step 1: 写入详情页静态失败校验**

脚本读取 `ArticleDetail.vue` 并断言包含：

```javascript
const required = [
  'favoriteArticle',
  'unfavoriteArticle',
  'getFavoriteStatus',
  'buildFavoriteLoginRedirect',
  'hasFavoriteIntent',
  'clearFavoriteIntentQuery',
  'favoriteLoading',
  '已收藏',
  '收藏'
]
```

任一缺失时输出文件和缺失文本并 `process.exit(1)`。

- [ ] **Step 2: 运行脚本并确认红灯**

Run: `node scripts/verify-favorite-ui.mjs`

Expected: FAIL，详情页尚未接入收藏。

- [ ] **Step 3: 添加稳定收藏按钮**

在文章标题区标签下方增加固定尺寸按钮：未收藏使用 `Star`，已收藏使用 `StarFilled`，按钮文案分别为“收藏”和“已收藏”；绑定 `favoriteLoading` 并在请求期间禁用。

脚本状态和函数使用以下结构：

```javascript
const authStore = useAuthStore()
const favorited = ref(false)
const favoriteLoading = ref(false)

async function loadFavoriteState(articleId) {
  if (!authStore.isLoggedIn) {
    favorited.value = false
    return
  }
  try {
    const result = await getFavoriteStatus(articleId)
    favorited.value = !!result.data?.favorited
  } catch {
    favorited.value = false
  }
}

async function setFavorite(articleId, nextValue) {
  if (favoriteLoading.value) return false
  favoriteLoading.value = true
  try {
    if (nextValue) await favoriteArticle(articleId)
    else await unfavoriteArticle(articleId)
    favorited.value = nextValue
    ElMessage.success(nextValue ? '已收藏' : '已取消收藏')
    return true
  } catch {
    return false
  } finally {
    favoriteLoading.value = false
  }
}

async function handleFavorite() {
  if (!article.value) return
  if (!authStore.isLoggedIn) {
    router.push({
      path: '/login',
      query: { redirect: buildFavoriteLoginRedirect(article.value.id) }
    })
    return
  }
  await setFavorite(article.value.id, !favorited.value)
}

async function initializeFavorite(articleId, token) {
  if (!authStore.isLoggedIn || token !== loadToken.value) return
  if (hasFavoriteIntent(route.query)) {
    try {
      await setFavorite(articleId, true)
    } finally {
      await router.replace({ query: clearFavoriteIntentQuery(route.query) })
    }
    return
  }
  await loadFavoriteState(articleId)
}
```

在文章加载成功且 `article.value` 已赋值后异步调用 `initializeFavorite(r.data.id, token)`，不要让收藏状态请求阻塞正文 loading。文章切换时先清空收藏状态；`setFavorite` 已捕获接口错误，因此不得修改正文错误状态。

- [ ] **Step 4: 运行收藏校验、前端测试和构建**

Run: `node scripts/verify-favorite-ui.mjs`

Expected: PASS。

Run: `npm.cmd test`

Expected: 所有前端单元测试通过。

Run: `npm.cmd run build`

Expected: 构建成功，允许保留现有包体积提示。

- [ ] **Step 5: 提交详情页收藏**

```text
git add blog-frontend/scripts/verify-favorite-ui.mjs blog-frontend/src/views/ArticleDetail.vue
git commit -m "feat: 添加文章详情收藏交互"
```

### Task 8: 添加我的收藏页面、不可用项和菜单入口

**Files:**
- Create: `blog-frontend/src/components/FavoriteArticleItem.vue`
- Create: `blog-frontend/src/views/Favorites.vue`
- Modify: `blog-frontend/src/router/index.js`
- Modify: `blog-frontend/src/components/AppHeader.vue`
- Modify: `blog-frontend/scripts/verify-favorite-ui.mjs`

- [ ] **Step 1: 扩展静态失败校验**

在脚本中读取新页面、列表项、路由和 Header，并断言：

```javascript
const requiredRoutes = ["path: '/favorites'", 'requiresAuth: true']
const requiredPageText = [
  'getFavorites', 'unfavoriteArticle', 'buildFavoriteListParams',
  '关键词', '标签', '重置', 'el-pagination', '尚未收藏文章', '当前筛选无结果'
]
const requiredItemText = [
  'item.available', '该文章暂未公开', 'favoritedAt', '取消收藏', 'el-tooltip'
]
```

Header 必须包含“我的收藏”和 `/favorites`。

- [ ] **Step 2: 运行脚本并确认红灯**

Run: `node scripts/verify-favorite-ui.mjs`

Expected: FAIL，收藏页面、路由和菜单入口尚不存在。

- [ ] **Step 3: 添加收藏列表项组件**

组件 props 为必填 `item`，事件为 `open` 和 `remove`：

- 根元素根据 `item.available` 添加 `is-unavailable` 类。
- 可用标题按钮触发 `open`；不可用标题放入 `el-tooltip`，不触发跳转。
- 只在可用时渲染摘要、封面、作者、阅读数和标签。
- 始终展示 `formatDate(item.favoritedAt)`。
- `Delete` 图标按钮固定为 36x36，标题和 `aria-label` 都是“取消收藏”，点击停止冒泡并触发 `remove`。
- 不可用样式降低颜色和背景对比，但正文与提示仍满足可读性；移动端标题和按钮不得重叠。

- [ ] **Step 4: 添加收藏页面**

页面状态：

```javascript
const items = ref([])
const tags = ref([])
const page = ref(1)
const size = ref(10)
const total = ref(0)
const keywordInput = ref('')
const keyword = ref('')
const tagId = ref('')
const loading = ref(false)
const loadError = ref('')
```

`load()` 使用 `buildFavoriteListParams` 请求分页；搜索、标签变化和重置都把页码设为 1。取消成功后，如果当前页清空且页码大于 1，先减页再加载，否则重新加载当前页。`open` 仅对 `available` 条目执行 `router.push('/article/' + articleId)`。

页面使用 `AppHeader`，工具栏由输入框、标签选择、搜索和重置组成；加载错误显示可重试 `el-alert`，没有收藏和筛选无结果使用不同 `el-empty` 文案。

- [ ] **Step 5: 添加认证路由和菜单**

路由：

```javascript
{
  path: '/favorites',
  name: 'Favorites',
  component: () => import('../views/Favorites.vue'),
  meta: { requiresAuth: true }
}
```

在用户下拉菜单“我的文章”附近增加“我的收藏”，点击跳转 `/favorites`。

- [ ] **Step 6: 运行前端完整验证**

Run: `node scripts/verify-favorite-ui.mjs`

Expected: PASS，详情、页面、路由、菜单和不可用状态全部接线。

Run: `npm.cmd test`

Expected: 所有前端测试通过。

Run: `npm.cmd run build`

Expected: 构建成功。

- [ ] **Step 7: 启动前后端并做浏览器验证**

使用 README 中的启动命令。验证桌面 1440x900、手机 390x844 的亮色和暗色主题：收藏按钮不改变标题区尺寸；收藏工具栏不溢出；不可用标题、提示和取消按钮不重叠；登录后自动收藏只执行一次。

- [ ] **Step 8: 提交收藏页面**

```text
git add blog-frontend/src/components/FavoriteArticleItem.vue blog-frontend/src/views/Favorites.vue blog-frontend/src/router/index.js blog-frontend/src/components/AppHeader.vue blog-frontend/scripts/verify-favorite-ui.mjs
git commit -m "feat: 添加我的收藏页面"
```

### Task 9: 更新项目状态并完成全量验证

**Files:**
- Modify: `docs/项目状态与目标.md`

- [ ] **Step 1: 更新功能完成状态**

在“已完成功能”的公开阅读面增加：详情收藏按钮、登录续接、我的收藏搜索/标签/分页、不可用标题快照。

将“当前阶段目标”调整为阅读历史，把收藏从“已排期但未开始”删除；阅读历史继续保留为下一优先级。不要把点赞、评论、趋势或排行移入计划。

- [ ] **Step 2: 运行后端全量测试**

Run: `mvn -q test`

Expected: Failures 和 Errors 均为 0。

- [ ] **Step 3: 运行前端全量测试、静态校验和构建**

Run: `npm.cmd test`

Run: `node scripts/verify-login-redirect.mjs`

Run: `node scripts/verify-dark-table-theme.mjs`

Run: `node scripts/verify-creator-pages.mjs`

Run: `node scripts/verify-article-review-api.mjs`

Run: `node scripts/verify-article-back-navigation.mjs`

Run: `node scripts/verify-admin-review-ui.mjs`

Run: `node scripts/verify-favorite-ui.mjs`

Run: `npm.cmd run build`

Expected: 所有命令退出码为 0；生产构建只允许保留已经记录的包体积提示。

- [ ] **Step 4: 复查范围并提交文档**

Run: `git status --short`

Expected: 只包含 `docs/项目状态与目标.md`。

```text
git add docs/项目状态与目标.md
git commit -m "docs: 更新文章收藏功能完成状态"
```

- [ ] **Step 5: 最终提交和工作区检查**

Run: `git status --short`

Expected: 工作区为空。

Run: `git log -10 --oneline`

Expected: 计划中的数据模型、公开摘要、关系操作、分页查询、接口、前端基础、详情交互、收藏页面和状态文档提交均存在，且每次提交未混入无关文件。
